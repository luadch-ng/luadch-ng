# Changelog

All notable changes to the `luadch-ng/luadch-ng` fork are documented here.
The repo lived at `Aybook/luadch` before the v3.1.x line; auto-redirects
handle historic links.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

The upstream project (`luadch/luadch`) is a separate codebase; its release
history is at https://github.com/luadch/luadch/releases.

## [Unreleased] - 3.2.x line

Phase-8 feature development on the 3.2.x line (no `v3.2.0` tag yet).
The modernisation programme (Phases 1-7) and the large Phase-8 arcs
recorded below have all merged to `master`: the HTTP API
([#82](https://github.com/luadch-ng/luadch-ng/issues/82)), audit log
([#84](https://github.com/luadch-ng/luadch-ng/issues/84)), Prometheus
metrics ([#83](https://github.com/luadch-ng/luadch-ng/issues/83)), the
deferred [#147](https://github.com/luadch-ng/luadch-ng/issues/147) items
(T2 BLOM, T3 ZLIF), the unified blocklist + in-hub GeoIP
([#78](https://github.com/luadch-ng/luadch-ng/issues/78) /
[#79](https://github.com/luadch-ng/luadch-ng/issues/79) /
[#352](https://github.com/luadch-ng/luadch-ng/issues/352)), and the
automatic-backup arc
([#480](https://github.com/luadch-ng/luadch-ng/issues/480)). The open
enhancement arcs that remain are
[#86](https://github.com/luadch-ng/luadch-ng/issues/86) (share-policy),
[#100](https://github.com/luadch-ng/luadch-ng/issues/100)
(self-registration) and
[#122](https://github.com/luadch-ng/luadch-ng/issues/122) (filelist
scanner). Security-fixes-only for the v3.1.x line land on
`release/3.1.x` per
[`CLAUDE.md` §8](CLAUDE.md#8-release-lines-and-support-policy).

> **Note:** This section tracks the 3.2.x line. Some entries here were
> also cherry-picked to `release/3.1.x` and shipped in a `v3.1.x` tag
> (master-first, then backport, per the §8 workflow); the latest such
> maintenance release is
> **[v3.1.15](https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.15)**
> (2026-07-29). Which specific entries a given tag carries is recorded
> on the `release/3.1.x` tags themselves, not tracked per-entry here.

### Breaking

- **`cmd_ascii` removed from the bundled scripts (`+ascii` no longer ships).** The plugin (ASCII-art pictures to main chat) is retired from `luadch-ng/luadch-ng` as part of the pre-3.2.0 lang **Lua -> JSON / Weblate** migration: its lang files do not fit the monolingual `key -> string` model Weblate needs - the keys themselves are **localised** (`pics.afk` en vs `pics.weg` de: the typed command name is translated, so the key set differs per language) and the art is a `function(user)` closure, not a string. Rather than redesign a niche 654-line-of-art plugin, it moves to the companion [`luadch-ng/scripts`](https://github.com/luadch-ng/scripts) repo (`scripts/cmd_ascii/`, v0.5 unchanged). Removed from `core/cfg_defaults.lua` (the `cmd_ascii_minlevel` default + the `scripts` whitelist), `examples/cfg/cfg.tbl` (both), `docs/SCRIPTS.md` and the `docs/HTTP_API.md` §11 command list. **Action for operators who use it:** it keeps working on an in-place upgrade (your `scripts/cmd_ascii.lua` + `cfg.scripts` entry are yours and are not touched); for a fresh install or a Docker image, drop `scripts/cmd_ascii/` from the companion repo in and add `"cmd_ascii.lua"` back to `cfg.scripts`. 3.2.x only.

- **`kill_wrong_ips` default flipped from `true` to `false`.** Safe because [#214](https://github.com/luadch-ng/luadch-ng/issues/214) Gap 2 (shipped v3.1.10) already overrides any client-claimed mismatched IP with the authenticated TCP source IP before broadcasting, regardless of this toggle. Effect: clients whose advertised IP does not match their authenticated TCP source (stale VPN IP, hand-set wrong WAN IP, dual-stack family mismatch) now stay connected instead of being kicked; the toggle only concerns that mismatch and is unrelated to how the hub blocks users (e.g. the Traffic Manager decides on account, not IP). Kick message itself was upgraded in PR [#331](https://github.com/luadch-ng/luadch-ng/pull/331). **Operators who prefer the loud kick:** set `kill_wrong_ips = true` in `cfg.tbl`. Trade-off detail in [`docs/SECURITY.md`](docs/SECURITY.md). 3.2.x only.

- [#231](https://github.com/luadch-ng/luadch-ng/issues/231) - HTTP API now requires an **explicit token in cfg.tbl** before binding the listener. Previously the first-boot bootstrap generated a token AND auto-activated it in-memory, so the API "just worked" on first boot. That mechanism had a footgun: `+reload` reads `cfg.tbl` fresh and silently wiped the in-memory token, locking the operator out until a process restart (which then generated a NEW token, overwriting `api_token.first`). Post-#231: hub writes a sample token to `cfg/api_token.first` (chmod 600) when `http_port` is set but `http_api_tokens` is empty, logs a warning, and does NOT bind the listener. Operator must copy the value into `cfg.tbl http_api_tokens` and restart (or `+reload`) to activate. `cfg.tbl` is now the single source of truth. **Action required for operators:** if you upgrade with `http_port` set but no token in `cfg.tbl`, the HTTP API will not be reachable until you copy the sample value over. Details in [`docs/HTTP_API.md` §4.7](docs/HTTP_API.md). 3.2.x only, not backported.

- [#453](https://github.com/luadch-ng/luadch-ng/issues/453) - **`util.trimstring` now rejects non-string input with `(nil, err)` instead of stringifying it.** The `util.lua` string helper opened with `local str = tostring( str )`, so `trimstring( nil )` silently returned the literal `"nil"` (and `trimstring( {} )` a `"table: 0x.."` address) - a data-corruption footgun for callers that trusted the type guard that sat dead for 15 years and was removed in [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 4. It now returns `(nil, err)` for any non-string, matching the `util.*` "errors return nil, err" convention. `util.encode` / `util.decode` deliberately keep their `tostring` coercion (zero callers, harmless round-trip) and are only documented, not changed. No consumer needed a companion change: `trimstring` has exactly one caller across both repos (`etc_requests` in `luadch-ng/scripts`), and its command dispatch already gates empty input, so `rel` never reaches it as a non-string - the hardening is contract cleanup that removes the footgun for any future or third-party caller. **Action required for third-party plugins:** if you pass a possibly-nil value to `util.trimstring`, guard the result (`local s, err = util.trimstring( x ); if not s then ... end`) - see [`docs/PLUGIN_API.md` §5.3](docs/PLUGIN_API.md). First-ever unit coverage added (`tests/unit/util_string_test.lua`). 3.2.x only.

### Tests

- **Tooling: bulk-apply Weblate component settings (`tools/weblate_apply_settings.py`).** Weblate's linked components (the 70 plugins linked to Core) do NOT inherit per-component translation-workflow settings - "Adding new translation", "Turn on suggestions", "Suggestion voting" must be set on each of ~71 components. This applies them over the REST API, idempotently: **dry run by default** (lists what it would change, writes nothing), `APPLY=1` to write, and it skips a component already matching. Defaults restrict language creation to a maintainer request (`new_lang=contact`) and enable community suggestions + voting; edit the `DESIRED` dict to change policy. Same env/UA/slug conventions as `weblate_sync_components.py`. Runs via the `Weblate settings` **workflow_dispatch** workflow (`.github/workflows/weblate-settings.yml`) so it executes on the CI runner - a local run is blocked by the Cloudflare bot challenge on write methods, while the runner (the path that created the components) gets through. The workflow takes an `apply` checkbox (unchecked = dry run). 3.2.x only.

- **CI: automated, filtered Weblate translation funnel.** New `.github/workflows/weblate-funnel.yml` + `tools/weblate_funnel.py` open/update a `weblate` -> `dev` PR (weekly + on demand) carrying **only real translations**. Weblate writes a target language file the moment a language is *added* - 0% translated, all-empty values - and pushes it to the `weblate` branch, plus reformats files it touches without changing a value; a wholesale `weblate` -> `dev` merge dragged all that clutter in (the reason the first manual funnel PR was 20 languages of which 18 were empty). The script includes a language file only when its non-empty translated values differ from `dev`, so empty languages and formatting-only diffs are dropped. Because the lang unit tests only checked `de`, `lang_test.lua` + `plugin_lang_test.lua` gained an **`LANG_TEST_EXTRA_CODES`** env hook (unset in the normal smoke run -> unchanged `de`-only behaviour, no directory globbing so the Windows leg stays portable); the funnel enumerates the languages it imports on its Linux runner and passes them, so each imported language gets the same orphan-key + `%s`/`%d` placeholder-signature checks `de` gets before the PR is opened. The workflow does not auto-merge - a maintainer reviews/squashes. `docs/TRANSLATING.md` updated. CI/tooling only, 3.2.x only.

- **CI: auto-create the Weblate component for a new bundled plugin.** luadch has one Weblate component per plugin, and the "Component discovery" add-on that would auto-create them is unavailable on our instance, so a new plugin's component used to need creating by hand. New `.github/workflows/weblate-components.yml` runs `tools/weblate_sync_components.py` when a `scripts/lang/en/*.json` source file lands on `dev`: it creates only the *missing* components over the Weblate REST API (each linked to the Core component's repo via `weblate://`), idempotent and non-destructive (never modifies or deletes, skips existing). Runs on `dev` push + `workflow_dispatch` only - never on pull requests, so the `WEBLATE_API_TOKEN` secret is never exposed to fork PRs; `permissions: contents: read`. The script also seeds the initial full set. CI/tooling only, 3.2.x only.

- [#511](https://github.com/luadch-ng/luadch-ng/issues/511) - **CI: scope the smoke-workflow `push:` trigger to `dev` only.** `smoke.yml` ran on every branch push AND every PR, so a feature branch smoked once on its own push and again on its PR, and a `dev` -> `master` promotion re-smoked master for content already validated on `dev`. The `push:` trigger is now `branches: [dev]`; `pull_request:` stays unfiltered, so `feat` -> `dev` PRs, the `dev` -> `master` promotion PR, and security-backport PRs (base `master`) all still smoke - and every master-bound change lands via a PR, so nothing loses coverage. Also kills the duplicate push+PR runs that used to orphan/hang on feature branches. Docker image builds are untouched (`:dev` and `:master` are distinct published images, each genuinely built on its branch push - only the re-*test* was redundant). CI-only, 3.2.x only.

- [#408](https://github.com/luadch-ng/luadch-ng/issues/408) - **de-flaked the BLOM hash-search smoke tests (#147 T2.2).** The per-user bloom filter installs asynchronously after the client's HSND upload, and `_blom_filter_check` (core/hub_user_object.lua) fails open when no filter is installed yet - so on a loaded CI runner a not-in-filter search fired before install would be routed (fail-open) and trip "bloom filter not consulted". The positive control (a TTH IN the filter) is not a reliable install gate - it passes whether the filter is installed (`contains`) OR fail-opens - so the fixed `time.sleep(0.5)` before the negative case was the race. Fix (smoke-harness only): poll the negative case - re-send the not-in-filter search and only fail if it still reaches the user after 8s; the DROP is the sole observable proof the filter is installed + consulted, and a real "installed but not consulted" leak still fails past the deadline. Applied to both `test_blom_roundtrip` and `test_blom_zlif_combined`. No hub change. 3.2.x only.

- [#275](https://github.com/luadch-ng/luadch-ng/issues/275) - holistic HTTP API review follow-up, Coverage PR. Adds the smoke + unit coverage gaps the review surfaced: **COV-1 (BLOCKER)** auth scope matrix - new `SMOKE_READ_TOKEN` fixture (injected alongside the bootstrap admin token at active-mode setup) drives a parametric test that probes every admin endpoint family with a read-scope token and asserts 403 / E_FORBIDDEN, plus a positive control and an inverted-gate guard with the admin token; pre-PR the `_token_scope_ok` branch (~30 admin endpoints) was 0% covered. **COV-2 (BLOCKER)** Windows CI now runs the same five Lua-side unit tests as Linux (iostream / bloom / http_router / util_http / util_safe_path); `mingw-w64-ucrt-x86_64-lua` added to the msys2 install set. **COV-3 (BLOCKER)** `/v1/events` `cursor_lost` branch + "no waiting on stale cursor" rule now exercised: shrink `http_events_buffer_size` to 16 via PUT /v1/config (live cfg), burst 20 topic_changed events, assert `cursor_lost=true` and timed immediate-return on `wait=2`. **COV-5 (CONCERN)** X-Request-ID echo asserted on both 200 and 404 paths; spec §6.5 mandates echo, no test was checking. **COV-7 (CONCERN)** New `tests/unit/http_filter_test.lua` covers `core/http_filter.lua` end-to-end: string substring, integer exact / `_min` / `_max`, boolean, date `_after` / `_before` with nil-date row exclusion, sort asc / desc, pagination clamp, unknown-field + unknown-sort 400, and input-mutation safety. Wired into both Linux and Windows CI. **COV-N3 / COV-N4 (NITs)** positive GET /v1/users/{sid} via `_logged_in_user`, /v1/log/api?lines=1000/1001 boundary. Deferred to follow-up: COV-4 idempotency replay on reload/restart/shutdown, COV-6 PM-event scope filter, COV-N2 /metrics counter-value assertion. Two-pass review per §1a.6 caught the http_filter unit-test spec-shape drift (wrapped getters as `{get=fn}` instead of bare functions); fixed before push. 3.2.x only, tests of HTTP API.

### Added

- **New `GET /v1/gags` read endpoint (`cmd_gag` v0.15) - lists the gags the hub is currently enforcing.** Read counterpart to the write-only `POST` / `DELETE /v1/users/{sid}/gag` pair, added for the WebUI's status-aware Gag/Ungag toggle (show "Ungag" for a gagged user, "Gag" otherwise). Returns `data: {gags: [...]}`, each entry `{firstnick, mode, added_by, added_at, expires_at, sid}`: `firstnick` is the gag store's stable key, `added_at` / `expires_at` are ISO 8601 UTC (§7.4, matching the POST sibling and `/v1/bans`), and `sid` is the live session id resolved per firstnick (`null` if the gagged user is offline) so a client can join a gag onto `/v1/users[].sid` even under `usr_nick_prefix` (which re-keys the *displayed* nick but not the firstnick the gag store keys by). The list mirrors ADC `+gag show`: it reports every enforced gag, including one already past its `expires_at` that the 60s cleanup timer has not yet swept - the hub keeps muting until the sweep (`check_user_input` does not consult `expires_at`), so reporting it as active matches enforcement. Registered via raw `hub.http_register` (like `/v1/bans`), not the `util_http` per-`{sid}` helper - it is a hub-wide collection, not a single-user action. Read-scoped, no mutation. New unit test `cmd_gag_endpoint_test.lua` (all-enforced-listed incl. past-expiry, ISO 8601 timestamps, online-vs-offline sid) on both smoke.yml legs; [`docs/HTTP_API.md`](docs/HTTP_API.md) §10.2 updated. 3.2.x only.

- [#571](https://github.com/luadch-ng/luadch-ng/issues/571) - **new `+botflag <nick> on|off` command displays a registered account with the bot icon (ADC `CT` bot bit), no privilege change.** An external announcer client (e.g. Herald) logging in as a registered account can now render with the DC++ / AirDC++ bot icon without gaining any rights: `CT` is a display-only INF field the hub derives from level, and nothing reads it for authorization, so OR-ing the bot bit changes only the icon. A new dedicated profile flag `show_as_bot` (distinct from the load-bearing `is_bot`, which `hub_bot_cleaner` deletes while offline) is added to `core/hub.lua`'s `_regex.reguser` schema - without that schema line `updateusers()` would treat the flag as an unknown key and wipe `user.tbl` at boot - and `insertreglevel()` ORs `CT` bit 1 when the flag is set (takes effect at the account's next login). The new `scripts/cmd_botflag.lua` (hub-owner level, en/de lang) toggles it via mutate-in-place + `cfg.saveusers`, guards `is_bot` accounts, and resolves the target by firstnick ([#473](https://github.com/luadch-ng/luadch-ng/issues/473)/[#474](https://github.com/luadch-ng/luadch-ng/issues/474)). **Ships disabled** (`enabled = false` in `examples/cfg/cfg.tbl`). New unit test `cmd_botflag_test.lua` (handler guards, both CI legs) + smoke `test_botflag_ct_bit` (seeds a flagged account, asserts `CT` bit 1 at login - fails pre-fix on either the missing schema line or the missing CT change). 3.2.x only.

- **Dual-format language loader - lang files may now ship as JSON (`.json`), the first code step of the lang Lua -> JSON / Weblate migration** (the same migration the `cmd_ascii` removal above serves). `core/cfg_lang.lua`'s `loadlanguage` now probes a `.json` path first and falls back to the legacy Lua table when it is absent, so files migrate one at a time with no flag day. JSON files live in a per-language subdirectory (the Weblate-standard layout, tidy as languages are added): core `lang/<lng>/hub.json` before the legacy `lang/<lng>.tbl`, plugin `scripts/lang/<lng>/<name>.json` before the legacy flat `scripts/lang/<name>.lang.<lng>`. Backed by a new sandboxed reader `util.loadjsontable` that decodes with the bundled `dkjson` (pure-Lua data parsing - no `_ENV` / RCE surface, unlike the executable `.tbl`), rejects unsafe paths before `io.open` (#266), and is **silent on a missing file** so the incremental migration does not spam `error.log` on every boot / `+reload`; a file that exists but is malformed / non-utf8 / a non-object root is still a logged fault. No lang files are converted in this change, so runtime behaviour is unchanged - every existing `.tbl` / `.lang.X` loads exactly as before. New unit tests `util_loadjsontable_test.lua` (loader properties incl. the no-spam guarantee, real `dkjson`) and `cfg_lang_dualformat_test.lua` (JSON-first / .tbl-fallback contract + path shapes), both wired into the Linux and Windows smoke legs. 3.2.x only.

- **Core hub language files migrated `.tbl` -> JSON** (`lang/en/hub.json`, `lang/de/hub.json`) - the first files converted onto the dual-format loader above. Every key and string value is **byte-identical** to the old `.tbl` (verified by an independent `.tbl`-vs-`.json` value comparison + UTF-8 validation, not just a self round-trip); nothing was translated or reworded. The runtime loads them via `cfg_lang.loadlanguage` (JSON-first), the old `lang/en.tbl` / `lang/de.tbl` are removed, and `tests/unit/lang_test.lua` now validates the JSON with the same 110 required-key / de-en-parity / one-`%s` assertions (decoded via the same bundled `dkjson` the hub uses). `docs/CONFIGURATION.md` updated. Plugin lang files follow in the same layout - see the next entry. 3.2.x only.

- **All 70 bundled plugin language files migrated to JSON in a per-language subdirectory** - `scripts/lang/<name>.lang.{en,de}` (140 files) become `scripts/lang/{en,de}/<name>.json`. This is the layout Weblate expects (`scripts/lang/*/<name>.json`, `*` = language) and the one that stays maintainable as more languages land - a flat directory would grow to (plugins x languages) entries. Every key and value is **byte-identical** to the old Lua tables, including nested `ucmd_menu*` / `month_name` tables (arrays stay arrays) and the CR bytes inside a few CRLF-authored multiline strings (preserved as JSON `\r` escapes - zero behaviour change): verified by a strict deep-equal round-trip and an independent per-file `.lang.X`-vs-`.json` comparison across all 140, plus UTF-8 validation. The loader is dual-format so a third-party plugin still shipping a flat `.lang.<lng>` Lua table keeps working; `tests/unit/plugin_lang_test.lua` (4518 key-parity checks) and `cmd_userinfo_align_test.lua` load whichever format is present. The Docker entrypoint's add-only lang autosync (`docker/entrypoint.sh`), `docs/PLUGIN_API.md` §3.5, `DEVELOPMENT.md`, `DOCKER.md` and `CONFIGURATION.md` are updated for the subdir layout. **Operator note:** if you hand-customised a *bundled* plugin's lang file (rare), the JSON now wins over your legacy flat file - re-apply your edit to the new `scripts/lang/<lng>/<name>.json`. 3.2.x only.

- **Weblate translation enabled** ([translate.dcvault.net](https://translate.dcvault.net/)) - the community can now translate luadch through a web UI; new languages need no hub code (the loader builds the path from `cfg.language`). Two changes make partial, translator-managed languages safe: (1) `cfg_lang.checklanguage` **drops empty-string values**, so a Weblate-untranslated key becomes `nil` and the universal `lang.key or "<English>"` fallback fires - without this an empty value would blank the line, since `""` is truthy in Lua. (2) The i18n CI (`lang_test`, `plugin_lang_test`) is re-based for the Weblate era: **English is the strict source of truth** (complete + consistent with the code), a translation may be **incomplete** (an untranslated key falls back to English, no failure), but must carry **no orphan key** (a key the source dropped) and must keep each translated string's **`%s`/`%d` placeholder signature** - same conversion types in the same order - matching its English source (Lua `string.format` fills positionally, so a reordered or dropped placeholder would crash it). New `docs/TRANSLATING.md` (translator how-to + the Weblate -> `weblate` branch -> `dev` PR flow); `README.md` + `CONTRIBUTING.md` link it. No behaviour change for a fully-translated hub. 3.2.x only.

### Documentation

- **Docs readability pass (formatting only, no content change).** Two of the longest operator docs were hard to scan. `docs/SCRIPTS.md` (86 stacked plugin sections) gains a grouped **Index** table at the top - one jump-link + one-line purpose per plugin, per category - so a reader gets a one-glance overview and can navigate to any entry; purely additive, every detail section is untouched (all 86 anchors verified resolving). `docs/HTTP_API.md` drops genuine repetition: the authorisation rule ("the ADC-side level / permission guards do not apply on the HTTP path; the bearer token's scope is the sole gate") that was restated verbatim in ~46 footnotes is now stated once as a §10 note and removed from each footnote (unique per-endpoint caveats kept); the ~39 obsolete `**migrated (Phase X PR-Y)**` status markers (the whole API has shipped) are stripped, and the "User control" table's migration-only `Status` column removed - endpoint contracts, request/response shapes, error codes and footnote refs all preserved. No behaviour change. 3.2.x only.

- **Docs currency sweep - refresh stale operator/contributor docs after the #480 / #500 / #501 batch.** A thorough audit (four parallel passes over `CLAUDE.md`, the reference docs, the operator/top-level docs, and a mechanical link + dead-reference check) found no broken links and all `CLAUDE.md` hard facts correct, but a cluster of drift: `docs/SCRIPTS.md` was missing catalog entries for the three newest plugins (`etc_backup` #480, `etc_lockdown` #501, `etc_forcetlstransfer` #500) and carried stale cfg-key names for `etc_records` / `cmd_usercleaner` / `etc_msgmanager` / `etc_trafficmanager`; `docs/HTTP_API.md` still framed the (shipped) [#82](https://github.com/luadch-ng/luadch-ng/issues/82) API as a "design draft"; `docs/DOCKER.md` omitted the `:dev` image tag + `dev`-push publish; `docs/INSTALLING.md` listed firewall port 5000 without the TLS-only caveat; `CLAUDE.md` §5 had not folded in #480 / #500 / #501; and this changelog's `[Unreleased]` intro + note block were frozen at the pre-#480 / v3.1.9 era. Smaller fixes to `README.md`, `tests/README.md`, `docs/SECURITY.md`, `docs/CONFIGURATION.md` and `docs/PLUGIN_API.md` (the last drops a hard-coded plugin count per the anti-bit-rot rule). Docs-only, no code change. 3.2.x only.

- [#457](https://github.com/luadch-ng/luadch-ng/issues/457) - **`docs/PLUGIN_API.md` §8 now enumerates every public plugin export instead of sampling 7 of the 15 that have a public surface.** The old table closed with "See each plugin's source for the full exported surface", so §8 read as a contract but was a sample - during [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 6 "not in §8" was mistaken for "dead export" and cost a full re-derivation. The table is now exhaustive (15 rows) and states the completeness contract plus the repo convention that `_`-prefixed exports are internal unit-test / migration seams, deliberately omitted - four plugins (`cmd_gag`, `etc_blocklist`, `etc_whitelist`, `hub_runtime`) export only such seams and appear nowhere, so absence now provably means "exports nothing public". Also fixes a live doc bug: `cmd_ban` exports `bans` as a **table by reference** (mutated in place, per the #238/#239 idiom), not the callable `ban.bans()` the old table showed; the new row documents `ban.bans` + the `ban.bans_path` load-on-demand alternative. Docs-only, no code change. 3.2.x only.

- **new [`CONTRIBUTING.md`](CONTRIBUTING.md) - a contributor entry point, closing a real doc gap.** Three external PRs ([#437](https://github.com/luadch-ng/luadch-ng/pull/437) / [#438](https://github.com/luadch-ng/luadch-ng/pull/438) / [#439](https://github.com/luadch-ng/luadch-ng/pull/439)) all arrived against `master`, and correctly so from the outside: nothing documented the GitFlow-A branch policy, `master` is the repository's default branch (so GitHub offers it), and `README.md` literally described 3.2.x as "active development (`master`)" without mentioning `dev` at all. The new page leads with the branch table (`dev` = target this, `master` = release substrate, `release/3.1.x` = security backports only), records that a mistargeted PR just gets retargeted by a maintainer with the contributor's authorship intact (no close-and-reopen), routes vulnerability reports to [`docs/SECURITY.md`](docs/SECURITY.md), and links out for the rest - build, tests, the core restricted-env `use "X"` contract, the plugin sandbox, the companion scripts repo - rather than duplicating them; the engineering how-to stays in [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md). `README.md` gains a `dev`-branch pointer directly under the release-lines table (the sentence that was actively pointing contributors at the wrong branch) plus `CONTRIBUTING.md` and the previously-unlisted `docs/DEVELOPMENT.md` in its documentation list; `docs/DEVELOPMENT.md`'s orientation table gains a "your first PR" row. Docs-only, no code change. 3.2.x only.

- Contributor-doc currency + pattern capture (post-#398 webhook arc). **`CLAUDE.md`:** latest-release marker v3.1.12 -> **v3.1.13**; the §3 module map gains `core/hmac.lua` (HMAC-SHA256, #398) and `core/geoip_update.lua` (was omitted); the §5 current-era note re-framed from "bug triage" to feature-work + triage; §8 gains a "drop-in plugin patch as a lighter alternative to a 3.1.x release" note (the `usr_uptime` v0.10.1 case). **`docs/DEVELOPMENT.md`:** six durable patterns added - `scope="none"` custom-auth HTTP routes, verifying a signed body with `hmac.sha256` + dynamically-named per-instance secret keys, bots-at-module-load / routes-in-`onStart` (reload safety), bulky operator config in its own `cfg/<name>.tbl`, the `getpeername()`-nil-after-accept guard (#401), and poll-don't-fixed-settle for a smoke assertion whose hub state installs asynchronously and fails open (#408). **`docs/HTTP_API.md`:** the §4.4 `none` scope row and the §5.1 plugin-registration `scope` arg now document `scope="none"` for plugin-owned auth (`etc_webhook` is the first user); the `meta` key list gains `plugin`. Plus: two unit tests that existed but were never wired into CI (`etc_blocklist_test`, `cmd_usercleaner_orphan_test` - both pass) are now registered on both smoke legs, so `tests/README`'s "the whole set runs in CI" holds. Docs / test-wiring only. 3.2.x only.

- [#364](https://github.com/luadch-ng/luadch-ng/issues/364) (Sopor) - **clarified the `kill_wrong_ips` wording so it no longer reads as if it relates to CGNAT traffic/download blocking.** `kill_wrong_ips` only governs the login-time *mismatch* kick (client-advertised INF IP vs authenticated TCP source); it does not affect how the hub blocks users - e.g. the Traffic Manager, which on 3.x decides on level / share / account-nick, not IP (the actual fix for the #364 CGNAT block-bypass reported on 2.24). The old wording listed bare "CGNAT users" as a `kill_wrong_ips = false` beneficiary and claimed peers "reach them correctly in CGNAT-egress scenarios", which both overstated the effect (genuine carrier-NAT users get no inbound active-mode P2P regardless) and invited conflation with #364. Tightened in `docs/SECURITY.md`, the `kill_wrong_ips` `cfg_defaults` comment, `examples/cfg/cfg.tbl`, and the Breaking entry above: the beneficiary is now described by the actual mismatch mechanism (stale VPN IP, hand-set wrong WAN IP, dual-stack family mismatch), with an explicit "does not change how the hub blocks users" note. Docs-only, no code change. 3.2.x only.

- [#95](https://github.com/luadch-ng/luadch-ng/issues/95) - **`docs/SECURITY.md` now documents the command-reply password disclosure and its resolution**, closing the issue's last acceptance item. Records that `+accinfo` / `+usersearch` / `+setpass` were redacted in PR [#119](https://github.com/luadch-ng/luadch-ng/pull/119) (caller no longer sees the value; a `+setpass` target still gets it via PM to log in), and that `+reg`'s auto-generated-password delivery is **retained by design**: the value must reach the new user (PM when online) and the admin (required for the offline-target case, where the admin is the only party who can convey it), it is a directed disclosure to the two parties who need it (the opchat reg-report carries no password), and a leak-free channel (SMTP [#100](https://github.com/luadch-ng/luadch-ng/issues/100) / first-login token) is Phase-8+ infra luadch does not yet ship. Cross-links F-AUTH-1 and the #96 `cmd.log` redaction. Docs-only, no code change. 3.2.x only.

- Contributor-doc currency refresh (post-#78-arc). `CLAUDE.md`: the #78 unified-blocklist arc is marked COMPLETE + on master (#78/#79/#352 closed) instead of "in flight"; the module map adds `core/ensuredirs.lua`; a note records the current bug-issue-triage era + the dev->master MERGE-commit (not squash) rule. `docs/DEVELOPMENT.md`: the Windows CI now runs `lua5.4` (pinned to msys2 `lua54` = Lua 5.4.8, the hub's runtime) with a warning not to let it drift to a newer Lua (5.5 makes generic-for control vars const); the privilege checklist gains the "user-action commands must reject a bot target (`msg_isbot`); login/connect enforcers are bot-unreachable" rule from #355. Docs-only. 3.2.x only.

- [#275](https://github.com/luadch-ng/luadch-ng/issues/275) - holistic HTTP API review follow-up, Consistency PR. Wraps up the doc + minor-code drift the review surfaced: **CON-1 (BLOCKER)** `GET /v1/log/api` response shape now matches the sibling tail endpoints (`/v1/log/error`, `/v1/log/cmd`, `/v1/chatlog`) - `{lines, returned, total_lines}` instead of `{lines, path}`. Pre-PR generic admin clients needed special-cased rendering and `path` leaked `cfg.log_path` to any admin token. Default `lines` aligned to 200 to match the family. Handler refactored to stream the file line-by-line with a bounded rolling window so memory stays O(lines_q) regardless of file size (the audit log has no rotation hook). **CON-1 / B2** `docs/HTTP_API.md` §4.6 + §6.3 X-Confirm canonical list now includes `DELETE /v1/usercleaner/expired` and `DELETE /v1/usercleaner/ghosts` (router was already enforcing them; spec was stale). **CON-2** §10.2 + §13 "future" subsections pruned to remove the four items shipped post-Phase-4 (#261 / #262 / #263 / #264) and SSE/UDS notes restructured. **CON-3** §6.4 documents that `parse_query` does NOT URL-decode - `?nick=ali%20ce` matches the literal `ali%20ce`, not `ali ce`. Operator-visible contract that was previously implicit. **CON-4** `bootstrap_first_token` writes "section 4.7" instead of "s4.7" in `cfg/api_token.first`. **CON-N2** `[^http-blacklist-1]` footnote updated to reflect sorted-by-nick default (post-#264 PR-B; was "pairs() order"). **CON-N3 (C5)** `validate_schema` `min` / `max` now fail loudly when set on a non-numeric value (was silently bypassing the bound, schema-author footgun; no existing schema trips it - all `min` / `max` declarations in the codebase live on numeric fields). **CON-N4 (C4)** `_status` table gets a `202 Accepted` row so a future endpoint that emits 202 directly doesn't fall back to the default "202 Status" reason. **C7** spec §6.2 documents that deferred-response endpoints are NOT idempotency-cached. Smoke test extended to assert the new `/v1/log/api` shape. Two-pass review caught the unbounded full-file read + default 100→200 drift in the catalog / endpoint description; bounded streaming + default sync done before push. 3.2.x only.

- New [`docs/PLUGIN_SANDBOX_MIGRATION.md`](docs/PLUGIN_SANDBOX_MIGRATION.md) for third-party plugin authors whose plugins worked pre-2026-05-23 (pre-#206). Lists every primitive that became unreachable across Tier-1 + Tier-2 sub-PRs (#210, #211, #212, #213) with copy-paste old-API → new-API mappings. Driven by a post-#206 audit of the companion `luadch-ng/scripts` repo that found 2 of 30 third-party plugins broken (`ptx_freshstuff` uses `loadfile/dofile`, `ptx_RSSFeedWatch` uses `require`); the doc covers their patterns + every other case in the bundled-plugin audit. [`docs/PLUGIN_API.md`](docs/PLUGIN_API.md) §2 also updated to reflect the post-#206 whitelist as the live spec; it no longer claims "full os/io libraries are available". 3.2.x only, not backported.

- [#167](https://github.com/luadch-ng/luadch-ng/issues/167) - documented the `etc_trafficmanager.lua` admin escape valve as intentional. Operators at level >= `masterlevel` (60 by default) bypass the block filter on **three** event types (`onConnectToMe`, `onRevConnectToMe`, `onSearchResult`) even when on `block_tbl`; the `[BLOCKED]` description flag still applies. Two reasons: (1) admin self-soft-lock protection (typo-resistance on `+trafficmanager block <yournick>`), (2) the threat-model assumes ops are trusted. The fourth listener (`onSearch`) has no exempt gate by design - blocked ops cannot send searches, but the block does not lock them out of PM / main-chat. Hard-block-everyone is not implemented because no operator has reported needing it; design options (global toggle, per-block flag, separate filter-min-level cfg) are sketched in the issue body for future revisits. Code-comment-only change next to the `masterlevel` definition. 3.2.x only, not backported.

### Refactors

- [#537](https://github.com/luadch-ng/luadch-ng/issues/537) - **deduplicate `find_online_by_firstnick`: 12 plugin-local copies -> one `hub.find_online_by_firstnick` core API.** The firstnick-fallback resolver (find an online human by `user:firstnick()`, the prefix-independent login nick - the [#473](https://github.com/luadch-ng/luadch-ng/issues/473)/[#474](https://github.com/luadch-ng/luadch-ng/issues/474) nick-prefix fix) was copy-pasted, in five cosmetically-different but semantically identical variants, into `bot_session_chat`, `cmd_ban`, `cmd_disconnect`, `cmd_gag`, `cmd_myinf`, `cmd_myip`, `cmd_redirect`, `cmd_sslinfo`, `cmd_userinfo`, `etc_msgmanager`, `etc_trafficmanager` and `usr_hide_share` - the exact site the nick-prefix fix had to touch in lockstep, a latent drift defect (§1a.1). The loop now lives once in `core/hub.lua` as `hub.find_online_by_firstnick( nick )` (iterating the same humans-only, normal-state set `getusers()` returns first; auto-exposed to the sandbox via the existing `pairs( hub )` copy), and each plugin's copy becomes the idiomatic `local find_online_by_firstnick = hub.find_online_by_firstnick` alias (matching the existing `local hub_getusers = hub.getusers` pattern), so call sites, the `_find_online_by_firstnick` test seams and the two nick-prefix regression tests stay intact. Two now-dead `local hub_getusers` aliases (`cmd_myinf`, `cmd_myip`) removed. Behaviour-neutral: the core function is a byte-for-byte move of the tested loop; the prefix regression tests (`nick_prefix_resolution_test` 59/59, `bot_session_chat_prefix_test` 12/12) plus `etc_trafficmanager_test` gain a faithful `find_online_by_firstnick` hub stub and stay green, and `luac -p -l -l` on the new `core/hub.lua` binding shows zero `SETTABUP _ENV` (proper forward-declared local, not a bare global - the #353 class). `hub.lua` main chunk: 168 locals, well under the 200 cap. Documented in [`docs/PLUGIN_API.md`](docs/PLUGIN_API.md) §5.1. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 6 - **plugins: remove 18 dead `lang` locals and the 22 language keys they orphaned** (62 lines across 35 files), and close the hole in the guard that let them accumulate. Each removed local is a `local msg_x = lang.msg_x or "<english fallback>"` binding that its own plugin declares and never reads: `cmd_accinfo` (`ucmd_menu_ct1`, `ucmd_menu_ct3`), `cmd_ban` (`msg_import`, `msg_ban_added`), `cmd_hubstats` (`msg_empty_tbl`), `cmd_myinf` (`msg_unknown`), `cmd_reg` (`msg_import`), `cmd_upgrade` (`ucmd_level`), `cmd_userinfo` (`msg_denied`), `etc_blocklist` and `etc_whitelist` (`msg_add_failed` / `msg_export_fail` / `msg_import_fail` each), `etc_clientblocker` (`msg_client_not_allowed`, `msg_err`), `etc_trafficmanager` (`msg_notregged`). Their keys go from **both** `.lang.en` and `.lang.de`, along with four keys that were already orphaned with no local at all: `cmd_reg`'s `ucmd_passwort` (a German key name in the English file, left behind when `+reg` stopped prompting for a password and started generating one) and `etc_userlogininfo`'s `msg_ccpm_1/2/3` (the CCPM row is gone from the `msg_info` template). **None of the 18 is a swallowed error path**, which is the thing worth checking before deleting a message template rather than after: `cmd_userinfo`'s denial is live via `msg_god` at the hierarchy check (`cmd_userinfo_permission` maps a level to the highest level it may inspect - there is no flat "you may not use this" concept to print `msg_denied` for); `cmd_ban` announces through `report.send( ..., message )`; `etc_blocklist` / `etc_whitelist` **do** report add/export/import failures, just with the raw error string the helper returns rather than through these templates - so the OK paths are translatable and the failure paths are English-only, a real i18n gap that removing an unwired template neither creates nor fixes ([#456](https://github.com/luadch-ng/luadch-ng/issues/456)); `etc_clientblocker`'s silence on a missing `.tbl` is deliberate and commented as such - the source marks the missing-file case as the canonical first-run path rather than an error condition, with no opchat feed, from the #81 seed-on-missing arc, which is what left `msg_err` stranded; and `etc_trafficmanager` uses its regged lookup only to derive `target_level`, so being unregistered was never an error to announce. **Derived twice, independently** (§1a.4), because the tracker's list is neither: it says 17 locals and 40 lines. Both are wrong - a subagent's per-file lexer and a second detector reusing the shipped guard's validated comment-stripper each independently returned the **same 18**, from the same 1021 declarations, and 18 decls + 18 `.en` + 18 `.de` cannot be 40 either way. The tracker most likely lost `cmd_accinfo`'s two `ucmd_menu_ct*` to an `[a-z_]+` identifier pattern that silently skips digits - the same class of miss that made PR 5's `sha256` invisible. **The four dead plugin exports the tracker also lists here are deliberately NOT removed** (`etc_report.broadcast`, `etc_usercommands.format`, `bot_opchat.bot`, `cmd_ban.bans_path`), because the criterion behind them - "not in `docs/PLUGIN_API.md` §8's contract table" - does not survive reading §8, which closes with *"See each plugin's source for the full exported surface"*: the table is explicitly a sample, not an enumeration. It documents 7 of the 18 bundled plugins that export anything, and the names missing from it include `etc_usercommands.add` (**210 call sites in the companion `luadch-ng/scripts` repo alone**), `cmd_ban.del`, `cmd_help.reg` and `etc_hubcommands.has` / `list` - so "absent from §8" would condemn the single most-used plugin export in the ecosystem. This is PR 3a's `getstats` lesson in reverse: non-enumeration cannot mean non-contract. The repo's actual convention is the `_` prefix (`etc_blocklist._parse_add_args` and friends are test seams); all four targets are bare names, three are changelogged deliberate features (`bot_opchat` v0.15 *"expose bot object via interface"*, `etc_report` v0.04 *"renamed old send() function to broadcast()"* - a level-**range** broadcast that `send` cannot do), and `etc_usercommands.format` is documented in its own header line 7 (`usage: user.write( ucmd.format( ... ) )`). Confirmed unused across both this repo and the companion repo (whose 27 import sites call only `.send`, `.feed`, `.add` and `.bans`), but unused-in-tree is the expected state of a public plugin API, not evidence against it. **`tests/unit/plugin_lang_test.lua` now asserts both directions.** The #442 guard only checked that every `lang.X` the source reads exists in the lang files; a key nothing reads was invisible to it, which is how 22 of them accumulated. It also under-scanned: its `lang%.([%w_]+)` pattern cannot see `lang. msg_notfound` (whitespace after the dot, which Lua accepts and `usr_uptime.lua:143` is written with), so that key was unguarded, and a reverse check without `%s*` would have reported it as orphaned and deleted a **live** German translation - the exact #442 bug, introduced by the cleanup meant to prevent it. Both directions now tolerate the whitespace. Provably fails pre-fix (§1a.7): on the unpatched tree the new assertion reports 8 failures (`cmd_reg ucmd_passwort` and `etc_userlogininfo msg_ccpm_1/2/3`, each in `.en` and `.de`); patched, 4280/4280 checks pass over 68 plugins. **No `scriptversion` bumps**: per `docs/DEVELOPMENT.md` a bump signals a semantic change (behaviour, cfg keys, wire surface) and is how the companion repo syncs - this has none of the three, and none of the 12 plugins exists there. Behaviour-neutral by construction: every removed binding was unreachable from its own file. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 5 - **remove 61 dead file-scope locals across 24 core modules** (88 lines, counting their orphaned assignments, two commented-out remnants and two section headers left labelling nothing). Mostly stdlib and module aliases that outlived their consumers (`table_insert`, `string_char`, `os_date`, `debug_traceback`, ...). Six were bind-late `local X` / `X = ...` pairs, assigned and never read: `out_put` in `util` and in `cfg_users`, `out_error` in `cfg_secret`, `types_adcstr` and `_cfgbackup` in `cfg`, `_integer` in `adc`. (`cfg_secret`'s `out_put` is **live** - five calls - and stays; only its `out_error` was dead.) `_integer` lost its consumer when `adc`'s `_regex.integer` was rewritten inline for upstream luadch/luadch#241. `whitelist`'s `_rebuild_indices` was copy-pasted from `blocklist`, whose only caller is a `bulk_replace` that whitelist deliberately does not have. `cfg`'s **`_cfgbackup`** is not an unfinished feature - it is a **finished one, deliberately disabled**: `cfg.lua` once wrote timestamped `cfg.tbl.backup.[dd.mm.yy.HH.MM.SS]` copies on every `cfg.set`; that call was commented out in 2016 and [#272](https://github.com/luadch-ng/luadch-ng/pull/272) deleted the remnant, orphaning `_cfgbackup` **and** `cfg`'s `os_date`, which is why both die here together. No data is at risk - `cfg.set` still persists through `util_savetable( _settings, "settings", _cfgfile )`; only the backup copies stopped, ten years ago. Derived by a purpose-built detector rather than the tracker's line list: comments stripped first (a bare grep counts the English word - the `stop` trap), every file-scope local counted, iterated to a fixpoint so cascade orphans surface. It found four the tracker's list did not - `adc`'s `debug`, `cfg`'s `types`, `hbri`'s `cfg` (each orphaned only by removing its last consumer, invisible to a single pass) and `hub.lua`'s `tonumber`, missed by PR 1 because that PR worked from the same short list. It also needed two corrections of its own, both caught in review: `X = X,` in a return table is an **export**, not an assignment (it was flagging live functions until that rule went in), and `local function X` parsed as a local named `function` - which hid `whitelist`'s `_rebuild_indices` until a reviewer named it. **Load order is unchanged**, the one thing that could have broken: `use "X"` on an unloaded core module triggers its `loadscript`, so dropping a `use` can move a module's load. Checked against the `_core` array - `mem` sits at position 2, ahead of `util` (4), `out` (7) and `adc` (18); `debug` is a stdlib global with no `core/debug.lua` shadow; `hbri` is not in `_core` and is only pulled at runtime from `hub.lua`'s `login`. The one real case is `cfg`'s `use "types"` (cfg 5, types 28, so cfg genuinely triggers that load today): it resolves because **`cfg.lua` loads `cfg_defaults` itself**, which does `use "types"` - about 50 lines further into the same load. Nothing between the two touches types, and types.lua is passive at load and exports no `init`. Six removals were all-or-nothing: deleting only the `local` turns its assignment into a bare global write, which `core/init.lua`'s restricted env rejects **at hub load** - the #353 class, invisible to `luac -p` and to the unit tests. Proven absent by a bytecode scan of every patched file: zero `GETTABUP` / `SETTABUP _ENV "<name>"` for any removed name, i.e. no dangling read, no captured closure, no stray global write. Hub boots, full smoke suite green. Two exports are now **orphaned in-tree but deliberately kept** - `util.handlebom` and `cfg_secret.is_active`, whose only consumers were aliases removed here; both live on plugin-reachable tables, so #447's conservative-on-contract-surface rule keeps them. `core/http_events.lua`'s `_max_size` is left alone on purpose: it is read only by its own clamp, so the `http_events_buffer_size` cfg key may do nothing - a behaviour question, not dead code. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 4 - **`core/util.lua` + `core/adc.lua`: remove unreachable branches** (33 lines from util.lua, 2 from adc.lua). All no-ops today, but each is a landmine for whoever touches the function next. Proven behaviour-neutral by loading the old and new `util.lua` side by side under Lua 5.4.8 and comparing both versions' output across every input class - **all 35 cases identical**; `git diff -w` collapses the util.lua change to pure deletions plus one edit. (1) **`util.encode` / `util.decode` / `util.trimstring`'s dead type guards.** `local str = tostring( str )` sits above each, and `tostring` never returns nil (`tostring(nil)` is the truthy string `"nil"`), so the guard is always satisfied and the error path is dead. Verified at runtime: `util.encode( nil )` returns the encoding of `"nil"` on both old and new code, never `(nil, err)`. Worse, encode/decode's dead branch read **`tbl`** - undeclared in that scope, a fossil of a table-taking ancestor's signature - so had it ever fired under `core/init.lua`'s restricted env it would have raised "attempt to read undeclared var" instead of returning `(nil, err)`. Disarmed only by being unreachable. The always-true `if` goes with each, and the orphaned `err` locals with those. **Behaviour is preserved exactly**: all three still stringify whatever they are given. That is a real hole - `util.trimstring( nil )` returns the string `"nil"`, and the companion repo's `etc_requests` feeds it unvalidated chat input - but closing it is a breaking change on sandbox-exposed functions and needs its own PR with tests, a PLUGIN_API update and a coordinated `etc_requests` fix, since hardening `trimstring` would turn that plugin's silent misbehaviour into a crash. Tracked in [#453](https://github.com/luadch-ng/luadch-ng/issues/453). (2) **Four unreachable `type( x ) ~= "number"` guards** in `formatseconds`, `formatbytes` and `difftime` (x2). Each sits after `local x = tonumber( x )` **and** an `if not x then return nil, err end` - and since `tonumber` returns a number or nil and nothing else, those two guards fire on identical inputs, verified across every input class. The type check is not a missing validation, it is the redundant second copy of one; `if not x` is the live guard and stays. This **supersedes the merge rationale on [#439](https://github.com/luadch-ng/luadch-ng/pull/439)**, which kept the guards as a latent trap for "the moment that `tonumber` rebind is ever refactored away": that reasoning does not hold, because in that scenario `if not x` is the guard that survives and the type check is not the safety net it appears to be. Note #439 (@Kcchouette) corrected the expression in `formatseconds` and `formatbytes` **only** - and correctly (`not type( x ) == "number"` really does parse as `( not type( x ) ) == "number"`); `difftime`'s two guards date to the SourceForge import and were always written correctly. `formatbytes`'s commented-out original combined guard goes too. **Untouched:** `convertepochdate` and `generatepass` use the same shadow but have **no** preceding nil-guard, so their type checks are reachable and load-bearing - `generatepass`'s is precisely what stops `nil < 0` from raising. (3) **`core/adc.lua`: duplicate `FC` / `TO` keys** in the **`STA`** named-parameter table (not `SCH`, which this arc's tracker misnamed and which has no `FC` at all), declared once for base STA and again under the ASCH block. Unlike the [#438](https://github.com/luadch-ng/luadch-ng/pull/438) `bool` duplicate, both writes store the identical value, so last-wins changes nothing today - the trap is future-tense: changing the base `FC` would be silently overridden back by the ASCH copy, and the edit would look like it did nothing. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 3b - **`core/server.lua`: remove the caller-less outbound-TCP wrapper and the broken STARTTLS seam** (139 lines; the file is now 1148, down from 1331 before PR 3a). Two clusters, both dead since long before this fork, plus the two accessors PR 3a deferred. (1) **`addclient` + `wrapclient` + `socket_tcp`.** `addclient` was exported but called by nothing in-tree - and never plugin-reachable either, since `server` is absent from `core/scripts.lua`'s `SANDBOX_GLOBALS` (`http_client` **is** in it). `wrapclient` and `socket_tcp` were reachable only through `addclient`. It was also **broken**: on the `timeout` retry path it discarded `wrapclient`'s return, so `handler` stayed nil while `err` was still `"timeout"` from the `connect` probe - it returned `nil, "timeout", id` for a connect that was in fact proceeding, and `wrapclient` had already armed the deferred wrap that would dispatch it. A caller would have abandoned a live connection as failed. `wrapclient` additionally called `listeners.failure(...)` unguarded. Outbound TCP is not lost: `core/http_client.lua` does its own non-blocking connect over `socket.tcp` + `server.addtimer`, and that is not just the established pattern but the one every outbound consumer already chose - `etc_regserver_announce` (the [#279](https://github.com/luadch-ng/luadch-ng/issues/279) hublist announcer), `etc_blocklist_feeds`, `etc_proxydetect` and `etc_status_push` all go through `http_client`; none ever used `addclient`. Note `core/server.lua`'s own comment (from [#188](https://github.com/luadch-ng/luadch-ng/pull/188)) recorded that `addclient` was knowingly kept and bug-fixed while caller-less, on "a divergent broken copy is a defect" grounds - that is a keep-it-correct decision, not a keep-it decision. (2) **The deferred-TLS `handler.starttls` branch.** Never called from anywhere - and not a seam worth preserving, because it never worked. Where the functioning `startssl` path installs the handshake coroutine as the event-loop entry point (`handler.readbuffer = handshake`, `handler.sendbuffer = handshake`), `starttls` instead wrote `handler.receivedata` / `handler.dispatchdata` - two field names **the event loop never reads**, assigned from `handler.handshake`, which **does not exist** (the handshake coroutine is a local, not a handler field), so they got `nil` besides. It left `readbuffer` / `sendbuffer` pointing at the **plaintext** `handle_read_event` / `handle_write_event`. A caller would have got a TLS-wrapped socket driven by plaintext handlers, stuck mid-handshake because the handshake could never resume across a readiness event. ADC has no STARTTLS anyway (`adcs://` is TLS from connect); this is Prosody `net.server_select` / XMPP heritage that no ADC path could ever call. Its dead companions go too: `needtls` (written twice, never read) and `wrapconnection`'s `local shutdown` (assigned twice, never called - not to be confused with `wrapserver`'s `handler.shutdown`, which is **live** via `hub.lua`'s `_servers` sweep). Also swept: `changetimeout`, declared and never assigned - the same defect class PR 3a removed `stop` for, missed by that pass. The `else` branch's `handler.readbuffer` / `handler.sendbuffer` assignments are **kept**: `sslctx`-without-`startssl` is unreachable from `hub.lua` today, but `addserver` takes the two independently, and removing the whole branch would leave such a connection with no read/write handlers at all - worse than the plaintext it gets now. (3) **`handler.socket` and `handler.id` on the `wrapserver` handler**, per PR 3a's correction: that object reaches no plugin (`add_server_handler` is a `hub.lua` local and `addserver`'s return lives in a private `_servers` table), and `hub.lua` calls only `shutdown` on it. The identically-named `wrapconnection` accessors are contract surface via `user:client()` and stay. Behaviour-neutral: builds, boots, full smoke suite green - including the TLS login path immediately adjacent to the removed branch. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 3a - **`core/server.lua`: remove 44 lines of unambiguously dead, core-internal code.** Two exported functions with zero callers anywhere: `getsettings` / `changesettings` (the runtime knobs they read and wrote - `_selecttimeout`, `_sleeptime`, the buffer caps, the idle timeouts - keep their module-level initialisers and every real read, so they are now the constants they already were in practice, since nothing ever called the setter), and `stats`, whose module-wide `_readtraffic` / `_sendtraffic` counters were read **only** there: dropping them also removes two additions from the read and send hot paths. The per-connection `readtraffic` / `sendtraffic` and their accessor `handler.getstats` are **untouched and live** (`cmd_userinfo.lua:212`) - the two pairs are easy to confuse. Plus five dead module locals (`table_remove` - `tick()` calls `table.remove` directly; `socket_bind`; `signal` + `signal_set` + `signal_get`; `stop`, declared and never assigned) and the unused `disconnect` binding in `wrapclient` / `wrapserver` (`wrapconnection`'s own binding is live - do not confuse those either). Removing `local signal = use "signal"` does not shift the module load order: `signal` is loaded well before `server` in `_core`, so the call only ever read an already-loaded `_global` entry and never reached `loadscript`. **Deliberately NOT removed, contra the audit's own note:** the unused `wrapconnection` handler accessors `id`, `dispatch`, `disconnect`, `bufferlen` and `pattern`. The audit argued they are unreachable because `server` is absent from `SANDBOX_GLOBALS`. That forecloses `server.X` access, but it is a non-sequitur for an object handed to a plugin by reference: `user:client()` returns `wrapconnection`'s handler verbatim, and `docs/PLUGIN_API.md` documents it both under §"Network" ("Underlying socket handler") and in its type glossary, where `handler` is the only type NOT linked to an enumerated method section - it is defined as "a wrapped socket from `core/server.lua`", i.e. the doc delegates the surface to the source. The decisive evidence is `getstats`: it appears **nowhere** in `docs/`, yet `cmd_userinfo.lua` depends on it through exactly that route. So non-enumeration cannot mean non-contract, or `getstats` would be equally removable and `+userinfo` would break. `bufferlen` and `pattern` are setters, so a third-party plugin may well be tuning buffers through them. #447's conservative-on-contract-surface rule keeps all five. Note the audit's list also spans two DIFFERENT objects: `handler.socket`, and a second `handler.id`, live on the `wrapserver` handler, which no plugin route reaches - those are genuinely dead and left to PR 3b on their own merits, not on this contract argument. Behaviour-neutral: builds, boots, full smoke suite green. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 2 - **remove `core/doc.lua`** (308 lines), the auto-doc generator, and its two dead references in `core/init.lua`. The module was dead three times over: commented out of the `_core` load list, its `doc.export()` call site commented out, and **its entire 19-entry `doc.add(...)` payload sits inside a `--[[ ]]` block** - so even after uncommenting the first two, `export()` would have written a file containing nothing but the heading. Zero `use "doc"` anywhere, absent from `core/scripts.lua`'s `SANDBOX_GLOBALS` and from `docs/PLUGIN_API.md`, and its output target `docs/documentation.txt` has never existed. This reverses the 2026-05-23 housekeeping decision to keep it "for potential future re-enable" (`docs/phases/PHASE_6.md`, and the housekeeping-sweep entry further down this same section), because that premise does not survive contact with the payload: it documents `util.save` / `util.load` (the exports are `savetable` / `loadtable`; the only occurrences of `util.save`/`util.load` in the repo were doc.lua's own examples, which would crash), `server.wrapsslclient` / `server.wraptcpclient` (gone), and `server.add` / `server.closeall` / `server.loop` (today `addserver` / `killall`, and the loop is `hub.loop`). A re-enable would mean rewriting all 19 entries from scratch, and `docs/PLUGIN_API.md` already covers the ground and is maintained. Riding along: `init.lua`'s `--"test",` and `--test( )`, dangling since `core/test.lua` was deleted in #49. `install(DIRECTORY core/)` is wholesale, so no build change - but it never deletes, so an in-place upgrade leaves a stale `core/doc.lua` behind. That leftover is inert because nothing calls `use "doc"`, NOT because it is out of `_core`: `use` resolves by filename via `loadscript`, so it would happily load a leftover copy off disk if anything asked for it. Docker is exempt either way (`core/` is baked from source into a fresh runtime image, never overlaid onto a previous tree). Verified by booting the hub with the file genuinely absent from the install tree: full smoke suite green - worth doing, because CMake's install had left the old copy in place and a run against an un-cleaned tree would have proved nothing. `examples/etc/other_available_scripts/cmd_showdoc.lua` still calls `doc.collect()`; it has been non-functional since the sandbox landed (`doc` is not a sandbox global, so under the default `no_global_scripting = true` the strict env raises "attempt to read undeclared var: 'doc'") and is left alone as out-of-scope graveyard code - `examples/etc/` is not installed at all. 3.2.x only, not backported.

- [#447](https://github.com/luadch-ng/luadch-ng/issues/447) PR 1 - **`core/hub.lua`: remove 19 dead file-scope locals** (dead-code audit). Declared and never referenced anywhere in the repo: `tablesize`, `loadfile`, `io`, `table_concat`, four `adclib_*` aliases and `util_difftime` (their consumers moved to `hub_dispatch.lua` in Phase 6d), `types_check`, the four `_verify` / `_normal` / `_protocol` / `_identify` state-table slots (likewise living in `hub_dispatch.lua` since Phase 6d), `_G`, `_cfg_nick_change`, `killuser`, `usercount` and `isuseronline`. Riding along: `isuseronline()`'s body (defined, never called, a strict superset of the live `isnickonline` / `iscidonline` / `issidonline` trio), `killuser`'s 39 commented-out lines, and the `hub.usercount` export - never assigned in hub.lua's history (`git log -S"usercount = function" -- core/hub.lua` is empty), so the constructor's `usercount = nil` never created the key and any plugin calling it already got "attempt to call a nil value". 83 lines, behaviour-neutral: the hub builds, boots and passes the full smoke suite. Two removals were all-or-nothing - `local _G` and its `_G = _G` self-assignment (a no-op, since `_G` resolves to the nil local on both sides), and `local isuseronline` with its assignment - because deleting only the declaration turns the assignment into a bare global write, which `core/init.lua`'s restricted env rejects at hub load (the #353 class). The `nick_change` **feature** is live and untouched; only hub.lua's stale cache slot was dead. **Follow-up:** a 20th (`tonumber`) survived - PR 1 worked from the tracker's list, which was short; PR 5 above removes it. Frees 19 of the main chunk's 200 local slots, and measuring that headroom with `luac -p` disproved a claim `CLAUDE.md` and `docs/DEVELOPMENT.md` had both been carrying: hub.lua's file-scope locals were documented as frozen ("any new top-level `local` fails the build") when the file actually had 13 slots free, and now has 32. Both docs state the constraint without a number that can rot again and point at `luac -p -l` as the way to measure it, and the four in-code comments asserting the ceiling in the present tense are reworded as the historical rationale they always were (a fifth, at the `#301` i18n cache, already used the past tense and is left alone). 3.2.x only, not backported.

- **three core cleanups from an external contributor** (@Kcchouette - [#437](https://github.com/luadch-ng/luadch-ng/pull/437), [#438](https://github.com/luadch-ng/luadch-ng/pull/438), [#439](https://github.com/luadch-ng/luadch-ng/pull/439)). (1) `core/hub_user_object.lua` `user.setlevel` called `cfg_saveusers( _regusers )` **twice** - once bare, then again as the return expression - so every level change wrote `user.tbl` to disk two times; the bare call and a stale `--return true` comment are gone and the return contract is unchanged (the sibling `setrank` already had the clean shape). (2) `core/adc.lua` `_regex` declared the key `bool` twice: the first matched `_sid` (the 4-char base32 SID pattern, wrong for a boolean), the second `_bool`. A Lua table constructor applies both in order, so the last already won - the correct `_bool` variant was live all along and the first was dead code. Removing it is a no-op, but the duplicate was a trap for anyone reordering the table. (3) `core/util.lua` `formatseconds` / `formatbytes` guarded with `not type( t ) == "number"`, which parses as `( not type( t ) ) == "number"` -> `false == "number"` -> always false, so the guard never fired; now `type( t ) ~= "number"`. Also a no-op today: the `local t = tonumber( t )` rebind directly above already reduces every non-numeric argument to `nil`, which the preceding `not t` branch rejects, so the check is unreachable either way - fixed because the expression becomes a live bug the moment that rebind is refactored away (the same file already used the correct form at a third site). Net effect: (2) and (3) are behaviour-neutral hardening; (1) halves the disk writes on a level change. 3.2.x only.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) follow-up - move the bundled core language tables from `examples/lang/` to `lang/` at source-tree root. Matches the install-tree layout (CMake was already installing them to `lang/`), matches the precedent of `scripts/lang/` living under its consumer rather than under `examples/`, and removes a "why is this under examples/?" stumbling block for new contributors / Weblate onboarding. Pure `git mv` so blame history is preserved; CMakeLists.txt updated to point at the new source path (1 line); `tests/unit/lang_test.lua` + smoke.yml comment updated. Install output is byte-identical (verified). 3.2.x only, not backported.

- [#238](https://github.com/luadch-ng/luadch-ng/issues/238) - hot-path: `cmd_accinfo` no longer re-reads `scripts/data/etc_msgmanager.tbl` from disk on every `+accinfoop` ADC banner / `GET /v1/registered/{nick}` HTTP request. `etc_msgmanager` v0.7 exposes the live in-memory blocklist via a new `get_block_tbl()` getter export; `cmd_accinfo` v0.34's `get_msgmanager` (ADC) and `http_format_msgblock` (HTTP) helpers use the getter and fall back to `util.loadtable` only if `etc_msgmanager` isn't loaded. A function-getter (not a direct table-reference export) is required because `etc_msgmanager`'s `block_tbl = util.loadtable(...)` rebinds on every `onStart` (i.e. every `+reload`) - same #239-class hazard as cmd_ban's exported `bans` table. Behaviour is byte-identical; this is purely perf. The existing PR-2 (#236) smoke test for `GET /v1/registered/{nick}` covers the `msg_blocked` code path. 3.2.x only, not backported.
- [#166](https://github.com/luadch-ng/luadch-ng/issues/166) - cosmetic refactor: unified `return nil` exit pattern in `etc_trafficmanager.lua` `onConnectToMe` / `onRevConnectToMe` / `onSearchResult` listeners. Three listeners had two `return nil` paths (inside-gate "allow" + outside-gate "exempt") that were functionally identical. Replaced with a single explicit `return nil` after the gate block. Behaviour unchanged. Bytecode is two instructions shorter per listener (a deduplicated `LOADNIL` / `RETURN1` pair), verified with `luac -l`. `onSearch` is not part of this refactor because its control-flow shape is different (no masterlevel gate, returns `PROCESSED` after manual fan-out). Plugin bumped to v2.3. 3.2.x only, not backported.

- Phase-journal housekeeping sweep: removed dead `local doc = use "doc"` imports in `core/server.lua` and `core/util.lua` (the `core/doc.lua` auto-doc generator is disabled since Phase 6 but the dead imports lingered; `core/doc.lua` itself stays on disk for potential future re-enable). Surfaced by a phase-doc audit. 3.2.x only, not backported. **Superseded** by #447 PR 2 above, which removes `core/doc.lua` entirely: the re-enable premise did not hold. Both entries ship in the same release; this one is kept for the audit trail.

### Bugfixes

- [#567](https://github.com/luadch-ng/luadch-ng/issues/567) - **Docker container no longer reports `unhealthy` on a default install.** The `HEALTHCHECK` hardcoded `nc -z localhost 5000`, but the default cfg ships TLS-only (`ssl_ports = {5001}`, `tcp_ports` empty), so nothing bound 5000 and the probe failed on every tick - breaking `depends_on: condition: service_healthy` and risking restart churn under some orchestrators. The healthcheck now probes `LUADCH_HEALTHCHECK_PORT`, defaulting to **5001** (the actual default listener). The `HEALTHCHECK` CMD runs via `/bin/sh -c`, so the var expands at check time; operators who run plain-only (`tcp_ports = {5000}`, ssl disabled) or a custom port override it via `-e` / compose `environment:` without a rebuild. Documented in `docker-compose.yml`, `.env.example`, and a new `docs/DOCKER.md` troubleshooting entry. Verified against the real image: default hub boots healthy (listens `:5001` only), runtime override honored, and the old probe-5000/listen-5001 shape reproduces `unhealthy`. 3.2.x only.

- **Docker: drop `coreutils` from the runtime image (clears CVE-2016-2781, slims the image).** `coreutils` was pulled in only for `base32` (the SHA-256 keyprint -> `adcs://` URL). That moved into the hub in v3.1.6 (`cert_bootstrap.lua` computes the keyprint via adclib base32), so the container hasn't shelled out to `base32` since - the dependency was dead weight. Every command the entrypoint uses (`cp -rT`, `tail -F`, `cmp -s`, `ls -A`, ...) is provided by busybox 1.36, verified by building the image without coreutils and booting a fresh container: seeding, cert-gen and the keyprint banner all work. Removing it eliminates the GNU-coreutils **CVE-2016-2781** (`chroot` TIOCSTI, LOW / local-only / never reachable in the container anyway) that a Docker image scan flags. **Note:** the scan also flags **CVE-2025-60876** (busybox `wget` request-splitting, MEDIUM) - not reachable here (the hub never invokes `wget`; all network I/O goes through its own Lua `http_client`) and currently unfixed in every Alpine branch, so it stays until Alpine ships a patched busybox and the image rebuilds. 3.2.x only.

- [#551](https://github.com/luadch-ng/luadch-ng/issues/551) - **GPA password-challenge salt now meets the ADC 1.0.1 minimum (24 bytes, was 6).** The hub's `IGPA <salt>` login challenge used `adclib.createsalt()` at its default of 10 base32 chars, which `hashpas` decodes as `10 * 5 / 8 = 6` bytes - below the ADC 1.0.1 floor of "at least 24 random bytes". The GPA call site ([`core/hub_dispatch.lua`](core/hub_dispatch.lua)) now requests **39 base32 chars -> 24 bytes**, matching the spec floor and uhub; ADCH++ sends 32 bytes. No C change (`MAX_SALT_BYTES = 64` already accommodates it) and no change to the other `createsalt` callers (PID gen, HBRI token, HTTP). The salt was already CSPRNG-backed (OpenSSL `RAND_bytes`) and fresh per login, so the practical impact was low - luadch was simply below the spec length; this closes that gap. `tests/smoke/run.py` now asserts the decoded login salt is >= 24 bytes (RED on the old 6-byte salt). `docs/SECURITY.md` §3 updated. 3.2.x only (spec-compliance, no security impact -> not a `release/3.1.x` backport).

- **`adclib/adclib.cpp`: `aes_gcm_seal`/`aes_gcm_open` guard the plaintext/ciphertext length against `INT_MAX` (pre-3.2.0 defense-in-depth).** Both cast the buffer length to `int` for OpenSSL's `EVP_*Update` (and `seal` computes `pt_len + AES_TAG_SIZE` for the output buffer); a buffer past `INT_MAX` would wrap the cast negative / overflow the addition. The `pbkdf2_sha256` and `cert_fingerprint_sha256` paths already carried the equivalent `INT_MAX` guard - these two did not, an inconsistency. Now `seal` rejects `pt_len > INT_MAX - AES_TAG_SIZE` (`luaL_error`) and `open` soft-fails `ct_len > INT_MAX` to `(nil, err)` like its adjacent too-short guard. Unreachable in practice (user.tbl / backups are KB-MB, never 2 GB); pure consistency hardening, no behaviour change for real inputs. Verified by an adclib rebuild + the full `aes_gcm` round-trip / utf8 / hashpas / unescape unit suites (Linux) - no `tiger.h` width change, so the Windows CI leg is unaffected. 3.2.x only.

- **`core/http_router.lua`: OPTIONS introspection no longer leaks path existence to anonymous callers (pre-3.2.0 defense-in-depth).** The `OPTIONS` auto-introspection (§6.6, returns `204 + Allow`) short-circuited **before** auth resolution, while the router's normal `405`/`404` outcomes only reveal a path to an **authenticated** caller (an anonymous request 401s regardless of whether the path exists). So `OPTIONS /v1/config` returned `204 + Allow` to anyone but `OPTIONS /v1/nonexistent` returned `401` - a path-existence + method oracle that contradicted the router's own stated invariant. Auth is now resolved before the OPTIONS branch, and introspection requires either a valid token or a `scope="none"` (public) route on the path: an anonymous `OPTIONS` on an auth-gated path now `401`s like a `GET` would, while `/health`, the webhook receiver and any other public route still answer anonymous `OPTIONS` with `204 + Allow`, and authenticated introspection is unchanged. LOW severity (loopback-only admin listener; reverse proxy fronts non-loopback). Proven via `tests/unit/http_router_test.lua` (drives `dispatch()` directly - RED 2/72 pre-fix: anonymous auth-gated `OPTIONS` returned `204 + "GET, POST, HEAD, OPTIONS"`) plus a wire-level smoke assertion. 3.2.x only.

- **`core/cfg_secret.lua`: harden master.key handling (pre-3.2.0 defense-in-depth).** Three fixes from the pre-3.2.0 audit, all LOW severity: (1) the on-disk permission gate refused **any** mode that was not literally `600`, so a correctly-hardened `0400` (owner read-only) key was classified insecure and *refused hub boot* with the advice to `chmod 600` - i.e. to LOOSEN it. It now treats a key as secure iff group and other have no access (the last two `stat -c '%a'` octal digits are `00`), so `600`, `400`, `700`, ... all pass and only genuine group/other access is rejected. (2) First-boot key generation wrote the key with a plain `io.open` + `chmod`-after, leaving the fresh key group-readable under the daemon umask (`027`) for the window until the chmod landed; it now uses the repo's atomic `tmp -> chmod 600 -> rename` secret-write idiom (mirrors `cert_bootstrap._write_secret`), so the real `master.key` path appears in one step already at `600` and a mid-write power loss cannot leave a half-written key. (3) `seal()` now validates the GCM nonce length before use (`#nonce == 12`), matching `backup_archive.pack`, so an RNG failure degrades to `(nil, err)` explicitly instead of relying on the C layer's arg-check. New `tests/unit/cfg_secret_perms_test.lua` (both CI legs) covers the mode classifier with a self-contained RED for the old exact-`600` logic. No operator action; behaviour is unchanged for the common `0600` key. 3.2.x only.

- **`etc_trafficmanager` v2.7 -> v2.8: also block the NAT-traversal fallback (DNAT / DRNT), closing a transfer / CCPM bypass for passive / CGNAT users.** The plugin dropped a blocked user's file-transfer / CCPM setup on the DIRECT-connection path (CTM via `onConnectToMe`, RCM via `onRevConnectToMe`) but never hooked the NAT-traversal fallback (`onNatTraversal` / `onNatTraversalReply`), so when a direct connection was impossible - exactly two passive / CGNAT peers, whose clients fall back to NAT traversal - a blocked user could still establish the connection. CCPM rides the same CTM / RCM / NAT wire commands, so CCPM leaked the same way (reported by Sopor: CCPM succeeds on passive users despite both being blocked with no common hub - the setup routes through his own hub's unblocked NAT-traversal relay, and the passive > active success rate is the tell). Two new listeners mirror the CTM / RCM block logic exactly (`user:level() < masterlevel` gate + `need_block` on user and target; `need_block` tolerates a nil target, so a targetless DNAT does not crash). Pre-existing since the plugin only ever hooked CTM / RCM - NOT a luadch-ng regression (it is pulsar's plugin, based on his `etc_transferblocker`). Provably fails pre-fix (§1a.7): `tests/unit/etc_trafficmanager_test.lua` gains a NAT-traversal section, RED on the unpatched plugin (the two listeners are unregistered) and GREEN patched (blocked user / target -> PROCESSED, clean and op-level -> nil, nil-target safe), alongside a CTM / RCM parity control. Operators on the 3.1.x line get a drop-in on the v2.2 base (§8). 3.2.x only.

- [#504](https://github.com/luadch-ng/luadch-ng/issues/504) - **`etc_proxydetect`: report and audit the connecting user's nick, not `"?"`.** The proxy/VPN block report and its `proxydetect.block` audit fire-site logged the nick as `"?"` instead of the real identity. They now capture the connect-time nick, so the opchat report and the audit record carry the actual user. Bugfix, 3.2.x only.

- [#502](https://github.com/luadch-ng/luadch-ng/issues/502) - **`etc_trafficmanager`: the show-blocks listing now includes auto-blocked users who are currently online.** The command that lists automatically-blocked users omitted the ones that were online at the time, so an operator auditing the block list saw an incomplete picture; online auto-blocked users are now listed too. A drop-in `etc_trafficmanager` v2.2.1 was handed to the 3.1.x operator as the lighter alternative to a maintenance release (per §8). Live-verified on the `:dev` hub before promotion. Bugfix, 3.2.x only.

- **Six operator commands (`+ban nick` / `+disconnect` / `+gag` / `+redirect` / `+hideshare` / `+msgmanager`) could not act on an online user whose nick carries a `usr_nick_prefix` prefix when the operator TYPED the base nick - they silently took the offline path.** With `usr_nick_prefix` active (a level-based tag like `[HUBOWNER]` prepended to the nick), the plugin re-keys the hub's internal nick table from the base nick to the **prefixed** nick (`user:updatenick`). `hub.isnickonline( <base nick> )` is an exact-key lookup with no fallback, so an operator typing the base nick got `nil`: `+ban nick <nick> ...` **stored the ban but never kicked** the online user (the operator believed they had removed them; a reconnect was blocked but the current session stayed), and the others reported "user offline" and did nothing. Right-click usercommands were **not** affected - they pass `%[userNI]` (the current *display* nick = the prefixed nick) or `%[userSID]`; only a typed base nick broke, which is why it went unnoticed for so long. `+accinfo` still showed the user online because it iterates by SID (prefix-agnostic), which is exactly what made it look like "permanent ban does not kick" on the dev testhub - it was this resolution bug, not the [#444](https://github.com/luadch-ng/luadch-ng/issues/444) permanent-ban feature (that works). Fixed by falling back to `user:firstnick()` - the original nick, captured once at login and never re-keyed - via a plugin-local `find_online_by_firstnick`, the **same idiom `etc_trafficmanager` already uses** (which closed upstream `luadch/luadch#240`, the identical bug class). The `§1a.1` grep, confirmed by an independent two-pass review, found six affected operator ACTION commands (and both of `cmd_ban`'s target-resolution sites): `cmd_ban` (the ADC `nick` branch and the HTTP `POST /v1/bans` `http_find_online` nick path), `cmd_gag` (gag and ungag), `cmd_disconnect`, `cmd_redirect`, `usr_hide_share`, and the ADC path of `etc_msgmanager`. Four of these (`cmd_gag`, `usr_hide_share`, `etc_msgmanager`, and cmd_ban's stored bans) already key their store by firstnick, so only the *resolution* was wrong. Kept **plugin-local, not a change to core `hub.isnickonline`**, because that primitive's exact-current-nick semantics back availability checks (`cmd_reg` / `cmd_nickchange` ask "is this nick free?") where a firstnick fallback would wrongly report free nicks as taken. The commands that already know the target's level up front (`cmd_upgrade` / `cmd_setpass` / `cmd_delreg` / `cmd_nickchange`) keep their existing prefix-dance and are intentionally untouched; the six here cannot prefix-dance (they act on an arbitrary online target whose level they only learn after resolving). A tail of read-only info commands (`+userinfo` / `+sslinfo` / `+myip` / `+myinf` / `+uptime` by nick) shares the same resolution gap but only *displays* stale "offline" - no misleading action - and `bot_session_chat` shares it but stores members by display nick (needs a different fix than the one-line fallback); both are left for a separate follow-up. `cmd_ban` v0.44 -> v0.45, `cmd_disconnect` v1.4 -> v1.5, `cmd_gag` v0.12 -> v0.13, `cmd_redirect` v0.7 -> v0.8, `usr_hide_share` v0.5 -> v0.6, `etc_msgmanager` v0.10 -> v0.11; `cmd_disconnect`, `cmd_redirect` and `usr_hide_share` gain their first `_`-prefixed test seams and so join the seam-only roster in [`docs/PLUGIN_API.md` §8](docs/PLUGIN_API.md). Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/nick_prefix_resolution_test.lua`, both smoke.yml legs) drives each patched resolution with a prefixed online user whose base nick misses `isnickonline`, and asserts the user is kicked (ADC STA 232 timed / 231 permanent) / gagged / ungagged / redirected / share-toggled / resolved-for-block instead of hitting the offline path - red on the unpatched plugins (0/15), green patched (41/41). No GitHub issue for the fix (maintainer decision); found during testhub validation of the post-[#447](https://github.com/luadch-ng/luadch-ng/issues/447) dev batch. 3.2.x only, not backported.

- **`bot_session_chat`: the deferred follow-up to the nick-prefix arc above - a session-chat owner could not `+add`/`+del` a prefixed member by their base nick.** Unlike the other affected plugins, this one identified the chat owner and every member by the **current display nick** (`user:nick()`) throughout - stored in `sessions_tbl` and compared on every membership/owner check - so it could not take the one-line `find_online_by_firstnick` fallback: resolving the add-target but still storing the typed base nick would leave the stored key (`Bob`) out of step with the membership check (which sees the prefixed `[REG]Bob`). Fixed by re-keying the whole identity model to `user:firstnick()` (the stable, prefix-independent nick): `owner` + `members` are stored by firstnick, `check_if_member` / `check_if_owner` / the `feed` relay compare firstnicks, `+add`/`+del` resolve the typed nick via `hub.isnickonline( id ) or find_online_by_firstnick( id )` and store/match the resolved firstnick, and `msg_to_members` resolves each stored firstnick back to the (possibly prefixed) online user. Live announcements (member added/removed, welcome, the create broadcast) still show the current display nick; the `+members` list and the bot's `by:` description now show the base nick (the stored firstnick), consistently from create through every `refresh_bot`. This is the store-by-firstnick convention `cmd_gag` / `usr_hide_share` / `etc_msgmanager` already use; the session store is ephemeral (wiped on hub exit), so there is no migration. Bot resolutions (the session bot is never prefixed) were left untouched. `bot_session_chat` v0.4 -> v0.5 (its first `_`-prefixed test seams, so it joins the seam-only roster in [`docs/PLUGIN_API.md` §8](docs/PLUGIN_API.md), now twelve). Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/bot_session_chat_prefix_test.lua`, both smoke.yml legs) drives the real `client` EMSG handler for `+add Bob` / `+del Bob` where Bob is online as `[REG]Bob`, and asserts the member store holds the firstnick `Bob` (not `[REG]Bob`) and that membership is recognised - red on the unpatched plugin, green patched (12/12). 3.2.x only, not backported.

- **Follow-up to the nick-prefix resolution fix above: the read-only info commands `+userinfo` / `+sslinfo` / `+myip` / `+myinf` had the same gap.** Typing the base nick of a prefixed online user made `+userinfo nick <base>` / `+sslinfo <base>` report "user offline / not found", and `+myip <base>` / `+myinf <base>` silently fall back to showing the CALLER's own info instead of the target's - the same `hub.isnickonline(<base nick>)` miss, applied the same `find_online_by_firstnick` fallback. These are read-only (they only display, never mislead an operator into thinking an action happened), which is why they were split from the action-command PR. `usr_uptime` was on the review's candidate list but re-deriving from source showed it is **not** reachably broken: its only nick-resolution site is the CT2 right-click path (which passes the prefixed `%[userNI]`), while a typed `+uptime <nick>` shows the caller's own uptime and never resolves a base nick - so it was deliberately excluded (§1a.5). `bot_session_chat` also shares the gap but stores members by display nick throughout (a one-line fallback would resolve then store an inconsistent key), so it stays deferred. `cmd_userinfo` v0.24 -> v0.25, `cmd_sslinfo` v0.04 -> v0.05, `cmd_myip` v0.3 -> v0.4, `cmd_myinf` v0.1 -> v0.2; `cmd_sslinfo` / `cmd_myip` / `cmd_myinf` gain their first `_`-prefixed test seams and join the seam-only roster in [`docs/PLUGIN_API.md` §8](docs/PLUGIN_API.md) (now eleven). Provably fails pre-fix (§1a.7): `tests/unit/nick_prefix_resolution_test.lua` (both smoke.yml legs) gains a read-only section - the firstnick resolver is unit-tested for all four and `+myip <base>` gets an end-to-end check that it shows the target's ip, not the caller's - red on the unpatched read-only plugins, green patched. 3.2.x only, not backported.

- **`etc_blocklist` / `etc_whitelist`: `+blocklist`/`+whitelist export` and `import` printed their failure reason in English regardless of `cfg.language`** ([#456](https://github.com/luadch-ng/luadch-ng/issues/456)). Both plugins localise every *success* and most *failure* reasons through a lang template, but three technical failure returns in the JSONL export/import helpers were raw English string literals - `"open failed: <io error>"` (export and import) and `"json.encode failed: <error>"` - and the dispatcher's `reply( msg or tostring( err ) )` surfaced them verbatim. The matching templates (the generic `msg_export_fail` / `msg_import_fail`) existed in the lang files once but were never wired in and were removed as dead by [#458](https://github.com/luadch-ng/luadch-ng/pull/458) (the #447 dead-code arc); this wires localisation back with granular per-reason keys instead. (The issue's table also listed `add` as affected; re-derived from source, `add` was already localised - `do_add_entry` always returns a `utf_format( msg_*, ... )` message - so only the three export/import strings were the real gap.) Fixed by adding two per-reason keys, `msg_open_failed` (`"Could not open the file: %s"`) and `msg_encode_failed` (`"JSON encode failed: %s"`), in **en + de** for both plugins, and routing the three raw returns through them - matching the plugins' existing per-reason localisation convention. The template embeds the `io.open` error verbatim, which already names the path (an earlier `"Could not open %s: %s"` that also passed the path printed it twice - `path: path: reason` - caught on the testhub), so the reason string carries the path exactly once. `etc_blocklist` / `etc_whitelist` v0.02 -> v0.04. Also gave `etc_blocklist` the `_do_export_jsonl` / `_do_import_jsonl` test seams its twin `etc_whitelist` already exported (§1a.1 twin parity). Provably fails pre-fix (§1a.7): the existing `tests/unit/etc_blocklist_test.lua` and `etc_whitelist_test.lua` (both smoke.yml legs) gain cases that force an import open-failure and an export encode-failure and assert the reply renders the template (`"Could not open"` / `"JSON encode failed"`) and no longer the raw `"open failed:"` / `"json.encode failed:"` - four checks per plugin that are red on the unpatched code. 3.2.x only, not backported.

- **`cmd_userinfo`: the `+userinfo` value column was ragged - it jumped between positions and the `Sent:` / `Received:` pair sat offset from the block above** ([#459](https://github.com/luadch-ng/luadch-ng/issues/459)). The labels come from the (translatable) lang file with inconsistent widths (`Nick:` vs `Version:` vs `Received:`), and the code prepended a **fixed tab count** to each value (`\t\t`, and inconsistently a single `\t` for `Sent`/`Received`). Tabs snap to 8-column stops, so the value column landed wherever the label length pushed it, and it also depended on how each client renders a tab - so the alignment was never reliable. Fixed by aligning in code, independent of tab-stop width: a small `align_labels` pass pads every value line's label to a single width **computed from the actual labels** (so it is correct in any language, and re-derives automatically if a label is retranslated) and lets the value follow directly; the fixed tab prefixes are gone. Space-padding in a monospace chat is the only client-independent alignment. Banner and blank lines are untouched, and the second `%s` on the `Level: %s  [ %s ]` line rides along in the tail. The lang files are not touched - `align_labels` normalises the loaded format string, so no per-language space-counting to maintain. Cosmetic only, no functional change; `cmd_userinfo` v0.23 -> v0.24. Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/cmd_userinfo_align_test.lua`, both smoke.yml legs) drives `align_labels` against the **real** `msg_userinfo` of both shipped lang files and asserts the raw format's value column is ragged (the bug) while the aligned one is uniform, in `en` and `de` independently. (`align_labels` is exposed as an `_`-prefixed test seam, so `cmd_userinfo` joins the seam-only plugins noted in `docs/PLUGIN_API.md` §8.) 3.2.x only, not backported.

- **`etc_cmdlog`: `+cmdlog show` and `GET /v1/log/cmd` showed a frozen, possibly mixed-language log because the language labels were baked into each line at write-time** ([#460](https://github.com/luadch-ng/luadch-ng/issues/460)). The `onBroadcast` writer stored `date .. msg1 .. cmd .. args .. msg2 .. nick`, embedding the labels active *when the command ran*; both readers dumped the file raw and never re-formatted. So every entry was frozen in its write-time language, a hub whose `cfg.language` changed showed a **mix**, and on a German hub whose entries all predated the switch `+cmdlog show` printed English (`Command:` / `used by:`) despite every other plugin reporting German. Distinct from the v1.4 `#301`-class fix below (that one was unreachable lang keys; this is a design flaw in *when* localization happens). Fixed by storing entries **language-neutral** - four fields (`ts`, `cmd`, `args`, `nick`) separated by the US control byte (`0x1f`) - and applying the current `msg1` / `msg2` labels at **read-time** via one shared `render_line`, on both the ADC and HTTP path, so old and new entries alike render in the hub's current language. Migration is automatic and lossless: an old baked line has no delimiter, so `render_line` returns it as-is (its frozen language) rather than dropping or mis-parsing it. Every field is run through `util.strip_control_bytes` at write-time, which both keeps the delimiter out of any field and closes a **latent corruption** where a newline in a command's arguments split one entry across two log lines. The `GET /v1/log/cmd` response contract is **unchanged** (`{lines:[string], returned, total_lines}`); its lines are simply re-localized now instead of frozen - so no `docs/HTTP_API.md` change was needed. Returning structured fields there is a possible future contract improvement, deliberately deferred to keep this a non-breaking language fix. `etc_cmdlog` v1.4 -> v1.5. Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/etc_cmdlog_test.lua`, both smoke.yml legs) writes an entry under `en`, switches the hub to `de`, and asserts `+cmdlog show` renders `Befehl:` / `benutzt von:` - on the unpatched plugin the labels are baked English and it fails; it also covers the old-line raw fallback, a mixed-language file, redacted-arg storage, control-byte stripping, and the HTTP path. 3.2.x only, not backported.

- **`etc_records`: a missing / empty / corrupt `etc_records.tbl` crashed `onLogin` for every user** ([#465](https://github.com/luadch-ng/luadch-ng/issues/465)). The record store is a positional 8-slot table; the load did `util.loadtable(p) or { }`, which guards a nil *table* but not nil *slots*. With an empty or truncated file `records[3]` / `[6]` / `[8]` were `nil`, and the max-tracking comparisons `new > tonumber(records[N])` in `hubshare` (`[3]`), `onliners` (`[6]`) and `topshare` (`[8]`) - all three fired from `onLogin`, and `hubshare` also from the `onTimer` - raised **"attempt to compare nil with number"**, spamming the error log on each login until the file was restored. The plugin only worked because the shipped seed happened to be present; it did not actually tolerate the degraded state its own `or { }` implied. Fixed by seeding each slot at load to the same default shape `reset()` already builds (numeric maxes to `0`/`1`, dates/times to now, nick to `"none"`); existing values are preserved, and `tonumber(...) or N` also repairs a slot that persisted as a non-numeric string. `etc_records` v0.8 -> v0.9. Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/etc_records_test.lua`, both smoke.yml legs) fires `onLogin` against empty / truncated / corrupt / per-slot-nil record tables and asserts no crash - on the unpatched plugin these raise the nil-compare **independently at each of the three sites** (`hubshare` :362, `onliners` :389, `topshare` :410), while a well-formed-file control still passes on both trees. Found while investigating #464, where an emptied `scripts/data/` in a smoke run surfaced this crash on every login. 3.2.x only, not backported.

- **The lifetime "hub runtime" counter silently reset to zero on every upgrade** ([#445](https://github.com/luadch-ng/luadch-ng/issues/445)). The persisted accumulator behind `+runtime show` / `+uptime` / `+hubinfo` / `GET /v1/runtime` lived in `core/hci.lua`. `core/` is a **shipped** directory - `CMakeLists.txt` does `install(DIRECTORY core/)` wholesale, and the Docker image bakes `core/` in rather than mounting it - so a source `cmake --install` over an existing tree, and every Docker image update, wrote the pristine `hubruntime = 0` straight over the operator's accumulated value. A **zeroed** file is indistinguishable from a fresh hub, so nothing detected the loss; a counter meant to survive restarts was wiped on the one event it most needed to survive. Fixed by moving the store to `scripts/data/hub_runtime.tbl` - the operator-owned, Docker-mounted plugin-state dir that upgrades never clobber (the CLAUDE.md §7 / DEVELOPMENT.md §3 rule this file always broke). `hub_runtime` (v0.9 -> v0.11) is now the **sole writer** and does a one-time migration on load: if the new store is absent it adopts the value from a legacy `core/hci.lua`. That adoption only recovers a value if the legacy file still holds it when the hub boots - and a naive `cmake --install` would zero `core/hci.lua` *before* boot (the very clobber #445 is about), so the migration would read zeros and recover nothing. The critical half of the fix is therefore in **`CMakeLists.txt`**: `install(DIRECTORY core/ ... PATTERN "hci.lua" EXCLUDE)` so a source upgrade leaves the operator's on-disk `core/hci.lua` untouched, letting the migration adopt the real value. Docker cannot migrate - its pre-fix counter lived in the container's ephemeral writable layer (`core/` is baked, not mounted) and was already discarded on every container recreate - but it gains a persistent counter going forward now that the store is on the mounted `scripts/data/` side. `cmd_uptime` (v0.10 -> v0.11) and `cmd_hubinfo` (v0.30 -> v0.31) became **pure readers** of the new path - their old `check_hci()` create-if-missing would otherwise have raced a zero file in ahead of the migration; `cmd_hubinfo` additionally now re-reads the store on each `+hubinfo` instead of caching it at plugin load (the cached value went stale as the 60s onTimer advanced the counter). `core/hci.lua` is kept read-only as the migration source and is no longer written by anything; a future release removes it. `docs/HTTP_API.md`, `docs/SCRIPTS.md` and `CLAUDE.md` §3 updated. Provably fails pre-fix (§1a.7): a new unit test (`tests/unit/hub_runtime_migration_test.lua`, both smoke.yml legs) exercises the pure migration decision + the load-time side effect over an in-memory fs; a `/v1/runtime` smoke stage asserts `scripts/data/hub_runtime.tbl` exists after a PUT (that file never existed pre-fix, when the store was `core/hci.lua`) and the PUT value did not leak into the shipped `core/hci.lua`; and the CMake exclusion was verified directly - seeding `core/hci.lua` to a non-zero value and re-running `cmake --install` preserves it with the `PATTERN EXCLUDE` and zeros it without (the exact scenario #445 describes). Found by the dead-code audit (`core/hci.lua` was flagged as a possible dead stub - it was neither, but the check surfaced this). **Testhub follow-up (v0.11):** `migrate_or_init` read the legacy `core/hci.lua` on *every* boot (it was an unconditional `util.loadtable` argument), and `util.loadtable` logs a `checkfile` "No such file" error for a missing file - so on Docker, where `core/hci.lua` is never present, the error log gained one such line on every start. It now consults the legacy file only when the new store is absent/corrupt, and probes its existence with `io.open` first so a missing one logs nothing; the migration test gains a steady-state assertion that the legacy file is never touched once the new store holds a value (red on v0.10). 3.2.x only, not backported.

- **`cmd_ban`: a negative `<time>` silently threw the ban away while the help text promised it meant "ban forever".** `+ban nick X -5 reason` stored `bantime = -300`; at X's next login the expiry check computed an always-negative `remaining`, treated the entry as expired and **pruned it** - so the target was kicked once and then walked straight back in, while the operator believed X was banned. The only time validation was `is_integer( time )`, and `-5 == math.floor( -5 )`, so negatives sailed through. Meanwhile `help_desc` and both lang files advertised *"negative values means ban forever"* / *"negative Werte bedeuten Bann für immer"* - a feature deliberately removed back in v0.15 (*"removed the ban forever crap"*), leaving the promise stranded for years. Fixed by rejecting `< 1` (new `msg_badtime`, en + de) at the existing validation site, which sits **before** target resolution; the stale promise is gone from `help_desc` and both lang files. This also closes a divergence flagged by §1a.1: the **HTTP path has enforced `min = 1` since #82** (request_schema + a `duration_minutes < 1` guard) - only the older ADC path was broken. Zero is rejected for the same reason (a 0-minute ban expires instantly). The commented-out ban-forever block is removed too: it referenced `msg_forever`, which no lang file defines, so it could never have been revived by uncommenting. Note the originally-suspected fix - dropping `[-]?` from the parser - is a **no-op**: `%S+` already matches `-5` (verified), so validation, not parsing, was the gap. cmd_ban v0.42 -> v0.43. Smoke: a new stage sends `+ban nick <nonexistent> -5|0 spam` and asserts the rejection, plus a positive control that a valid time still reaches the target lookup; provably fails pre-fix (§1a.7 - pre-fix the hub answers `User not found.`, proving the negative was accepted). A real permanent ban is deliberately **not** reintroduced here and is tracked as a separate feature (ADC `STA 231` + `TL-1`, an explicit keyword rather than a magic negative). Found by the dead-code audit. 3.2.x only, not backported.

- **`etc_cmdlog`: the German translation was dead - the plugin read lang keys that no lang file defines.** `scripts/etc_cmdlog.lua` looked up `lang.failmsg1` / `lang.failmsg2`, but `scripts/lang/etc_cmdlog.lang.{en,de}` define those messages as `msg_denied` / `msg_nofile`. Both lookups therefore returned nil, the `or "<english literal>"` fallback fired every time, and a German hub printed English on `+cmdlog show` regardless of `cfg.language` - silently, with no error ever raised. Pre-existing since the SourceForge -> GitHub migration (both sides landed already-mismatched); plugin bumped to v1.4. **This is the second instance of the same class** ([#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-2 fixed `usr_share`'s `lang.msg_minmax` vs `msg_sharelimits`), and it reappeared in a plugin the one-off `usr_share_lang_test.lua` could not see - so per §1a.1 the guard is now repo-wide: new `tests/unit/plugin_lang_test.lua` enumerates the shipped plugins from `examples/cfg/cfg.tbl`'s `scripts` whitelist and asserts every `lang.X` reference in each plugin resolves in **both** `.lang.en` and `.lang.de` (68 plugins, 1034 distinct references). It **supersedes** `usr_share_lang_test.lua` (deleted - `usr_share` is one of the 68 swept, so coverage is a strict superset, and the `msg_sharelimits` regression stays guarded); registered on both CI legs in its place. The sweep surfaced a third instance, also fixed here: `cmd_delreg` declared `local msg_reason = lang.msg_reason or "No reason."` - a key no `cmd_delreg` lang file defines - copy-pasted from `cmd_ban`, where the key exists and the local is used; harmless because the local was never referenced, but the lookup was dead either way, so the dead local is gone (v0.33). Two traps worth recording for anyone extending the test: asserting `type(value) == "string"` is wrong (`ucmd_menu*` menu structures, `month_name`, `cmd_ascii.pics` are legitimately **tables** - an early draft produced 149 false positives out of 151 hits), and enumerating via `io.popen("ls")` is wrong (a native Windows Lua routes popen through cmd.exe, finds no `ls`, scans zero plugins and passes **vacuously** - hence the cfg-whitelist enumeration plus explicit minimum-count guards). Provably fails pre-fix on all three findings (§1a.7). 3.2.x only, not backported.

- **`release.yml` never installed zlib's development package - the first `v3.2.0` tag would have failed the aarch64 build at configure.** `CMakeLists.txt` does `find_package(ZLIB REQUIRED)` (unconditional, for the Phase 8 S4b `zlib_stream` ZLIF module), but none of the three release legs installed it: the Linux leg had `build-essential cmake libssl-dev`, the aarch64 `debian:bullseye-slim` container had `build-essential perl wget ca-certificates patchelf file git` under `--no-install-recommends`, and the msys2 leg had no zlib package. Every *other* build path already had it (`smoke.yml` on both legs, `docker/Dockerfile`, and `docs/BUILDING.md`'s documented prerequisites) - only the release pipeline drifted. The trap: the runtime library `libz.so.1` is present on effectively every Linux box (much of userspace links it), so zlib "looks" installed, but `zlib.h` and the `libz.so` link ship **only** in `zlib1g-dev`, and `build-essential` does not depend on it - verified from the package database (`apt-cache depends build-essential` has no zlib entry; a box with gcc/cmake/OpenSSL headers still had no `/usr/include/zlib.h`). This was never caught because `release.yml` only fires on `v*` tags and on `release/**` pushes, no `v3.2.x` tag exists yet, and the `release/3.1.x` line predates `zlib_stream` entirely - so the release pipeline has never once been run against a tree containing `find_package(ZLIB REQUIRED)`. Fixed by adding `zlib1g-dev` to both apt legs and `mingw-w64-ucrt-x86_64-zlib` to the msys2 leg. The `CMakeLists.txt` comment that legitimised the gap ("aarch64 Bullseye container has zlib as a base-system package - no change needed") is corrected and now lists every install site to keep in sync. Found by a dead-code audit sweep of the build glue. 3.2.x only (the 3.1.x line has no `zlib_stream` and is unaffected).

- **`core/sysinfo.lua`: `+hubinfo` now works on old Windows (Server 2008 R2 / Windows 7) instead of showing `<UNKNOWN>`** (Sopor-reported). The OS / CPU / RAM probes used `Get-CimInstance`, which is PowerShell 3.0+; PowerShell 2.0 (the default on Server 2008 R2 / Win7) raises `CommandNotFoundException`, so the queries returned empty / non-numeric output. On 3.2.x `cmd_hubinfo` already wrapped that in `or msg_unknown` (so it degraded to `<UNKNOWN>`, no crash), but the values were simply unavailable on old Windows. Each Windows probe now runs `try { Get-CimInstance ... } catch { Get-WmiObject ... }` in one PowerShell call - `Get-WmiObject` exists in every Windows PowerShell (2.0-5.1; `powershell.exe`, not the PS-7/Core `pwsh`), so PS 2.0 hosts take the catch branch and report real values (validated live on Windows 11 + against `Get-WmiObject` directly). `tests/unit/sysinfo_test.lua` (new, 21 checks incl. a regression that the WMI fallback stays in every Windows command - provably fails if removed, §1a.7); registered on both smoke.yml legs. The pre-refactor 3.1.x `cmd_hubinfo.lua` (v0.29) additionally CRASHED here (`attempt to concatenate a nil value 'cache_check_ram_total'`, because it had no `or msg_unknown` guard); a v0.30 drop-in for the 3.1.x line carries both the nil-guard and the WMI fallback (no `v3.1.15` release, per the §8 drop-in-plugin path). 3.2.x.

- **clear "port already in use" message + Windows port-hijack fix** (Sopor follow-up). When a listener could not bind its port the hub was unhelpful: on Linux it logged only the cryptic luasocket line (`...bind: address already in use`) then silently retried every 30s; on Windows the pre-bind `SO_REUSEADDR` (added for the Linux #128 TIME_WAIT fast-restart) has `SO_REUSEPORT` semantics, so a second process re-bound the SAME port with no error at all - two hubs then silently shared it, and any local process could hijack a luadch port. Two changes: (1) `core/server.lua` sets `SO_REUSEADDR` only on Linux (detected via `util.path_sep()`); on Windows it is skipped both pre- and post-bind, so a same-port second bind now fails visibly with `WSAEADDRINUSE` and the port can no longer be hijacked. Linux keeps the #128 fast-restart behaviour unchanged. (2) `core/hub.lua` `add_server_handler` now logs a clear operator line (`server: IPv4 TLS port 5001 is already in use - another process (or a second hub) holds it. Stop it or change the port in cfg/cfg.tbl; retrying every 30s.`) plus a generic message for any other listener-start failure that was previously swallowed. Verified that dropping `SO_REUSEADDR` on Windows does NOT regress the fast-restart: the full Windows smoke restart battery (plaintext / tier / `+reload` / dual-stack-same-port #107) stays green. Smoke: a new stage starts a second hub (separate dir, same ports as the live first hub) and asserts the guidance appears, provably failing pre-fix (§1a.7 - the assertion is on the new `change the port in cfg/cfg.tbl` phrase that the pre-fix cryptic line lacks; pre-fix Windows silently re-binds and logs nothing). Complements the single-instance lock (same Sopor thread): the lock stops a second copy in the SAME dir; this makes a second hub in a DIFFERENT dir with clashing ports fail loudly instead of silently. 3.2.x only.

- **single-instance lock: a second hub started against the same install tree now refuses to start** (Sopor). Nothing stopped two `Luadch.exe` (or `./luadch`) processes running from one install directory at once - on Windows especially, where `core/server.lua` sets `SO_REUSEADDR` before bind (a deliberate fast-restart fix, #128) and Windows gives `SO_REUSEADDR` `SO_REUSEPORT` semantics, so the second process re-binds the same port and both hubs then share (and race on) `cfg/user.tbl`, `master.key`, the plugin `scripts/data/*.tbl` stores and the logs; interleaved `saveusers()`/`savetable()` writes can corrupt them (data loss). Fixed in the C launcher (`hub/hub.c`) with a per-install lock file (`luadch.lock` in the install dir): unix `flock(LOCK_EX|LOCK_NB)` + `O_CLOEXEC`, Windows exclusive `CreateFile` (deny-share, non-inheritable handle, delete-on-close). Held open for the whole process lifetime and released by the OS on crash (no stale-lock cleanup step), retained by the daemon child across the `-d` fork and across `+reload` (the fd survives the `restart()`/atexit re-entry). Scope is per install directory - a test hub and a prod hub in separate directories on one box still run in parallel; only a second copy of the SAME install is refused (non-zero exit + "another instance is already running" to stderr and `log/exception.txt`). Fail-open: only the definitive already-held signal (unix `EWOULDBLOCK`, Windows `ERROR_SHARING_VIOLATION`) blocks boot; an un-creatable lock file (read-only fs and the like) warns and continues. Deployment note: a supervisor that starts the replacement before the old process has finished its shutdown drain sees the refusal - stop must complete before start (systemd / `docker restart` are sequential and unaffected). Smoke: a new stage starts a second hub against the live staging tree and asserts it exits non-zero with the refusal while the first stays up, provably failing pre-fix (§1a.7 - on the unpatched Windows binary the second instance boots and runs concurrently). Two-pass review (§1a.6) caught the missing `O_CLOEXEC` (a spawned child could otherwise inherit the fd and brick the next restart) and the `ERROR_ACCESS_DENIED`-is-not-a-live-instance classification. 3.2.x only, not backported (hardening, not a security/crash fix; per §8 the 3.1.x bar is high). Operator guide: [`docs/BUILDING.md`](docs/BUILDING.md).

- **`usr_hide_share`: also hide the shared-file count, not just the share size.** The plugin zeroed only the ADC INF `SS` (share size) for a hidden user, never `SF` (shared-file count), so clients still displayed how many files the user shared. Fixed by mirroring every `SS`-zeroing write with an `SF`-zeroing one at all three sites (the `onInf`/`onConnect`/`onStart`/`onExit` listener and the manual `+hideshare` command) and adding `SF0` to the `BINF` broadcast. v0.4 -> v0.5. Intended side effect (a consistency fix): a hidden user now also contributes 0 to the hub's aggregate file counts (PING reply to hublist scrapers, Prometheus `/metrics`, HTTP stats), matching the pre-existing behaviour for aggregate share size; no new kick path (`usr_share` min-share enforcement keys on `SS` only). §1a.7: diff-is-self-evidence (the unpatched plugin never touched `SF`; the fix is a 1:1 mirror of the already-working `SS` path) plus testhub validation. 3.2.x; not backported (self-contained plugin, a 3.1.x drop-in is possible on request).

- [#419](https://github.com/luadch-ng/luadch-ng/issues/419) (Kcchouette) - **the ADC parser now discards messages containing unknown escape sequences** (ADC 1.0 section 3.1: only `\s` / `\n` / `\\` are defined; "any message containing unknown escapes must be discarded"). `core/adc.lua parse()` validated UTF-8, structure and field values but never escapes, so a message carrying `\q` (or a trailing/unescaped backslash) was accepted and relayed - other clients then interpret the unknown escape differently (undefined per spec), a cross-client display-impersonation vector (`admin\q` may render as `admin` on a client that drops it; complements the #315 `unescape` hardening, which kept the receiving-side lenient - this closes the relay side). Validated **pairwise** (strip the valid `\s`/`\n`/`\\` first; any leftover backslash is then unknown): a naive "`\` not followed by s/n/`\`" scan is wrong - it false-positives on `\\q` (an escaped backslash + literal `q`, i.e. the valid wire form of the text `\q`) and misses a lone trailing backslash. A backslash-free message (most protocol commands) skips the gsub via a single plain `find`; a message with escapes (e.g. any multi-word chat, which carries `\s`) takes one bounded single-pass gsub - both cheap. Compliant clients never emit unknown escapes and are unaffected (and a plugin that hand-builds a raw message body should escape backslashes as `\\` - the `hub.broadcast` / `user:reply` / `cmd:adcstring` helpers already do); a drop is logged to `event.log`. Smoke: 3 checks on both legs - `\q` discarded + trailing backslash discarded (both provably fail pre-fix, §1a.7) + a positive control that a valid `\\` is NOT discarded (guards the false-positive). Spec-compliance, no hub-crash / no remote-DoS: 3.2.x only, not backported.

- [#423](https://github.com/luadch-ng/luadch-ng/issues/423) - **the hub bot's INF now escapes the operator email (`hub_email`) in its `EM` field.** With `hub_bot_email = true` (the "advertise the hub email in the userlist" toggle), `core/hub_bot_object.lua` appended `hub_email` to the hub-bot BINF **raw** - unlike the bot nick/desc, which go through `adclib.escape`. A space / newline / backslash in `hub_email` would then malform that INF, and since #419 the hub would **discard its own hub-bot INF** as an unknown escape, so the hub bot fails to load. Fixed by escaping the value like its siblings. (The #419 review that surfaced this also flagged a "wrong variable"; that was a false positive - `hub_bot_email` is the boolean toggle and `hub_email` is the value, so gating on one while appending the other is correct.) Smoke: a mode-switch boots with `hub_bot_email=true` + a backslash-bearing `hub_email` and asserts the hub-bot INF is not discarded (fails pre-fix under #419). 3.2.x only.

- [#416](https://github.com/luadch-ng/luadch-ng/issues/416) (Sopor) - **Windows: the hub crashed once it held ~64 concurrent sockets** with `core/server.lua:...: bad argument #1 to 'socket_select' (too many sockets)`. The event loop's `tick()` calls `socket.select` over every connected socket at once; on Windows luasocket's `select.c` hard-caps that at `FD_SETSIZE`, and the bundled build inherited the Winsock default of **64** (`luasocket/CMakeLists.txt` set only `WINVER`, never `FD_SETSIZE`). Once a hub held ~64 sockets (users + v4/v6 listeners + HBRI + HTTP) the unguarded `select` in `tick()` raised and the main loop died, dropping every user. Fix: define `FD_SETSIZE=1024` for the Windows luasocket build (`socket` module + `luasocket_static`), i.e. parity with the Linux glibc `fd_set`. Windows-only by design: on Linux redefining `FD_SETSIZE` cannot enlarge glibc's fixed 1024-bit `fd_set` (the >1024 case there needs the `select`->`poll` port tracked in [#310](https://github.com/luadch-ng/luadch-ng/issues/310)). the hub now logs the compile-time select capacity (`socket._SETSIZE`) once at boot from `hub.loop` (to `event.log`, when `log_events` is enabled) as a supplementary diagnostic - the crash itself already surfaces in `error.log` by default; a smoke assertion requires the capacity `>= 1024`, provably failing pre-fix on the Windows leg (§1a.7). 3.1.x backport candidate (hub-crash under load).

- [#78](https://github.com/luadch-ng/luadch-ng/issues/78) follow-up - **`etc_geoip`: `+reload` no longer re-checks MaxMind ~30s after boot every time.** Same class of bug as the `etc_blocklist_feeds` reload-throttle (#386): `next_update` is RAM-only and `onStart` (which fires on every `+reload`, a full Lua restart) unconditionally set it to `now + 30`, so an operator reloading several times a day hit MaxMind's rate-limited download endpoint each time - even though the DB only changes twice weekly and the persisted per-edition sha256 already prevents a redundant DB *swap* (it does not prevent the redundant *fetch*, which is what the rate limit counts). Fix: persist the auto-update check time to `scripts/data/etc_geoip.tbl` (written at cycle start, before the fetch, so a MaxMind rate-limit / failure still counts - those are exactly who a reload-loop must not re-hammer) and schedule `next_update` from it in `onStart`: `min(last_update + interval, now + interval)`, or a staggered `now + 30` only if never checked / overdue (the `min` caps a bogus future timestamp from clock skew / a corrupt state file). The GeoIP `.mmdb` survives reload regardless, so skipping the reload-refetch drops no protection. `etc_geoip` v0.01 -> v0.02; `tests/unit/etc_geoip_test.lua` gains a reload-throttle regression (reload-within-interval no re-check, re-check after the interval elapses from the persisted last check) that provably fails pre-fix (§1a.7). 3.2.x only, not backported (in-hub GeoIP auto-update never shipped in 3.1.x).

- [#398](https://github.com/luadch-ng/luadch-ng/issues/398) follow-up (real-world Discourse bring-up) - **`etc_webhook`: two fixes from getting the receiver live against a real Discourse.** (1) `etc_webhook` v0.01 -> v0.02: `dedup_load` called `util.loadtable` on the dedup state file unconditionally, and on a first run (no file yet) `util.loadtable` -> `checkfile` logs an `error.log` line that the HubSecurity bot relays to ops on every fresh start. Fixed by probing with `io.open` first (first-run-silent), matching the plugin's own `load_config` and the sibling state-file loaders (`etc_regserver_announce`, `etc_blocklist_feeds`); `tests/unit/etc_webhook_test.lua` gains a regression that provably fails pre-fix (§1a.7) plus a complement proving an existing dedup file still loads. (2) The shipped Discourse example (`examples/cfg/webhooks.tbl` + `docs/WEBHOOKS.md`) mapped `event_header = "x-discourse-event-type"`, but that header carries only the category (`topic`); the specific event (`topic_created` / `post_created`) is in `x-discourse-event` - so the `events` filter and per-event templates never matched. Corrected to `x-discourse-event`, verified against a live Discourse 2026.7 delivery. The example templates now include a clickable forum link (built from the confirmed `topic.slug` / `topic.id` etc. payload fields; a bare `https://` URL is auto-linkified by DC clients), and the docs clarify that `default_template` is a top-level endpoint field and that `{event}` is empty unless `event_header` is set. 3.2.x only.

- [#83](https://github.com/luadch-ng/luadch-ng/issues/83) follow-up - **`etc_prometheus` could crash at plugin load when whitelisted on an older `cfg.tbl`.** Its master toggle `etc_prometheus_activate` shipped only in `examples/cfg/cfg.tbl` and was missing from `core/cfg_defaults.lua` - the lone `_activate` key not registered there. The plugin reads it at module scope (`local activate = cfg.get("etc_prometheus_activate")`), and `cfg.get` on a key absent from BOTH the operator `cfg.tbl` AND `cfg_defaults` raises (`_defaultsettings[target][1]` indexes nil) - the same `cfg.get`-raise mechanism as the #398 precursor below, here from a missing registration rather than a dynamic key. Fresh installs and CI smoke passed because the example `cfg.tbl` carries the key; an operator who added `etc_prometheus.lua` to `cfg.scripts` on a `cfg.tbl` predating the key crashed on load. Fix: register `etc_prometheus_activate` (default `false`) in `cfg_defaults.lua`. A repo-wide sweep confirmed it was the only plugin-read cfg key missing a default. New `tests/unit/cfg_defaults_test.lua` (both smoke legs) loads the real `cfg_defaults` settings table, reproduces `cfg.get`'s lookup for an omitting `cfg.tbl`, and provably fails pre-fix (§1a.7 - the exact `attempt to index a nil value` raise). 3.2.x only (the plugin is 3.2.x-only), not backported.

- [#398](https://github.com/luadch-ng/luadch-ng/issues/398) precursor - **`core/secrets.lua`: `secrets.lookup()` on a cfg key unknown to BOTH `cfg_defaults` and `cfg.tbl` no longer crashes the caller.** `lookup` falls back to `cfg.get(cfg_key)` when no `LUADCH_*` env var is set, and `cfg.get` RAISES on a wholly-unknown key (it indexes the nil default entry, `_defaultsettings[target][1]`). No current caller hits it (all lookups use real `cfg_defaults` keys), but the #398 webhook receiver resolves dynamically-named per-endpoint secrets (`etc_webhook_<name>_secret`) that need not be `cfg_defaults` keys - a miss would otherwise take down the plugin's `onStart`. Fix: pcall-guard the `cfg.get` fallback so an unknown key degrades to "unset" (`nil`); a genuine `cfg.tbl` value still returns normally. `tests/unit/secrets_test.lua` gains a regression that models the real `cfg.get` raise and provably fails pre-fix (§1a.7). 3.2.x only, not backported.

- [#405](https://github.com/luadch-ng/luadch-ng/issues/405) (Sopor) - **`usr_uptime`: per-user monthly online time was badly undercounted on busy hubs and lost across `+reload`.** 24/7 users showed only a few hours for the month ("22h" was common across users = roughly the time since the last hub restart). Two bugs, both meaning accumulated online time lived only in RAM and was dropped on the next restart: (1) `set_stop` reset the global `start` timer-gate on **every** logout, and `onTimer` only credits online users when `os.time() - start >= 60` - so on a hub with a logout more than once per 60s that gate never reached 60 and the periodic credit block never fired, leaving 24/7 users (who never log out) uncredited by the timer; their elapsed time only grew as the in-RAM `now - last_tick` delta and was lost on the next restart. (2) `last_tick` is RAM-only and set only in `set_start` (onLogin); a `+reload` re-runs `onStart` but fires no `onLogin` for still-connected users, so their `last_tick` stayed nil and the timer skipped them until they reconnected. Fixes: `set_stop` no longer touches `start` (the 60s credit cadence is now independent of logout activity), and `onStart` re-seeds `last_tick` for users already online. Max loss on a crash / restart is now <= one tick (60s) per user; pre-fix accumulated data cannot be reconstructed and stays as-is. `usr_uptime` v0.11 -> v0.12; new `tests/unit/usr_uptime_test.lua` (both smoke legs) reproduces both bugs and provably fails pre-fix (busy-hub 24/7 user credited 0 -> 300s; reload 120 -> stuck-120 vs 120 -> 240). This, not the #401 crash (which loses <= one tick), is where the reported multi-day loss came from. 3.2.x line; the 3.1.x line (v0.10, same bug) gets no release per maintainer decision - a patched drop-in (v0.10.1) is provided to the affected operator directly. 3.2.x only.

- [#401](https://github.com/luadch-ng/luadch-ng/issues/401) (Sopor) - **remote hub-crash: a peer that reset its connection right after the TCP accept took the whole hub down.** `core/server.lua`'s accept path reads the client IP via `client:getpeername()` and hands it to the pre-handshake guards; when the peer resets between `accept()` and `getpeername()` (trivially remote-triggerable - connect + immediate RST), `getpeername()` returns `nil`, and `ratelimit.accept_ip(nil)` raised `attempt to concatenate a nil value (local 'ip')` while building its token-bucket key `"ip:" .. ip`. The raise was **uncaught inside the listener's accept loop**, so the hub stopped accepting connections and dropped every online user (Sopor hit this on 3.1.11 - his first crash in 10+ years of running Luadch). `ratelimit.release_ip` already guarded `nil`; `accept_ip` did not - the asymmetry that made it reachable. Fix (defence in depth): `server.lua` now closes a socket whose peer address cannot be read *before* the guards run, and both `ratelimit.accept_ip` and `blocklist.check_ip` guard `nil`. New `tests/unit/ratelimit_test.lua` (both smoke legs) reproduces the exact nil-concat crash - provably fails pre-fix (§1a.7) - and locks the guard plus limiter-still-works / disabled-passthrough sanity. Remote unauthenticated DoS; **backport target v3.1.13** per [`CLAUDE.md` §8](CLAUDE.md#8-release-lines-and-support-policy).

- [#356](https://github.com/luadch-ng/luadch-ng/issues/356) follow-up - **the "Did you mean +X?" hint no longer fires on a command word followed by chat text.** After the #356 level-gate, an operator writing e.g. `talk to me brother` in main chat (they can run `+talk`, so the gate allowed it) still had the line swallowed and hinted - annoying, because that is an ordinary chat sentence that merely starts with a command word. `etc_hubcommands` v0.08 -> v0.09 tightens the bare-word trigger: the forgot-prefix hint now fires ONLY when the message is **exactly** a command word (`talk`), which is a genuine forgot-the-prefix attempt; a command word followed by text (`talk to me brother`) is treated as chat and passes through untouched. The #356 access-level gate still applies to the exact-word case (op-only command names do not leak to unprivileged users). **Exception (credential guard):** a command that carries a secret inline - `setpass` (`[+!#]setpass nick <NICK> <PASS>`), the only such command; `reg` auto-generates passwords now - registers with a new `{ secret = true }` flag (`add(cmd, func, minlevel, opts)`), and its `cmd <args>` forgot-prefix form is still swallowed **regardless of level**, so a mistyped `setpass nick x <pw>` never broadcasts the password to main chat (same intent as the #137 literal-bracket guard). `tests/unit/etc_hubcommands_test.lua` reworked: 10 assertions provably fail on v0.08 (op `talk to me brother` hinted, `setpass x pw` not swallowed for a low user, ...) and pass on v0.09. 3.2.x only, not backported.

- [#356](https://github.com/luadch-ng/luadch-ng/issues/356) (Sopor) - **the bare-word "Did you mean +X?" hint fired for commands the user cannot run.** `etc_hubcommands` swallows a main-chat line that starts with a known command word typed without the `[+!#]` prefix and hints at the command - but it fired for ANY registered command name regardless of access level. So an unprivileged user typing ordinary chat that happens to start with a privileged command word (the report's examples: `talk to me brother`, `reg me`; `talk` and `reg` are op-only) had their message swallowed (never reached main chat) and learned that an op-only command exists. Root cause: `etc_hubcommands.add(cmd, func)` was the only one of the three command-registration APIs that did not receive the command's level (`cmd_help.reg` and `etc_usercommands.add` both already take it), so the dispatcher had no way to know who may run a command. Fix: `add` gains an optional third arg `minlevel`; the bare-word hint now only fires (and only swallows) when `user:level() >= minlevel`, otherwise the line passes through to main chat untouched. A command whose level is not registered (nil - public commands, or a not-yet-updated third-party plugin) stays ungated, so the hint behaves exactly as before for them. The **literal-bracket** branch (`[+!#]reg <user> <pw>`, [#137](https://github.com/luadch-ng/luadch-ng/issues/137)) is deliberately NOT gated - it must keep swallowing regardless of level to prevent the credential leak it was built to stop, and its `[+!#]word` input form has no false-positive overlap with normal chat. Alias-resolved hints are gated on the *resolved* command's level. `etc_hubcommands` v0.07 -> v0.08. Sweep: all 46 bundled command plugins that gate access now pass their level to `hubcmd.add` (a 1-token change mirroring the level they already pass to `cmd_help.reg`; `permission`-table gates advertise `util.getlowestlevel(permission)`, the same value they use for help registration); genuinely public commands (`+help`, `+myip`, `+slots`, `+userinfo`-on-self) stay ungated. New `tests/unit/etc_hubcommands_test.lua` (both smoke legs) provably fails pre-fix (low-level user's `talk to me` is hinted + swallowed) and passes patched, and locks the literal-bracket branch staying ungated (no cred leak). 3.2.x only, not backported (UX bug, no security impact per [`CLAUDE.md` §8](CLAUDE.md#8-release-lines-and-support-policy)).

- [#355](https://github.com/luadch-ng/luadch-ng/issues/355) (Sopor) - **`cmd_gag`: the ADC `+gag` path could gag a bot (Hubbot / OpChat / RegChat).** The `+gag mute|kennylize|shadowmute` handler resolved an online target and checked level-hierarchy + self-gag but never `target:isbot()`, so a hubowner could create a persistent gag entry against a hub bot. The HTTP endpoint was already safe (util_http's non-bot preflight), and ~10 sibling user-action commands (cmd_redirect, cmd_disconnect, cmd_ban, cmd_nickchange, cmd_setpass, cmd_upgrade, cmd_delreg, cmd_mass, etc_msgmanager, cmd_sslinfo) already reject a bot target (via `msg_isbot` / `msg_bot` / `msg_reg` guards or a broadcast filter) - `cmd_gag` was the lone moderation command with no guard. A 3-agent sweep of all 75 bundled scripts confirmed no other command in the moderation/silence class has the gap (the connect/login-time enforcers - usr_hubs/share/slots/nick_*/hub_inf_manager - are structurally bot-unreachable because bots never fire onLogin/onConnect/onInf). Fix: reject a bot target in the ADC handler with `msg_isbot` ("User is a bot."), matching the sibling pattern; `ungag` stays unguarded so a stale gag set on a bot before this fix can still be removed. `cmd_gag` v0.11 -> v0.12; new `tests/unit/cmd_gag_bot_test.lua` (both smoke legs) provably fails pre-fix (a bot reaches add_user and is gagged) and passes patched. The right-click menu still appears on bots (it is client-side: UCMDs register with context CT2 = any user; the hub cannot hide it selectively - the server-side reject is the correct defence). The new unit test load-exposed a CI drift: the Windows smoke leg's unversioned `mingw-w64-ucrt-x86_64-lua` package had rolled to **Lua 5.5** (where generic-for control variables are `const`), while the hub bundles + runs **Lua 5.4.8**. Two follow-on fixes bundled here: (a) the Windows smoke job now installs `mingw-w64-ucrt-x86_64-lua54` (Lua 5.4.8, invoked as `lua5.4` - matching the Linux leg) so the unit tests validate against the hub's real runtime, not a newer language version; (b) `cmd_gag`'s `parse_duration` no longer reassigns its `gmatch` for-control variable (a fresh `local`), which is a minor cleanup that also stays valid under 5.5+. 3.2.x only, not backported.

- [#78](https://github.com/luadch-ng/luadch-ng/issues/78) follow-up - **`etc_blocklist_feeds`: `+reload` no longer re-pulls a feed fetched less than its interval ago.** `onStart` (which fires on every `+reload`, a full Lua restart) seeded each feed's `next_refresh = now + stagger` (3-9 s), and `next_refresh` was in-memory only - so a reload re-pulled every external feed within seconds, and an operator making several config edits + reloading each time could hammer a rate-limited provider (Spamhaus firewalls IPs fetching more than once per 12 h; AbuseIPDB has a daily quota) into an **IP ban**. The per-feed interval clamp only guarded the `onTimer` polling path, not the reload path. Fix: persist each feed's last-fetch epoch to `scripts/data/etc_blocklist_feeds.tbl` (written when a request is actually sent - so a 429/500/timeout throttles too, since it still hit the provider) and seed `onStart` from it: `next_refresh = min(last_fetch + interval, now + interval)`, or a staggered boot refresh only if the feed was never fetched or is overdue. The `min()` caps a bogus future timestamp (clock skew / corrupt file) to one interval so a feed can never freeze; a non-numeric / unreadable state file degrades to "refresh after boot". Feed entries already survive reload in the `core.blocklist` store (TTL 2x interval), so skipping the reload-refetch drops no protection. Mirrors `etc_geoip`'s persisted-update-state pattern. `etc_blocklist_feeds` v0.01 -> v0.02. `tests/unit/etc_blocklist_feeds_test.lua` gains reload-throttle coverage (reload-within-interval no re-pull, re-pull after interval, failed-fetch-also-throttles, sync-reject-does-not, bogus-future-timestamp cap) - the reload-no-re-pull regression provably fails pre-fix (§1a.7). Immediate-refresh workaround + a tracked `+blfeeds refresh` follow-up documented in `docs/BLOCKLIST.md`. 3.2.x only, not backported (feeds never shipped in 3.1.x).

- [#78](https://github.com/luadch-ng/luadch-ng/issues/78) follow-up - **boot-time runtime-directory self-heal + a first-boot boot-crash fix.** The hub writes into `log/`, `cfg/`, `certs/`, `scripts/data/` and `cfg/geoip/`, but nothing in the hub CODE created them - packaged deploys get them from the CMake install tree / Docker entrypoint, so a bare-metal or wiped-bind-mount install could be missing one. Two concrete failures: (a) `core/cfg_secret.lua`'s first-boot master.key generation did `io.open(master_key_path, "wb")` and, if the parent dir was missing (e.g. an operator relocating `master_key_path` to a not-yet-existing / absolute dir like `/secrets/master.key`), the `error()` on the failed open **hard-crashed the boot**; (b) `cfg/geoip/` (the in-hub GeoIP DB destination) was created by NOTHING, so the operator had to `mkdir` it by hand. Fix: new `core/ensuredirs.lua` runs at boot (from `core/init.lua`, before any core module inits) and `makedir`s the fixed default dirs - idempotent (mkdir -p + EEXIST-tolerant), so it is a silent no-op on a packaged install and net-new self-heal only for a wiped install + `cfg/geoip/`. `core/cfg_secret.lua` and `core/geoip_update.lua` additionally create the PARENT dir of their operator-configured path (master.key / the GeoIP `.mmdb`) right before writing, via the raw `makedir` primitive (not `util.makedir`, whose `safe_path` gate rejects the absolute paths an operator may legitimately configure). **Security hardening bundled because this change exposes it:** `hub/hub.c`'s `daemonize()` set `umask(0)`, so in `-d` daemon mode every file the hub creates was 0666 and every dir 0777 (world-readable/writable) - harmless when only the installer created the dirs, but the new self-heal would materialise a world-writable `cfg/` / `certs/` holding `user.tbl` / the TLS key / `master.key`. Changed to `umask(027)` (files 0640, dirs 0750; `master.key` / `serverkey.pem` stay chmod 600), which also closes the pre-existing world-readable-`user.tbl`-in-daemon-mode exposure. New `tests/unit/ensuredirs_test.lua` (creates every dir incl. `cfg/geoip`, degrades cleanly when the primitive is absent, per-dir failure isolation) + `tests/unit/cfg_secret_test.lua` (makedir-of-parent runs BEFORE the key write) + extended `geoip_update_test.lua` (dest-dir `makedir` + bare-filename no-op); both new tests wired on both smoke.yml legs. The cfg_secret + geoip regressions provably fail pre-fix (§1a.7). 3.2.x only, not backported.

- [#78](https://github.com/luadch-ng/luadch-ng/issues/78) follow-up - **in-hub GeoIP auto-update failed with `sha256 fetch HTTP 302`; `core/http_client.lua` now follows redirects (opt-in).** MaxMind's GeoLite2 download endpoint (`download.maxmind.com/geoip/databases/<edition>/download`) responds to an *authenticated* request with a **302** to a pre-signed Cloudflare-R2 URL on a **different host** - and `http_client` treated any non-2xx as a failure, so `core/geoip_update.lua`'s first step (the `.sha256` sidecar fetch) aborted before any download. An unauthenticated probe returns 401 (no redirect), which masked it; the 302 only appears once the Basic credential authenticates. Fix: new opt-in `max_redirects` request field (default `0` = do NOT follow, so all existing callers are byte-identical). When set, both the RAM and `download_to_file` paths follow up to N `3xx` responses that carry a `Location`, capped at a hard `MAX_REDIRECT_CEIL = 10`, sharing ONE `req.timeout` across the whole chain. Security invariants on every hop: the `Authorization` / `Cookie` / `Proxy-Authorization` headers are **dropped when the redirect crosses to a different origin** (scheme/host/port) so the MaxMind credential is never sent to the R2 host; an `https -> http` **downgrade is refused**; the target is re-validated by `parse_url` (http/https only, no control bytes, no embedded credentials); redirect error strings name only the host, never the signature-bearing target URL. `core/geoip_update.lua` passes `max_redirects = 5` on both its sidecar + download requests. `tests/unit/http_client_test.lua` gains 50 checks (resolve/strip/prepare helpers, cross-origin auth-strip, downgrade refusal, hop cap, RAM + download 302->200 integration, and a Finding-1 guard that an intermediate 3xx whose headers exceed a small `max_response` is still read up to `MAX_HEADER_SIZE`); the 302->200 behaviour is provably red on the pre-fix client (`on_complete` saw status 302, not 200) and green patched (§1a.7). Verified against the live MaxMind endpoint (302 -> R2 signed URL -> 200 + valid sha256). `docs/SECURITY.md` outbound-HTTPS section documents the widened trust boundary. 3.2.x only, not backported (in-hub GeoIP auto-update never shipped in 3.1.x).

- **Fresh / partially-seeded install: `cfg.tbl` + `user.tbl` self-heal, and the fallback default cfg no longer references an unbundled plugin.** Two first-boot issues that surface on a wiped bind-mount. (1) The container entrypoint only seeded `cfg/` from `/defaults` when the directory was *entirely* empty - but the hub writes operator-facing artifacts INTO `cfg/` (`cfg/geoip/` for the GeoIP DB, `cfg/blocklist-export-*.jsonl`), so a `cfg/` that had lost its `cfg.tbl` / `user.tbl` but still held one of those stayed non-empty, the seed never re-fired, and the hub booted on the in-memory default with no `cfg.tbl` on disk to edit and no `user.tbl` to log in with. `docker/entrypoint.sh` now additionally restores a *missing* `cfg.tbl` / `user.tbl` from `/defaults` per-file (add-only, existing files never overwritten - same rule as the bundled-lang autosync). (2) `core/cfg_defaults.lua`'s built-in fallback `scripts` list still named `cmd_pm2offliners.lua`, which was migrated to the companion `luadch-ng/scripts` repo (commit `7772bda` removed it from `scripts/` + `examples/cfg/cfg.tbl` but missed the fallback default) - so the no-`cfg.tbl` boot path logged a `checkfile: No such file` error trying to load it. Removed from the default list (the plugin's cfg keys / validators stay for operators who install it from the companion repo). New CI guard `tests/check_default_cfg_scripts.py` (both smoke legs) fails if the fallback `scripts` default ever again names a plugin that isn't bundled - falsifiable: it FAILS on the pre-fix list. 3.2.x only.

- [#325](https://github.com/luadch-ng/luadch-ng/issues/325) - **Plugin sandbox missing `os.clock`** in the curated `_os_safe` shim (`core/scripts.lua`). The #206 Tier-2 Sub-PR-2 audit narrowed `os` to `time` / `date` / `difftime` based on the then-bundled 66-plugin sweep, which did not see `os.clock`. The companion `luadch-ng/scripts` plugin `ptx_freshstuff` (and any 3rd-party plugin that renders stopwatch-style output, e.g. `+reloadrel` / `+delcrap` / `+prune`) crashed at onStart with `attempt to call a nil value (field 'clock')`. Fix: add `clock = os.clock` to `_os_safe`. `os.clock` is the same trust class as `os.time` / `os.difftime` - read-only CPU-seconds accessor, no filesystem / process / env side effects - so it belongs in the time-accessor family already exposed by the shim. Surrounding comment block updated to list `clock` alongside the existing triad. New smoke check `test_sandbox_os_clock_whitelisted` static-greps `core/scripts.lua` for the literal `clock = os.clock` entry inside the `_os_safe` block; falsifiable - the assertion FAILS on the unpatched code. 3.2.x only.

- (Sopor) - **Switch unrecoverable ISTA-kicks from `TL10` / `TL300` to `TL-1`** at 9 sites across 6 files. ADC `TL<seconds>` is the client-facing reconnect-delay hint: a positive value tells the client "wait N seconds and try again", `TL-1` says "do NOT auto-reconnect, this won't fix itself". Six kick sites were sending positive `TL` for failures the user cannot resolve by waiting - IP-mismatch (`core/hub_dispatch.lua:469`), forbidden post-login INF flags (`scripts/hub_inf_manager.lua` 3 sites - kill / strip already handled by #222, this fixes the survivor branches), invalid nick length (`scripts/usr_nick_length.lua`), hub-count violation (`scripts/usr_hubs.lua`), slot-count violation (`scripts/usr_slots.lua`), and share-size violation (`scripts/usr_share.lua` 2 sites). All require a client-side config change (different nick, fewer hubs / slots, larger share, client restart for new IP) - retrying after 10s or 5 min just produces a reconnect-loop, which on the operator side surfaces as N FAILED-AUTH reports in OpChat per user-session instead of 1. The OpChat report (security signal) itself stays intact; only the spam volume drops. Plugin bumps: `hub_inf_manager` v0.07 -> v0.08, `usr_nick_length` v0.02 -> v0.03, `usr_hubs` v0.13 -> v0.14, `usr_slots` v0.08 -> v0.09, `usr_share` v0.12 -> v0.13. Same operator-facing argument that landed for `ptx_tagcheck` in the companion `luadch-ng/scripts` repo last week: if the user can't fix it by waiting, don't tell their client to retry. Pattern-sweep over the remaining `TL[0-9]+` sites kept `cmd_disconnect TL30` (deliberate short-timeout operator action), `cmd_upgrade TL300` (level cache refresh on reconnect), `usr_hide_share TL300` (SS-flag refresh on reconnect), and `usr_nick_prefix TL300` (transient nick-collision retry) - those ARE recoverable via reconnect. 3.2.x only, not backported (operator-friendly fix, no security risk in either direction; 3.1.x users can pull the affected files manually if they want the cleanup before the 3.2.x release).

- [#320](https://github.com/luadch-ng/luadch-ng/issues/320) (Kcchouette) - **`cmd_ban` offline-by-nick path silently bypassed the hierarchy check** (`cmd_ban_permission[level] < target:level()`) that fires on the online path. A lower-level operator could ban a higher-level offline registered user (incl. the hubowner-level account, which would lock them out on their next login attempt). Root cause: the online branch resolves `target` to a user OBJECT (has `:level()` method), the offline branch resolves it to a profile TABLE from `regnicks[]` (has `.level` field), and the existing check site at `cmd_ban.lua:971` was only reachable from the online branch - the offline branch returned at `addban()` two lines earlier. Fix: explicit hierarchy guard inside the offline-by-nick branch using `target.level`, mirroring the online check semantics with the table-shape accessor. cid / ip offline branches keep no profile lookup and stay unchecked by design (no hierarchy info available). New smoke test `test_320_offline_ban_hierarchy` registers an operator (level 60) + a higher-level offline target (level 100) via the HTTP API, has the operator broadcast `+ban nick smoke_320_target ...`, and asserts no ban entry was persisted; falsifiable - the assertion FAILS on the unpatched code. cmd_ban plugin bumped to v0.42 (master had `scriptversion = "0.41"` while the in-source changelog header stopped at v0.39 - this PR resyncs both). Pattern-sweep across `cmd_gag` / `cmd_setpass` / `cmd_upgrade` / `cmd_delreg` / `cmd_nickchange` / `cmd_redirect` / `cmd_disconnect` / `etc_trafficmanager` / `etc_msgmanager` for the same online-vs-offline-hierarchy-check divergence came back clean - either properly guarded on both paths (6 plugins) or online-only by design (3 plugins). 3.2.x only.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-5 - **de+en quality pass** across all lang files (closes the i18n content cleanup). Systematic audit of all 59 plugin lang-file pairs + core lang.tbl turned up 24 confirmed fixes spanning typos, subject-verb errors, and mixed-language drift, including the partial-English `usr_hide_share.lang.de msg_unhide_user` called out in the tracker. **en fixes (10):** `cmd_disconnect.report_msg` "were disconnected" → "was disconnected"; `cmd_reg.msg_error` "occured" → "occurred"; `cmd_reg.msg_length` "lenght" → "length"; `cmd_upgrade.msg_same` "still have" → "already has"; `etc_dhtblocker.report_msg` "were banned" → "was banned"; `etc_trafficmanager` 3 instances "blockes"/"her blockmodes" → "blocked"/"their blockmodes"; `usr_hubs.msg_max` "You was disconnected" → "You were disconnected"; `cmd_delreg.msg_error` "occured" → "occurred"; `cmd_mass.msg_lvl_exists` "does not exists" → "does not exist"; core `hub_hbri_succeed` "Validation succeed" → "Validation succeeded" (synced fallback in `core/hub.lua` + `core/hbri.lua` defaults). **de fixes (14):** `bot_pm2ops.msg_send` "wure" → "wurde"; `cmd_ban.msg_ban_attempt` grammar ("niedrigerem"/"mit der Grund" → "niedrigeren"/"mit dem Grund"); `cmd_disconnect.msg_usage` `<REASON>` → `<GRUND>`; `cmd_redirect` "standart" → "Standard"; `cmd_reload.help_desc` "Läd" → "Lädt"; `cmd_userinfo.help_desc` "oder dir" → "oder dich" (accusative); `etc_cmdlog.msg_usage` "Usage:" → "Gebrauch:"; `etc_records` 2 instances "Rekrode" → "Rekorde"; `cmd_mass.msg_allowed_levels` "allowed levels" → "erlaubte Level" (post-PR-3 deferred wording); `cmd_mass.msg_out_lvl/_hub` "Message:" → "Nachricht:"; `cmd_usercleaner.msg_exceptions_level` "and kann" → "und kann"; `cmd_shutdown.msg_hub_disabled` "faehrt" → "fährt"; `cmd_accinfo` "gebannt::" double-colon → "gebannt:"; `usr_hide_share.msg_default` "dieses User" → "dieses Users" (genitive); `usr_hide_share.msg_unhide_user/_target` partial-English fixed. Six UNCERTAIN findings deferred (cmd_topic/hub_inf_manager Topic/Flag gender, hub_runtime "resette", cmd_upgrade idiom, cmd_accinfo msg_seconds suffix, cmd_ascii deliberately-different keysets) - all stylistic, none load-bearing; Weblate translators or follow-up can refine. No format-string `%s/%d` count mismatches found across the sweep. Reviewer-caught **source-fallback sync** (`lang.X or "<literal>"` defaults that ship when the lang file fails to load): also synced in `cmd_delreg.lua` / `cmd_reg.lua` (msg_error, msg_length) / `cmd_mass.lua` (msg_lvl_exists) / `etc_trafficmanager.lua` (help_desc + [[...]] usage block) / `usr_hubs.lua` (msg_max [[...]] block) — without this, an operator with a broken lang file would still see the typos. 3.2.x only, not backported.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-4 - **NO-LANG-NEEDS plugins** brought into the i18n surface. Three plugins emitted user-facing English literals but had no lang file at all: `etc_hubcommands` v0.06 (3 operator-facing chat hints - the `[command] %s` echo prepended to every command typed, the "Did you mean +X?" forgot-prefix hint upstream `luadch/luadch#223`, and the literal-bracket hint from `luadch-ng/luadch-ng#137`); `etc_unknown_command` v0.04 (the "Unknown command. Check leading spaces and syntax." denial); `hub_bot_cleaner` v0.5 (the `[ BOT CLEANER ]--> deleted unused bot:  %s` ops-report broadcast). 6 new lang-file pairs created (`scripts/lang/<plugin>.lang.{de,en}`); plugin sources gained the standard `cfg.loadlanguage` + per-key `local msg_X = lang.msg_X or "<literal>"` pattern. The literal-bracket hint deliberately never echoes input args (passwords may pass through `[+!#]reg <user> <pw>`), preserved post-i18n. Per the don't-germanize guardrail, DC jargon (Hub, Main-Chat, command, prefix) stays English on the de side. 3.2.x only, not backported.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-3 - **plugin MIXED stragglers** routed through lang. Seven plugins had user-facing English literals injected into otherwise-translated message templates, so a German hub got mixed-language output. Fixed: `etc_msgmanager` v0.10 (block-mode "main" / "pm" / "main + pm" in reply + report); `cmd_accinfo` v0.35 ("Main" / "PM" / "Main + PM" mode display); `cmd_gag` v0.11 (inline duration unit labels "y "/"d "/"h "/"m "/"s"); `cmd_usercleaner` v0.8 (showall / showexpired / showghosts / showexceptions status-column "true" / "false" booleans); `cmd_hubinfo` v0.30 ("        Level:  " row label in get_levels); `cmd_mass` v0.20 ("  \|  allowed levels: " help-desc appender); `etc_userlogininfo` v0.20 (hardcoded "<unknown>" / "<UNKNOWN>" fallbacks unified through the existing `msg_unknown` lang.X). New lang keys: 3 (`msg_mode_main/pm/both`) + 3 + 5 (`msg_unit_*`) + 2 (`msg_yes/no`) + 1 + 1 = 15. Per the don't-germanize guardrail (`[[i18n-terminology-dont-germanize]]`), DC jargon (Main / PM / Mainchat) stays English in both de + en - the strings are byte-identical de = en for now, deferred to PR-5 (quality pass) for any wording adjustment. No new test file: visual review covers the mechanical string moves; the broader plugin-lang-key sweep planned for follow-up to PR-2's usr_share-only check will catch regressions. 3.2.x only, not backported.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-2 - `usr_share` dead German translation. The plugin read `lang.msg_minmax` but the lang files define the key as `msg_sharelimits` (introduced in v0.06's new output-msg refactor), so the lookup always returned nil and the `or` fallback to the hardcoded English literal fired on every msg - the de translation was unreachable since v0.06. One-character fix (rename the lookup to `lang.msg_sharelimits`); plugin bumped to v0.12. New `tests/unit/usr_share_lang_test.lua` scans the plugin source for every `lang.X` reference and asserts X is defined in BOTH `.lang.de` + `.lang.en` - generic enough that the same pattern can be lifted into a sweeping check across all plugins (deferred). Provably fails on the pre-fix source (2/5 checks fail with `de/en[msg_minmax]` missing). Wired into both Linux and Windows CI. 3.2.x only, not backported.

- [#301](https://github.com/luadch-ng/luadch-ng/issues/301) PR-1 - **core i18n cleanup** + load-bearing refactor. 13 hardcoded English user-facing strings moved into the language files (`examples/lang/{de,en}.tbl`): the 5 HBRI ISTA reasons (`#214` debt), the 2 ZLIF reject reasons, the `[TLS: ...]` login-banner label, and the 5 `insertreguser` failure strings that leak to clients via ISTA 220. A German hub now actually shows a German message for these paths (previously they were always English regardless of `cfg.language`). Operator-visible bug: the `[TLS: ...]` literal was baked into the otherwise-translated `hub_login_message` so the de banner read e.g. "Auf diesem Server läuft Luadch-NG v3.2.0-dev [TLS: 1.2] (Uptime: ...)" with the English label in the middle - now `hub_login_tls_label` is its own key. Load-bearing refactor: the 14 individual `local _i18n_*` slots in `core/hub.lua` (mirrored as 11 in `core/hub_dispatch.lua` + 1 in `core/hub_bot_object.lua`) collapsed into a single `local _i18n = { }` per file, deps-passed as one entry instead of 11+1 + verbose deps-key lists - we'd otherwise have hit Lua's 200-locals cap in `core/hub.lua` (see `[[reference-hub-lua-local-limit]]`); empirically reproduced on the first build attempt. `core/hbri.lua` gets a new `set_i18n(strs)` export called from `loadlanguage`, so a `+reload` after editing the lang file rebuilds the HBRI reason strings in place. Falsifiable `tests/unit/lang_test.lua` (110 checks: required-key coverage + de/en parity + `login_tls_label` exactly-one-%s); wired into both Linux and Windows CI; provably fails on master (28/110). DC/ADC-protocol jargon stays English per `[[i18n-terminology-dont-germanize]]` (Hub, Slot, OP, Nick, CID, TLS, ZLIF / ZOF / HSUP, ...). 3.2.x only, not backported.

- [#279](https://github.com/luadch-ng/luadch-ng/issues/279) follow-up - `etc_regserver_announce` no longer logs a spurious `checkfile` error to `error.log` on first start. `util.loadtable` calls `checkfile` which logs when the file is missing; the announcer's state file does not exist until the first confirmed registration, so a fresh hub surfaced `util.lua: function 'checkfile': error in scripts/data/etc_regserver_announce.tbl: No such file or directory` - relayed to ops by the HubSecurity bot. Fix: probe with `io.open` before `util.loadtable` (a missing state file is the normal first-run case, loaded silently). `etc_regserver_announce` v0.03. 3.2.x only.

- [#275](https://github.com/luadch-ng/luadch-ng/issues/275) - holistic HTTP API review follow-up, Security PR. Five fixes bundled because they all touch the audit-log / cache-key / cfg-classification surface that a generic audit-redaction helper unifies: (1) **SEC-1** PUT `/v1/registered/{nick}/password` + POST `/v1/registered` no longer leak the cleartext password into `api_audit.log`; new per-route `audit_redact_body = true` meta flag on `hub.http_register(...)` makes the router log `body=[redacted]` for those routes. Diagnostics still get method + path + token + status + idem-key + request-id. (2) **SEC-2** POST `/v1/bans` now strips control bytes from the `target` field before `addban()` persists to disk + the `report.send` ops broadcast - other fields (reason / actor_label) were already stripped, `target` was a Phase-2 oversight. (3) **SEC-3** unauthenticated requests (401 / 404 / 405 / 429-prefix) now log `body=[skipped]` instead of the verbatim attacker-chosen bytes - no more insider-exfil channel via `/v1/log/api`. (4) **SEC-5** idempotency cache key extended from `(token, idem-key)` to `(token, method, path, idem-key)` so a client that reuses an `X-Idempotency-Key` across two unrelated write endpoints does not get the first action's cached response replayed for the second. (5) **SEC-6** `http_api_rate_*` / `http_api_burst` / `http_api_authfail_prefix_*` cfg keys classified as `reload_required` in PUT `/v1/config/{key}` response; ratelimit caches these at init time so the prior `live` classification was misleading. Plus SEC-N1 defensive `%c` strip on the failed-auth token-prefix label, SEC-N2 SECURITY.md sentence on the total-trust admin-token posture. Smoke regression `test_http_security_holistic_review` provably fails on the unpatched code (sentinel password lands in audit). Spec §6.2 + §6.8 + SECURITY.md §2 updated. `cmd_ban` v0.41, `cmd_setpass` v0.24, `cmd_reg` v0.35. 3.2.x only, not backported (API never shipped in 3.1.x).
- [#266](https://github.com/luadch-ng/luadch-ng/issues/266) - plugin-callable `util` I/O functions (`checkfile`, `loadtable`, `savetable`, `savearray`, `maketable`, `atomic_write`) captured the unsandboxed `io.open` at module load and bypassed the `_io_safe` path gate added in [#213](https://github.com/luadch-ng/luadch-ng/issues/213). A malicious plugin could call `util.checkfile("/etc/passwd")` to read absolute paths or `util.atomic_write("/tmp/backdoor", ...)` to write outside the hub's working directory. Reporter framed this as "Critical Sandbox Escape"; that's overstated - plugins are documented as trusted by design (`docs/SECURITY.md` §2), the attack requires filesystem write access to `scripts/` + `cfg/` which already implies RCE, and the fix does NOT close the legitimate `_io_safe.open("cfg/master.key", "r")` read (relative path, no traversal, passes the gate). Fix is incremental hardening matching the #206 / #213 trajectory: new `util.safe_path` helper exported as single source of truth; `checkfile` / `atomic_write` / `maketable` apply it before calling `io.open`; `scripts.lua`'s `_io_safe.open` shim is refactored to delegate to `util.safe_path` (DRY). Core callers (cfg.lua / cfg_users.lua / cfg_lang.lua) use only relative paths anchored at `CONFIG_PATH = "././cfg/"`, so applying the guard unconditionally does not break any core code path. New unit test `tests/unit/util_safe_path_test.lua` covers the helper + the application sites with stubbed `io.open`; the test errors out pre-fix because `util.safe_path` does not exist, and passes post-fix (§1a.7). `docs/SECURITY.md` §2 rewritten to reflect the post-#206 / #213 / #266 defence-in-depth posture. 3.2.x only, not backported.
- [#265](https://github.com/luadch-ng/luadch-ng/issues/265) - `_regex.nowhitespace` (INF.NI validator) no longer rejected ADC escape sequences `\s` / `\n` after the Phase 7d rewrite (`6d0d83a`) - the 7d swap added raw `%c` coverage but lost the original escape check. Per ADC §2.4 a spec-compliant receiver decodes these back to space / LF in the nick, violating NI's no-whitespace contract. Reporter framed this as "Critical Protocol Command Injection"; that's overstated - spec-compliant clients split commands on raw 0x0A BEFORE escape decoding, so an embedded `\n` cannot cross a command boundary. Real impact is field-content spec-violation (UI / log-line confusion). Fix scoped to `nowhitespace` only - `default` stays as-is because DE / MS / EM / AP legitimately carry `\s` / `\n` escapes. Smoke regression scans event.log for the parser's `invalid named parameter` line; fails on unpatched code, passes with the validator update. 3.2.x only, not backported.
- [#257](https://github.com/luadch-ng/luadch-ng/issues/257) - latent two-bug pair in `etc_trafficmanager.lua` `add()` / `del()` exports. (a) Dispatch check `( not scriptname ) or ( not scriptname == 1 )` Lua-parses as `(not X) == 1` and is always false, collapsing the condition to `not scriptname` - the documented external-string code path was unreachable. (b) Inside the `add()` external path, `block_tbl[firstnick][1] = user:nick()` would crash with `attempt to index a nil value (local 'user')` because the external path is invoked with `user=nil`. Both bugs are dead code on master - the only existing external caller (`cmd_usercleaner`'s `block.del(nick)`) passes `scriptname=nil` which trips the first dispatch clause and reaches `del()`'s correctly-written external branch. Fix: (a) `( not scriptname ) or ( scriptname ~= 1 )` in both functions; (b) use `scriptname` as the "by" field in `add()` external (matches `del()`'s pattern + the show-blocks display). Plugin bumped to v2.5. **§1a.7 deviation:** no regression test added - the dispatch bug is provable from Lua operator precedence, no production caller passes string scriptname, and adding a fake-caller smoke harness would be disproportionate infrastructure for a fix-by-inspection. 3.2.x only, not backported.
- [#243](https://github.com/luadch-ng/luadch-ng/issues/243) - `cmd_upgrade` ADC `+upgrade nick <NICK> <LEVEL>` path crashed with `attempt to concatenate a nil value (local 'prefix')` when `usr_nick_prefix_prefix_table` had no entry for the target user's level (operator cfg drift, partial wipe, ad-hoc level absent from the prefix table). The handler indexed `prefix_table[level]` and concatenated the result without routing through `hub.escapeto` (which `luaL_optstring`-defaults nil to `""` and would have masked the crash). Fix: `if prefix_activate and prefix_table then ... prefix_table[level] or ""`. Family-wide consistency sweep applies the same defensive pattern to cmd_setpass / cmd_nickchange / cmd_delreg (these route through `hub.escapeto` and therefore did not actually crash, but get the explicit guard for parity + future-wrapper robustness). Smoke regression: `_switch_to_partial_prefix_table_mode` rewrites cfg.tbl to remove the `[ 30 ] = "[VIP]"` entry (smoke_pr1_a's post-PR-5 level), then ADC `+upgrade nick smoke_pr1_a 40` and assert `error.log` shows no nil-concat / nil-index entry (test FAILS pre-fix per §1a.7). `cmd_setpass` v0.23, `cmd_nickchange` v2.1, `cmd_upgrade` v0.23, `cmd_delreg` v0.32. 3.2.x only, not backported.
- [#239](https://github.com/luadch-ng/luadch-ng/issues/239) - `cmd_ban` exported `bans` table went stale across `+ban clear`. The plugin's `cleanbans()` did `bans = {}` (rebind the local) instead of mutating in place; the table reference exported via `return { bans = bans, ... }` (captured at module-load time) then pointed at the orphaned old table. Importers like `cmd_accinfo`'s file-scope `local bans_tbl = ban.bans` kept surfacing the no-longer-active ban entries through both `+accinfoop` and the HTTP `GET /v1/registered/{nick}` ban field. Fix: `cleanbans()` now uses `for k in pairs(bans) do bans[k]=nil end` to clear in place; the export reference stays live. All other ban-table mutations (`add` / `del` / HTTP DELETE-by-index) already used `table.insert` / `table.remove` / `bans[k] = ...` in place, so this is the only call site that needed the change. Smoke regression added: POST a ban via HTTP, fire `+ban clear` via dummy (level 100) ADC connection, assert subsequent GET `/v1/registered/{nick}` returns `ban: null` (test FAILS pre-fix per §1a.7). `cmd_ban` v0.38. 3.2.x only, not backported.
- [#207](https://github.com/luadch-ng/luadch-ng/issues/207) - TLS-handshake CPU-exhaustion hardening: (a) reduced the stuck-handshake retry loop in `core/server.lua` `wrapconnection()` from 20 to 10 iterations - the loop only ever iterates on the SSL wantread/wantwrite I/O dance, not new ECDH work (which happens once on the first flight); 10 yields are plenty for TLS 1.3, the configured default protocol. (b) Extracted `ratelimit_expired_handshakes()` from the 120s `_checkinterval` sweep into a dedicated faster sweep gated by a new cfg key `ratelimit_handshake_sweep_interval` (default 10s). Worst-case lifetime of a stuck handshake's handler + coroutine drops from `handshake_timeout + 120s` (~130s under defaults) to `handshake_timeout + 10s` (~20s under defaults). The per-IP rate-limit aspect of the original issue is already covered by the existing `ratelimit_perip_conn_rate` / `_burst` / `_max_conns` (defaults 10/s, burst 30, max 16 parallel) which fires on TCP-accept - which IS the TLS-handshake-start point - so no new code path was added for per-IP rate enforcement. Documented in code comments. 3.2.x only, not backported.
- [#208](https://github.com/luadch-ng/luadch-ng/issues/208) - integer overflow in `adclib.gen_self_signed_cert()` on any platform where `long` is 32-bit. The input bound of `36500` days (~100 years) combined with `long days * 86400` overflowed (LONG_MAX = 2,147,483,647; `36500 * 86400 = 3,153,600,000`), producing a negative-seconds value that `X509_gmtime_adj` interpreted as "seconds before the epoch" - the generated cert's `notAfter` landed in ~1933 and TLS clients got `certificate has expired`. Affected platforms: 32-bit ILP32 (i686 / ARMv7 / MIPS32) AND Windows x64 LLP64 (MinGW UCRT64, MSVC; `long` is 32-bit by Windows ABI regardless of process width). Unaffected: Linux LP64 (x86_64, aarch64). Fix: lower the input bound from 36500 to **24855** days (~68 years = `LONG_MAX / 86400` on a 32-bit long), making the arithmetic safe on every supported platform without platform-conditional code. The new ceiling is well above any realistic deployment; `cert_bootstrap.lua` uses 3650 days = 10 years by default and was always in-bounds. The issue only surfaced if an operator or sandboxed Lua passed `days > 24855` on a long-is-32-bit host. 3.2.x only, not backported.
- [#161](https://github.com/luadch-ng/luadch-ng/issues/161) - BINF without `I4` / `I6` fields was rejected with `ISTA 220 No CID/PID/NICK/IP found in your INF.` Per ADC 4.3.x the `I4` / `I6` fields are *conditionally* required (only when the client advertises TCP4 / UDP4 / TCP6 / UDP6 in `SU`); hublist pingers and any IP-agnostic probe legitimately omit them. The hub now treats a missing `I4` / `I6` like the spec-defined `0.0.0.0` placeholder - fills in the TCP-source IP under the connection's address family, no special-case "no-IP user" shape downstream. `kill_wrong_ips` spoof-detection is unchanged for actually-mismatched claims. Mirrors upstream [luadch/luadch#176](https://github.com/luadch/luadch/issues/176). Backported to `release/3.1.x` as v3.1.9.
- [#162](https://github.com/luadch-ng/luadch-ng/issues/162) - ADC PING HSUP handler errored out (sandbox-undeclared `pairs`) on public (`reg_only = false`) hubs, returning zero frames to the pinger. Hublist scrapers timed out and dropped the hub from listings. Regression introduced by T1.3 of [#147](https://github.com/luadch-ng/luadch-ng/issues/147) in v3.1.8. Backported to `release/3.1.x` as v3.1.9 (self-introduced functional regression breaking the public-hub deployment mode - judgement call outside CLAUDE.md §8 table's listed categories).
- Latent crash in `core/server.lua` `changesettings()`: `tonumber()` was called seven times without `local tonumber = use "tonumber"` import. Function is currently dead code (no caller in hub or plugins) so no production impact; surfaced by the #162 sandbox-locals audit. Fix is a one-line `use` declaration alongside the existing locals. Backported to v3.1.9 alongside #162.
- [#159](https://github.com/luadch-ng/luadch-ng/issues/159) follow-up (Sopor / Boro) - the v3.1.9 `linux-aarch64` artifact was unusable on Bullseye-based Pi systems (DietPi v9.x, glibc 2.31) AND on fresh Pi OS Bookworm (glibc 2.36) because it was built on `ubuntu-24.04-arm` (glibc 2.39) and inherited `GLIBC_2.34` / `GLIBC_2.38` symbol requirements. The aarch64 build now runs inside a Debian Bullseye container on the same native-arm runner, with OpenSSL 3.x built from source and bundled (`libssl.so.3` + `libcrypto.so.3` alongside the binary, same pattern as the Windows `libssl-3-x64.dll`). `rpath` is patched to `$ORIGIN` on the main binary and `$ORIGIN/../../..` on `luasec/ssl.so` so the bundled libs resolve without depending on the system OpenSSL version. The workflow has an explicit `objdump -T | grep GLIBC` step that fails the build if any required symbol exceeds GLIBC_2.31 (Bullseye baseline) - catches a future regression where someone bumps the container away from Bullseye without thinking. Backport candidate for v3.1.10.
- Phase 8a F-INF-1f cosmetic fix in `scripts/etc_userlogininfo.lua`: the `or "<unknown>"` fallback on a nil `user_version` never fired because `hub.escapefrom(nil)` returns `""` and `"" or "<unknown>"` evaluates to `""` in Lua (`""` is truthy). Operators saw an empty client-version field in the login-info message instead of `<unknown>`. Fix: explicit `nil`-check before calling `hub.escapefrom`. Deferred from Phase 8a closeout. 3.2.x only, not backported.
- [#219](https://github.com/luadch-ng/luadch-ng/issues/219) - Phase 8a F-INF-2: per-field integer clamps on the user accessors `user:share()` / `user:files()` / `user:slots()` / `user:hubs()` in `core/hub_user_object.lua`. Caps: `SS=[0, 2^53]`, `SF=[0, 2^32]`, `SL`/`HN`/`HR`/`HO=[0, 2^16]`; negatives floor to 0. Defends hub-stat aggregates, PING reply totals (#147 T1.3/T1.4), and HTTP API JSON output against a client claiming `SS=-1` or `SS=10^18` (would otherwise pollute `etc_records` persistence permanently). Phase 7d (#65) parser contract preserved: parser still accepts negative integers (DC++ `DS-1` sentinel), stored `_inf` untouched, only accessor reads normalised. See `docs/PLUGIN_API.md` user accessor section for the clamp contract. 3.2.x only, not backported.
- [#214](https://github.com/luadch-ng/luadch-ng/issues/214) Gap 2 - DDoS-amplification hardening on the `kill_wrong_ips = false` opt-out path in `core/hub_dispatch.lua`. The NAT-weird-deployment opt-out preserves the user's connection on a primary-IP claim mismatch (default `kill_wrong_ips = true` kills) but pre-fix the wrong claim STAYED in the BINF and was broadcast to other clients - they would then direct CTM / RCM frames at the spoofed address (Maksis-confirmed DDoS-amplification vector, see [Wikipedia DC++ DDoS history](https://en.wikipedia.org/wiki/Direct_Connect_(protocol)#Direct_Connect_used_for_DDoS_attacks)). Fix: the mismatch + opt-out branch now stamps the verified `userip` over the lie via `adccmd:setnp( userfam, userip )`, so the broadcast INF carries the authenticated TCP-source IP. Opt-out intent (don't kill the user) preserved AND legitimate NAT-deployments now broadcast a routable IP (strictly better UX than pre-fix). Default `kill_wrong_ips = true` deployments unaffected. The remaining vector (Gap 1: secondary-family unverified broadcast) is closed by the upcoming HBRI implementation. **Backport candidate for v3.1.10** (DDoS-amplification fix; the opt-out users are precisely the NAT-weird operators most likely to enable it without realising the broadcast hole).
- [#214](https://github.com/luadch-ng/luadch-ng/issues/214) Gap 1 (PR-A, [#283](https://github.com/luadch-ng/luadch-ng/pull/283)) - **strip the unverified secondary IP family before broadcast.** Since #147 T3.1 a dual-stack BINF may carry both `I4` and `I6`; the hub authenticates only the family matching the TCP source and pre-fix forwarded the OTHER family as-sent - a DDoS-amplification hole (a client could advertise an arbitrary victim as its secondary and have other clients aim CTM / RCM at it). Fix: `strip_unverified_secondary` in `core/hub_dispatch.lua`'s BINF handler deletes the secondary family's `I4` / `I6` + `U4` / `U6` + `TCP*` / `UDP*` SU flags before the INF is stored or broadcast, so only the authenticated primary reaches the wire. Real HBRI (PR-B) re-adds a validated secondary. Falsifiable smoke `test_binf_secondary_family_stripped` (fails on the pre-fix code, which broadcast the claimed I6). 3.2.x only.
- [#298](https://github.com/luadch-ng/luadch-ng/issues/298) - **regression fix: HBRI was disabled on TLS-only / autossl hubs.** The #294 Tier-1 change ([#295](https://github.com/luadch-ng/luadch-ng/pull/295)) restricted the side-channel port to a PLAIN listener, on the (wrong) assumption that the side-channel must be unencrypted. On a TLS-only hub (the common `adcs` deployment - e.g. a single `autossl` port on both families) `hbri.active()` then never saw a port, so the hub stopped advertising `ADHBRI` in its ISUP. AirDC++ only advertises a secondary family when the hub advertises HBRI, so dual-stack clients connected single-stack (flipping between v4 and v6 on reconnect) and HBRI never fired - even though it had worked over the same autossl port before #295. The validation socket rides the normal accept path, so it uses the advertised port's transport; a client connecting back to a TLS / autossl port does TLS on the side-channel too (matching its main connection), and the HTCP is read after the handshake - proven by the real-client roundtrip over an autossl port. Fix: restore the `tcp_ports[1] or ssl_ports[1]` fallback (plain preferred, TLS / autossl otherwise); HBRI is active when a listener of EITHER transport exists on both families, disabled only when a family has no listener at all. Comments + `docs/SECURITY.md` + `examples/cfg/cfg.tbl` + the `hbri_test.lua` wording corrected from "plain listener" to "listener (plain or TLS)". The audit's "TLS-port trap" was a shared wrong assumption; the empirical real-client evidence corrected it (CLAUDE.md §1a.4 / §1a.5). 3.2.x only.
- [#294](https://github.com/luadch-ng/luadch-ng/issues/294) - HBRI holistic-audit follow-ups (after the #214 / #291 / #286 arc). A final 3-reviewer audit (security / correctness / consistency) found no exploitable or correctness defect; the amplification invariant holds across all three paths. Tier-1 ([#295](https://github.com/luadch-ng/luadch-ng/pull/295)) attempted to disable HBRI on a family lacking a plain listener - this turned out to be a regression on TLS-only / autossl hubs and was reverted by #298 (see above; the side-channel works over a TLS port). New `tests/unit/hbri_test.lua` locks the `active()` / `eligible()` gate (a family with no listener -> inactive), wired into both CI lua steps. Tier-2 ([#296](https://github.com/luadch-ng/luadch-ng/pull/296)): falsifiable smoke for the wrong-family side-channel reject, the v6-main/v4-secondary direction, post-login disconnect cleanup, and post-login concrete mismatch. 3.2.x only.
- [#222](https://github.com/luadch-ng/luadch-ng/issues/222) (HadesDCH) - post-login INF updates carrying `I4` / `I6` are now silent-stripped from `scripts/hub_inf_manager.lua` instead of killing the user with `ISTA 240` + `TL300`. Real DC++ clients refresh INF (incl. `I4`) on routine triggers (NAT rebind, ISP-IP change, plain refresh); the existing kill-on-flag logic disconnected those legitimate users, producing the FAILED-AUTH log spam HadesDCH reported. The `flags_on_inf` table split into `flags_on_inf_kill` (`PD` / `ID`, identity spoofing = kill) and `flags_on_inf_strip` (`I4` / `I6`, IP mutation attempt or routine refresh = silent strip). Anti-spoofing (#97) intent preserved: stored `_inf` IP fields are NEVER mutated, the stripped fields never reach the broadcast - other clients keep seeing the original verified IP. Other INF fields in the same post-login update (e.g. `DE` description change, `SS` share size update) still get applied. Plugin bumped to v0.07. **Backport candidate for v3.1.10**.
- [#160](https://github.com/luadch-ng/luadch-ng/issues/160) (Sopor) - defense-in-depth for `etc_trafficmanager.lua` search blocking. The `onSearch` listener already swallows searches in both directions for blocked users, so they normally have no search to reply to. The new `onSearchResult` listener catches the protocol-violating edge case where a blocked user sends an unsolicited DRES / FRES (or a DRES targets a blocked user). Plugin bumped to v2.2. Backported to `release/3.1.x` as v3.1.9.

### Features

- **`show_reguser_password` cfg toggle - opt-in reversal of the #95 password redaction in `+accinfo` / `+usersearch`.** The #95 sub-task (`cmd_accinfo` v0.32 / `cmd_usersearch` v1.5) replaced the password column with `<REDACTED>` so the cleartext-equivalent ADC password (protocol-mandated, F-AUTH-1) stops being echoed into client-side chat logs. Hub operators reported this broke a real workflow - a user forgets their password and asks an op for it, and many older users cannot self-reset - and were hand-editing `cmd_accinfo.lua` to undo it. New shared cfg key `show_reguser_password` (default **false**, so the secure behaviour is unchanged and the promotion is non-breaking) restores the pre-redaction display when set true: `+accinfo` / `+accinfoop` and `+usersearch` show the stored password again. Kept as ONE shared key read by both plugins (no drift) rather than a per-plugin pair, and it is the supported escape hatch for the operators who were patching the source. Two bounds still hold: the existing per-user hierarchy gate (an op only sees passwords of users it may inspect; `+usersearch` otherwise shows `<Not allowed to view>`) and the password-free HTTP `/v1/registered` surface - the toggle touches only the ADC chat/PM paths. `+usersearch` is the higher-exposure case (bulk listing) and is documented as such. `cmd_accinfo` v0.36 -> v0.37, `cmd_usersearch` v1.5 -> v1.6; boolean key + validator in `cfg_defaults`, sample in `examples/cfg/cfg.tbl`, tradeoff in [`docs/SECURITY.md`](docs/SECURITY.md) §"Password disclosure". The toggle/redaction decision is extracted into a `_password_cell` unit-test seam in each plugin (both now on the [`docs/PLUGIN_API.md`](docs/PLUGIN_API.md) §8 seam-only roster), pinned by `tests/unit/reguser_password_toggle_test.lua` on both smoke.yml legs so a silent inversion of the toggle is caught; an empty/nil stored password renders the plugin's unknown string. Also corrected the stale `cmd_usersearch` Config keys in `docs/SCRIPTS.md` (`_permission` / `_advanced_rc` -> `_minlevel` / `_max_limit`) while touching that line. 3.2.x only.

- [#500](https://github.com/luadch-ng/luadch-ng/issues/500) - **`etc_forcetlstransfer`: force TLS-encrypted client-to-client transfers (force ADCS).** A TLS-only hub (`adcs://`) encrypts the client<->hub link, but the actual file transfer is a direct client<->client connection with its own encryption - so on an ADCS hub a peer can still negotiate a *plaintext* transfer (`ADC/1.0`), the C2C analogue of an unforced PM. The hub brokers every transfer setup (`CTM` / `RevCTM` / NAT-traversal + its reply all route through the hub carrying the transfer protocol at `adccmd[8]`), so it can refuse to relay a plaintext setup and thereby force `ADCS/`. The plugin hooks all four setup listeners (`onConnectToMe` / `onRevConnectToMe` / `onNatTraversal` / `onNatTraversalReply`) and, on a non-`ADCS/` protocol, either drops the setup (`return PROCESSED`) or lets it pass, per the single cfg key `etc_forcetlstransfer_mode` (`"warn"` | `"block"`). **All-or-nothing, no exemptions** - no level / whitelist carve-out (either the hub wants every transfer TLS or it does not; bots must move to ADCS too, and reusing the blocker-allowlist here would couple two unrelated trust decisions). **Ships ENABLED in `warn`** (non-breaking): unlike transparent hub-TLS, transfer-TLS needs each user's own TLS port reachable, so `block` out of the box would break un-forwarded users - `warn` instead PMs BOTH parties a one-shot notice with a client-setup link and lets the transfer through; the operator escalates to `mode="block"` when the community is ready. **Fail-closed:** `blocking = (mode ~= "warn")` (a typo blocks, never silently passes) and the block decision is locked in before the best-effort `pcall(notify)` (a notify that throws must not turn a block into a pass). Notify dedupe per firstnick (30s), cleared on `onLogout`. `etc_forcetlstransfer_mode` added to `cfg_defaults` (enum `warn|block`); en + de lang (2 role-neutral keys); `tests/unit/etc_forcetlstransfer_test.lua` (classifier / block / warn / all four events / dedupe / onLogout / fail-closed unknown-mode + throwing-notify / nil target + protocol), both smoke.yml legs. Live-validated on a `:dev` hub (active + passive, all four setup paths). 3.2.x only.

- [#501](https://github.com/luadch-ng/luadch-ng/issues/501) - **`etc_lockdown`: a transient maintenance-mode access gate.** `[+!#]lockdown <minlevel> [minutes] [reason] | off | status` raises a temporary minimum-level barrier so an operator can drain a hub for maintenance without a permanent `reg_only` config change. While active, `onConnect` vetoes any login below `<minlevel>` (ADC `ISTA 226` + `user:kill(ista, "TL"..retry_seconds)` so the client shows a reconnect countdown), and enabling it also **kicks the already-online users below the threshold** (snapshot-before-kick over `hub.getusers()` humans-only). An optional `[minutes]` sets an absolute `os.time()` expiry that `onTimer` auto-clears; `off` lifts it early; `status` reports state + remaining time. **Self-lockout guard:** an operator cannot set a `minlevel` above their own. **`sane_state()`** degrades a corrupt/partial persisted state to inactive rather than trusting it (a malformed store must never wedge the gate on). Time parsing uses `math.type(n) == "integer"` + a `MAX_MINUTES` (525600 = 1 year) cap so a float / `inf` / absurd value cannot poison the expiry. Bots never fire `onConnect`, so they are auto-exempt. 7 cfg keys in `cfg_defaults`; en + de lang; `tests/unit/etc_lockdown_test.lua` (parse / veto / kick-on-enable / expiry / self-lockout / sane-state), both smoke.yml legs. Ships **disabled** in `examples/cfg/cfg.tbl`. Live-validated on a `:dev` hub. 3.2.x only.

- [#480](https://github.com/luadch-ng/luadch-ng/issues/480) - **automatic encrypted local backups + offline restore (no cloud).** A four-part arc that lets a hub seal its own restore-critical state on a schedule and restore it offline. **`core/backup_archive`** (P0, [#481](https://github.com/luadch-ng/luadch-ng/issues/481)) defines **LDBK1** - a pure-Lua `tar` payload sealed with AES-256-GCM, keyed by PBKDF2-HMAC-SHA256; the KDF runs in C (`adclib.pbkdf2_sha256` -> OpenSSL `PKCS5_PBKDF2_HMAC`) because a pure-Lua PBKDF2 would freeze the single-threaded hub ~40s per backup. **`core/backup`** (PR-A, [#482](https://github.com/luadch-ng/luadch-ng/issues/482)) is the engine: it reads its own cfg, collects the restore-minimum set (`cfg/`, `scripts/data/`, `user.tbl`, the keys), seals a `.ldbk`, and rotates old archives; a new core-only `listdir` C primitive (not sandboxed, like `makedir`) enumerates the state dirs. **`scripts/etc_backup.lua`** (PR-A) is the scheduler, exposing the engine to the sandbox as `backup`. **`./luadch --restore <file>`** (PR-B, [#484](https://github.com/luadch-ng/luadch-ng/issues/484)) is a standalone offline entry: the C launcher's `run_restore` loads `core/restore.lua` into a fresh Lua state (it boots no hub - the cfg it would read is what it restores). Manifest evaluation is bounded by an instruction budget so a crafted archive cannot hang restore ([#485](https://github.com/luadch-ng/luadch-ng/issues/485)). Hardening: the offline restore entry sets `umask(077)` so every restored file is owner-only (the hub daemon itself keeps its `umask(027)`), and an F1 fix so a restore never clobbers a live `master.key`. Docker restore UX + an operator guide with an rclone off-site walkthrough ([`docs/BACKUP.md`](docs/BACKUP.md), [#487](https://github.com/luadch-ng/luadch-ng/issues/487)); the Docker compose / `.env` wire the backup passphrase + secret env vars ([#498](https://github.com/luadch-ng/luadch-ng/issues/498)). Rides along: the `adclib` `%zu` -> `%I` `lua_pushvfstring` diagnostic fix ([#483](https://github.com/luadch-ng/luadch-ng/issues/483)), same conversion-bug class as the P0 `listdir` `%lu` fix. 3.2.x only.

- [#444](https://github.com/luadch-ng/luadch-ng/issues/444) - **`cmd_ban`: permanent ban via the `permanent` keyword** (`+ban nick|cid|ip <TARGET> permanent [<REASON>]`, and the HTTP body flag `POST /v1/bans {..., "permanent": true}`). `+ban` had no forever-option since v0.15 removed the old negative-time one; #443 then correctly rejected negatives, so an operator who wanted a permanent removal had no first-class way to get one (and `+ban nick X permanent` silently gave a **20-minute** ban with the word "permanent" swallowed into the reason - `tonumber("permanent")` is nil, so the time fell back to `cmd_ban_default_time`). Now `permanent` is a recognised literal in the `<TIME>` slot (a fixed keyword, like `show`/`clear` - not translated; only the display label is). It stores a real `ban.permanent = true` marker with `time = 0`, **never a magic negative** - so #443's rejection of negative times stays final rather than flip-flopping. The login expiry check guards on the flag **before** the remaining-time math, so a permanent ban is never pruned (the placeholder `time = 0` would otherwise read as already-expired and be silently unbanned on the target's next connect). The kick uses **ADC STA 231 + TL-1** - the "permanently banned" code paired with the no-expiry `TL-1` - **not** a 30-year finite 232, which would render as a 30-year countdown on the client. cmd_ban keeps its own two-arg kill form (the `TL-1` rides on the `IQUI`, matching the timed path's `TL<seconds>`); that is the same 231/TL-1 the hub's other permanent-kick plugins (`etc_clientblocker` / `etc_geoip` / `etc_proxydetect`) use, though those embed the `TL-1` on the STA line instead - the framing differs, the semantics are identical. This is the *correct* 231 pairing and does not reintroduce the #147 T1.5 problem (that moved TIME-LIMITED bans off 231 precisely because 231 with a FINITE TL is self-contradictory; 231 with TL-1 is not). `+ban show`, `+ban showhis` and `GET /v1/bans` render the ban time as "permanent" (de "dauerhaft", new lang key `msg_permanent`) instead of a countdown, and the HTTP entry carries `"permanent": true` with no `expires_at`. The exported `add()` and internal `addban()` gained an optional trailing `permanent` argument (backward-compatible - existing 5-arg callers like the companion repo's `ptx_tagcheck` are unaffected). Smoke: a new ADC stage (`+ban ip X permanent <reason>` - the reply carries "permanent" and no time countdown) and an HTTP stage (`POST {permanent:true}` -> `"permanent":true` + no `expires_at`, persisted marker, DELETE cleanup), both provably red pre-fix (§1a.7 - pre-fix the keyword becomes a 20-minute countdown / the body flag is ignored). cmd_ban v0.43 -> v0.44. 3.2.x only.

- **`cmd_ban`: `permanent` added to the right-click Ban submenu.** Follow-up to [#444](https://github.com/luadch-ng/luadch-ng/issues/444): the permanent ban was reachable only by typing the keyword; the CT2 right-click Ban menu offered only 1 hour ... 1 year plus a free-form "other". A new `permanent` entry sits between "1 year" and "other" and sends `+ban sid <SID> permanent <reason>` - the #444 keyword path on the SID target (prefix-agnostic, resolves the clicked online user). New lang key `ucmd_menu_perm` (en `{ "Ban", "permanent" }` / de `{ "Bannen", "dauerhaft" }`); the sent keyword stays the fixed literal `permanent` (only the menu label is localized). Menu-only change - the permanent-ban behaviour, and its §1a.7 coverage, are unchanged from #444 (the SID keyword parse is identical to the already-tested nick path). `cmd_ban` v0.45 -> v0.46. 3.2.x only.

- [#310](https://github.com/luadch-ng/luadch-ng/issues/310) - **the POSIX event loop now uses `poll()` instead of `select()`, removing the ~1024 concurrent-socket ceiling.** The hub's single event loop (`core/server.lua` `tick()`) watches every connected socket in one `socket.select` call; the bundled luasocket `select.c` builds a fixed 1024-bit `fd_set`, so once the hub held ~1024 sockets (users + v4/v6 listeners + in-handshake conns + HBRI + HTTP) the call raised an uncaught error and the whole hub crashed, dropping every user - the Linux sibling of the Windows [#416](https://github.com/luadch-ng/luadch-ng/issues/416) crash at 64. `luasocket/src/select.c` gains a `global_poll` backend (variable-length `struct pollfd[]` via `lua_newuserdata`, no FD_SETSIZE cap) registered under the same `select` name behind a `#if defined(_WIN32)` guard, so the hub side is untouched on POSIX and Windows keeps its `fd_set`/`select` path (FD_SETSIZE=1024, #416). Contract parity with the select backend is exact: the `check_dirty` force-include of TLS sockets holding buffered-decrypted data (LuaSec), empty-not-nil result tables on timeout, a socket reportable in both read and write lists in one tick, and errored fds surfaced into every list they were watched in. `hub/hub.c` raises `RLIMIT_NOFILE` soft->hard at boot so the default `ulimit -n = 1024` does not become the new silent cap; the practical ceiling is now the process file-descriptor limit (raise further via `ulimit -n` / systemd `LimitNOFILE` / Docker `--ulimit nofile`). The boot line reports the backend and the real fd ceiling; the #416 smoke guard became backend-aware (the Linux leg asserts the poll line, which fails pre-fix; Windows keeps the `FD_SETSIZE >= 1024` assertion). No behavioural change on Windows. 3.2.x only.

- **#78 allowlist, Phase D: HTTP API for the whitelist (`etc_whitelist` v0.02).** Four endpoints mirroring the blocklist HTTP API: `GET /v1/whitelist` (read, filter/sort/paginate via `http_filter`), `GET /v1/whitelist/counts` (read), `POST /v1/whitelist` (admin, body `{cidr, source?, reason?, expires_at?}`, source enum `{manual, pinger}`, host-bits-set CIDRs rejected), `DELETE /v1/whitelist/{id}` (admin). HTTP admin tokens bypass the ADC hierarchy guard (the token is the trust surface; entries attributed `by_nick = <token label>` / `by_level = 100`). `etc_whitelist_test` gains 32 HTTP checks (registration + scope + schema + all four handlers, happy + error paths); a seed-aware `test_http_phase_d_whitelist` smoke does a real token-authed POST -> GET -> DELETE roundtrip (baseline count captured so it stays robust to the pinger seed). Docs: `SCRIPTS.md` (new `etc_whitelist` section) + `BLOCKLIST.md` (allowlist layer). Completes the 4-PR #78 allowlist arc. 3.2.x only.

- **#78 allowlist, Phase C: the IP-blocking plugins now consult the whitelist first.** `etc_geoip` (country/ASN kick), `etc_proxydetect` (proxy kick + the paid provider query) and `usr_hubs` (invalid-hubcount kick + hub-limit ban) each gained a one-line `if whitelist.is_whitelisted(ip) then return end` guard at the earliest point of their per-connection decision, so a whitelisted IP (hublist pinger / trusted infra) is exempt from these three - and for `etc_proxydetect` the guard sits BEFORE the cache / quota / HTTP so a trusted IP never costs a provider query. This silences the recurring `PROXYDETECT` / `GEOIP` / `USER HUBS` log lines for whitelisted IPs. Scope: this covers the IP-reputation blockers (plus the unified blocklist store via Phase A) and the hub-limit ban; the share / slots / nick-policy plugins (`usr_share` / `usr_slots` / `usr_nick_*`) are NOT yet whitelist-aware (a whitelisted IP still faces those), a possible follow-up. `etc_geoip_test` and `etc_proxydetect_test` gain whitelist-exemption regressions that provably fail without the guard (§1a.7); `usr_hubs` has no unit harness so its guard is the identical one-line mirror (diff-is-self-evidence). 3.2.x only.

- **#78 allowlist, Phase B: the `+whitelist` admin plugin + bundled hublist-pinger seed (`etc_whitelist.lua`).** Operator-facing chat command over the Phase A engine: `+whitelist show [source] | add <cidr|ip> [reason="..."] [expires=YYYY-MM-DD] | del <id> | count | export | import <path>`. Mirrors `etc_blocklist`'s structure (verb dispatch, `by_level` hierarchy guard on del, JSONL export/import with control-byte stripping + a master-only `etc_whitelist_import_min_level`, opchat report + audit fire-sites `whitelist.add`/`.remove`/`.export`/`.import`) minus stealth/HTTP. **Bundled seed:** on the FIRST run (store `.tbl` missing) the plugin seeds a small set of known hublist-pinger IPs (source=`pinger`, v6 as `/64`, v4 as `/32`) so the pingers are exempt from the automated blockers out of the box; seed-on-missing only (operator edits/deletions are never re-seeded), disable with `etc_whitelist_seed = false`. Ships enabled in `examples/cfg/cfg.tbl`. cfg keys `etc_whitelist_oplevel` / `_show_limit` / `_seed` / `_report[_hubbot|_opchat]` / `_llevel` / `_import_min_level`; lang en + de. `tests/unit/etc_whitelist_test.lua` (53 checks: parse / add / del-hierarchy / sanitize / format / seed-on-missing / export-import) + a `+whitelist` roundtrip smoke that also validates the seed lands on first boot; both registered on both smoke.yml legs. 3.2.x only.

- **#78 allowlist, Phase A: a global IP/CIDR whitelist engine (`core/whitelist.lua`).** The allowlist deferred from the unified-blocklist arc. A new core module - a stripped-down sibling of `core/blocklist.lua` (same bucketed radix cache, hex-encoded `.tbl` store, v4-mapped-v6 lookup; no source-priority / stealth / rollup) - exposes `whitelist.is_whitelisted(ip)` as a sandbox global so every IP-blocking path can consult it. `blocklist.check_ip` now applies **Model-A precedence**: a whitelisted IP overrides an AUTOMATED block (GeoIP / proxydetect / external feed) but NOT a deliberate manual pin (`source = "manual"` wins), so an operator can still block a specific IP that sits inside a whitelisted range. Full IPv4/IPv6 + CIDR support (a v6 `/64` covers a rotating pinger host); `expires_at` TTL; survives `+reload` via a cfg-reload listener. cfg keys `whitelist_enabled` (default true) + `whitelist_store_path` (`scripts/data/etc_whitelist.tbl`); the engine ships enabled with an empty store (zero overhead via a single `next()` fast path until an entry is added). `tests/unit/whitelist_test.lua` (63 checks incl. a blocklist-integration block whose precedence assertions provably fail on a blocklist without the hook, §1a.7); registered on both smoke.yml legs. Phase A of a 4-PR feature (B: `+whitelist` admin plugin + bundled hublist-pinger seed; C: per-plugin guards in etc_geoip / etc_proxydetect / usr_hubs; D: HTTP `/v1/whitelist`). 3.2.x only.

- [#420](https://github.com/luadch-ng/luadch-ng/issues/420) (#398 follow-up) - **`etc_webhook`: per-endpoint `conditions` filter on a decoded body field** (v0.02 -> v0.03). The event filter only matched the event *header*, but two real cases need a *body* field: a GitHub `release` webhook fires for every action (`created` / `edited` / `published` / `released`) under one `x-github-event: release`, and a Discourse new topic fires both `topic_created` AND `post_created` (its auto opening post) - so a release announced 5x and a new topic twice. New optional `conditions = { { path = "dotted.path", equals = X }, ... }` (or `not_equals`); ALL must hold to announce, evaluated against the decoded body via the existing `resolve_path` extraction (no new parsing surface). Values compare as strings (a number or string works); an unresolved path is `nil`, so `not_equals` passes when the field is absent (a `topic_created` carries no `post.post_number`, so the topic still announces while its opening post is dropped). Conditions apply endpoint-wide. The example config now filters GitHub to `action = "released"` (one clean line + release URL) and drops the Discourse opening post via `post.post_number != 1` (real replies still announce). `tests/unit/etc_webhook_test.lua` gains 6 checks, 3 of which provably fail pre-fix (§1a.7 - the unpatched plugin announces the opening post, a float `1.0` opening post, and `action=created`; the other 3 are positive controls that announce on both). Numbers compare numerically (a config `1` matches JSON `1` or `1.0`), otherwise as strings. Docs: `docs/WEBHOOKS.md` + `examples/cfg/webhooks.tbl`. 3.2.x only.

- [#398](https://github.com/luadch-ng/luadch-ng/issues/398) - **`etc_webhook.lua`: a generic INBOUND webhook receiver.** An external service (a Discourse forum, GitHub, GitLab, CI, monitoring, ...) POSTs an HMAC-SHA256-signed JSON body to a hub HTTP endpoint; the hub verifies the signature over the raw body (`adclib.constant_time_eq`), applies an event filter + delivery-id dedup, renders a `{dotted.path}` template and announces the event in chat as a named bot. The inbound-push mirror of `etc_status_push` (outbound heartbeat) and the complement of `etc_prometheus` (pull `/metrics`). **Multi-endpoint:** the operator-edited `cfg/webhooks.tbl` holds an array of endpoints, each with its own path / signature+event+id headers / event filter / bot nick / min-level / templates, so Discourse and GitHub can be received in parallel; `cfg.tbl` carries only the master switch `etc_webhook_activate` (keeps it lean). Per-endpoint secrets resolve env-var-first (`LUADCH_ETC_WEBHOOK_<NAME>_SECRET`), else the `etc_webhook_<name>_secret` cfg key, else an inline `secret` in `cfg/webhooks.tbl`; an endpoint with no resolvable secret is skipped (never runs unsigned). First plugin to register a **`scope="none"`** HTTP route - the router's bearer-token gate is skipped and the handler does its OWN HMAC auth; a missing/wrong signature is 401. Untrusted template fields are control-byte-stripped + length-capped, a per-minute flood cap bounds chat spam, and duplicate deliveries are deduped by the delivery-id header. Built on `core/hmac.lua` (the #398 precursor). Off by default (`etc_webhook_activate=false`; whitelisted `enabled=false` - advanced, needs `cfg/webhooks.tbl` + a reverse proxy). Unit-tested with the REAL hmac module (`tests/unit/etc_webhook_test.lua`, both smoke legs, 23 checks: HMAC pass/wrong/missing, `sha256=` prefix, event filter, dedup, template `{dotted.path}`/`{event}`/missing-path, control-byte strip + truncation, flood cap, min_level gating, no-secret-skip, activate=false inert). Operator guide `docs/WEBHOOKS.md`; security model in `docs/SECURITY.md`. 3.2.x only.

- **`core/hmac.lua`** - a pure-Lua HMAC-SHA256 (RFC 2104 / FIPS 198-1) primitive built on `core/sha256.lua`, whitelisted into the plugin sandbox. Precursor for the [#398](https://github.com/luadch-ng/luadch-ng/issues/398) inbound-webhook receiver: Discourse / GitHub sign each delivery with an HMAC of the raw request body (`X-Discourse-Event-Signature` / `X-Hub-Signature-256`), and a plugin must recompute that MAC to authenticate the request. The sandbox exposes no HMAC and deliberately withholds raw `sha256` (per the `geoip_update` ordering note), so this module is the whitelisted surface - plugins get the MAC (`hmac.sha256(key, msg)` hex, `hmac.sha256_bytes(key, msg)` raw), not the underlying hash. Standard block-size-64 construction with the `len(K) > B` pre-hash branch; a load-time self-test asserts RFC 4231 cases 1/2/6 (fail-loud, same discipline as `sha256`). Unit-tested against the full RFC 4231 vector set plus tamper-detection + argument validation (`tests/unit/hmac_test.lua`, both smoke legs); pair with `adclib.constant_time_eq` for the digest compare. 3.2.x only.

- **`etc_status_push.lua`** - a small opt-in plugin that periodically PUSHES the hub's PUBLIC status to an external HTTP(S) endpoint as JSON (a heartbeat). Generic: any consumer that accepts a JSON POST works - an external status page, a push-uptime monitor (healthchecks.io / Uptime Kuma / Better Uptime), a self-hosted dashboard, an automation webhook, a multi-hub aggregator; the DCVault wiki's online-status + user-count graph is the first consumer. Fixed contract: `POST <url>` with `Authorization: Bearer <token>` + `Content-Type: application/json` and body `{ "name": <hub name>, "users": <online humans, no bots>, "uptime": <seconds since start> }` - only public fields, no nick / secret / internal state, and no timestamp (the receiver stamps arrival, avoiding clock drift). Unlike `etc_regserver_announce` (register once then quiet) it is an unconditional heartbeat on a fixed interval (default 300 s) so the receiver gets evenly-spaced graph samples and detects staleness itself - no login/logout trigger, no give-up logic, missed beats are fine. Complements `etc_prometheus` rather than duplicating it: that is PULL (a scraper GETs `/metrics`, inbound exposure); this is PUSH (the hub dials out), which needs no inbound exposure and works behind NAT. Transport is the non-blocking `core/http_client` (the single-threaded hub never freezes on a slow endpoint); an `in_flight` guard prevents overlapping requests. Off by default (`etc_status_push_activate = false`); inert with no url or token. The token is read env-var-first via `core/secrets` (`LUADCH_ETC_STATUS_PUSH_TOKEN`, else the `etc_status_push_token` cfg key), registered as a secret when the plugin loads so `GET /v1/config` redacts it and `PUT /v1/config/{key}` refuses it (even while inactive); TLS `verify=peer` is the default (unlike the announce plugin) because the request carries the bearer secret. cfg keys: `etc_status_push_{activate,url,token,interval,tls_verify,cafile}` (in `cfg_defaults` + `examples/cfg/cfg.tbl`, whitelisted `enabled=true` but inert until activated). Unit-tested (`tests/unit/etc_status_push_test.lua`, both smoke legs); documented in `docs/SCRIPTS.md`. 3.2.x only.

- **`makedir` primitive + `util.makedir()`** - a `mkdir -p`-style directory-creation function compiled into the C launcher (`hub/hub.c`, portable `mkdir`/`_mkdir`, EEXIST-tolerant, bounded path) and registered as a global, plus a `safe_path`-gated `util.makedir(path)` wrapper for plugin-callable use (a sandboxed plugin can only create dirs INSIDE the hub tree; core self-heal that needs an absolute operator-configured dir uses the raw `makedir` global). Until now nothing in the codebase could create a directory - the hub relied entirely on the CMake install / Docker entrypoint having pre-created `log/`, `cfg/`, `scripts/data/`, `certs/`; a missing dir silently broke logging / plugin-state persistence (and a missing `master.key` dir hard-crashed boot). This is the precursor for boot-time runtime-directory self-heal. It is NOT a new bundled C module (a function added to the already-compiled launcher, so it does not hit the msys2-lua ABI-clash class); the wrapper is unit-tested (`tests/unit/util_makedir_test.lua`) and the C primitive is exercised end-to-end by the smoke harness. 3.2.x only.

- **`zlib_stream.gunzip()`** - a GZIP-wrapper variant of the existing `zlib_stream.inflate()` (`inflateInit2(windowBits = 15+16)`), reusing the same streaming `:push` / `:close` and the 4 MiB-per-push decompression-bomb cap; a non-gzip body is rejected loudly (`Z_DATA_ERROR`). `inflate()` (ADC-ZLIF, zlib-format) is unchanged. Enables in-hub decompression of gzip payloads - the precursor for the `etc_geoip` in-hub MaxMind DB auto-update. No new dependency (the same already-linked system zlib) and no ABI-surface change (an added function on an existing module, not a new C module). Linux CI round-trip test (`tests/unit/zlib_stream_test.lua`, run from the install tree with the bundled liblua, same as adclib). 3.2.x only.

- **GeoIP in-hub database auto-update** (`etc_geoip` Phase D3, `core/geoip_update.lua`). The hub can now download + refresh the MaxMind GeoLite2 `.mmdb` itself - no `geoipupdate` cron / Docker sidecar - on every platform it runs on (opt-in `etc_geoip_auto_update`, OFF by default; needs a free MaxMind account ID + license key). On an interval (daily default, 6 h floor) it fetches the tiny `.tar.gz.sha256` sidecar first and skips the download when unchanged (respecting MaxMind's rate limit), downloads the `.tar.gz` (via `http_client`'s `download_to_file` stream-to-disk mode), verifies it against the published SHA-256, then in a `server.addtimer` coroutine (yields per MiB so the hub keeps serving) gunzips (`zlib_stream.gunzip`) + extracts the single `*.mmdb` tar member, sanity-checks it with `mmdb.open`, and atomically swaps it into `cfg/geoip/` - the live reader picks it up immediately via the existing reopen-retain. Every step works on temp files, so a download / decompress / verify / extract failure keeps the last-good DB (fail-open) + fires a `geoip.update.fail` audit and one op-chat alert. Each cycle logs a fetch-start line to `event.log` (`out.put`) so the operator sees the update kick off, not just the result. The license key travels in an HTTP Basic `Authorization` header (never the URL, so never a log), is read env-var-first via `core/secrets.lua` (`LUADCH_ETC_GEOIP_LICENSE_KEY`), and is redacted in `GET /v1/config`. The tar extractor is a hardened untrusted-input parser (bounds-checked block reads, capped block scan + member size + total decompressed size (decompression-bomb guard), defensive octal parse, traversal-name reject, soft-fail-keep-last-good on a missing member). New core module wired before `scripts` in the load order + whitelisted in the plugin sandbox; unit-tested (`geoip_update_test.lua` orchestration + tar extractor, `etc_geoip_test.lua` update-cycle integration). MaxMind ships only `.tar.gz`, so the in-hub path needed gzip decompression (the `zlib_stream.gunzip` precursor above); the download / hash / atomic-rename / reopen-retain machinery is reused from the #78 arc. 3.2.x only.

- [#78](https://github.com/luadch-ng/luadch-ng/issues/78) - **unified blocklist arc** (in progress). A new pre-handshake IP/CIDR blocklist engine (`core/blocklist.lua` + `core/ipmatch.lua`, hooked at TCP accept before ADC/TLS) drops known-bad IPs and CIDR ranges before they cost a handshake, with provenance metadata per entry. Operator surfaces: the `+blocklist show/add/del/export/import/count` admin CLI with JSONL backup/restore (Phase B, `etc_blocklist`), and a `GET/POST/DELETE /v1/blocklist` + `/v1/blocklist/counts` HTTP API (Phase C). **GeoIP country/ASN policy blocking** (Phase D2, `etc_geoip`): resolves each connecting IP to its country / ASN via a bundled pure-Lua MaxMind DB reader (`core/mmdb.lua`, Phase D1) and logs or kicks connections by policy; per-connection post-handshake lookup (not a store sweep), `log_only` default, operators exempt, `+geoip` status + `GET /v1/geoip`, DB installed out-of-band via MaxMind `geoipupdate` (`docs/BLOCKLIST.md`). Supporting precursors: bundled Mozilla CA bundle + `verify="peer"` outbound-HTTPS default, `core/secrets.lua` env-var-first key lookup, pure-Lua `core/sha256.lua` + CA-bundle first-boot reconciliation, and a `core/http_client.lua` stream-to-disk download mode (`download_to_file`, 50 MiB on-disk body cap + 64 KiB header cap) so large external feeds are fetched without RAM pressure (Precursor 0a): bounded-drain per timer tick lifts throughput past the RAM path's one-chunk-per-tick ceiling; integrity guards keep the last good feed intact (only 2xx is written, `chunked` is rejected, a `Content-Length` short read is treated as truncation, `.tmp` + rename-with-Windows-rollback, concurrent same-path downloads refused). A `core/blocklist.lua` `bulk_replace(source, feed, entries)` API (Precursor E0) ingests a whole external feed's CIDR set in ONE atomic disk write and replaces the feed's previous rows in place - the per-CIDR `add()` rewrites the entire store each call (O(N^2) I/O), which would freeze the single hub thread for seconds on any real feed; bulk_replace is O(N), dedups, skips malformed CIDRs, caps per-feed entry count AND scan cost, rejects over-broad prefixes (a low-prefix v6 feed would otherwise explode the pure-Lua bucket cache to multiple GB), refuses `source="manual"` (feeds cannot reap operator pins) and refuses to wipe a good feed with an all-invalid input, sweeps expired rows, and rolls back atomically on a save failure. **External feeds** (Phase E, `etc_blocklist_feeds`): pulls public IP/CIDR blocklists over verified HTTPS on a per-feed timer and ingests them via `bulk_replace` - built-in **Tor** exit-node, **Spamhaus DROP** (v4 + v6), **AbuseIPDB** blacklist, and a **generic** operator-URL adapter, each independently opt-in and OFF by default. Per-feed refresh interval clamped up to the provider's published auto-fetch floor (aggressive polling gets the hub firewalled - AbuseIPDB's blacklist download is 5/day on the free tier, so its floor is 6 h); per-feed stealth; self-healing (a failed, empty, truncated, or malformed fetch keeps the last-good entries + a 2x-interval TTL backstop, never fail-opens the feed); `+blfeeds` status + `GET /v1/blocklist/feeds`. AbuseIPDB's API key is read env-var-first via `core/secrets.lua` (Docker-friendly) and, if placed in cfg.tbl, redacted in the config API (`GET /v1/config`) once the plugin is loaded; Tor / Spamhaus / generic need no key. Every source is independently opt-in; a minimalist hub keeps only the classic ban list. All parts ship OFF or manual-only by default. This closes [#79](https://github.com/luadch-ng/luadch-ng/issues/79) (AbuseIPDB integration). **Live proxy / VPN / Tor detection** (Phase F1, [#352](https://github.com/luadch-ng/luadch-ng/issues/352), `etc_proxydetect`): looks each connecting IP up against an external provider API (**proxycheck.io** in F1; VPNAPI.io + IPQS in F2) and logs or kicks proxy / VPN / Tor connections by policy. The lookup is non-blocking - the verdict arrives in a callback that re-resolves the user by session ID and CID-guards against SID reuse before kicking (a client that inherited a recycled SID is never kicked for the departed proxy's verdict). A positive verdict in block mode is ALSO pushed into the pre-handshake blocklist with a TTL, so a repeat connection from that IP is dropped at TCP-accept (silently by default) with no further query; clean verdicts are cached (`scripts/data/etc_proxydetect.tbl`) and a daily query cap (`etc_proxydetect_max_queries_per_day`, default 1000) protects the provider quota against a distinct-IP flood. **Fail-open by default** (a provider outage / timeout / spent quota lets the user IN rather than locking every joining user out; `etc_proxydetect_fail_open=false` for strict mode), operators exempt (`etc_proxydetect_check_levels`), `log_only` default, `+proxydetect` status + `GET /v1/proxydetect`. The client IP is strictly validated (v4 octet range / v6 hex+colon charset, length-capped) before it can reach the request URL (SSRF), and the provider key is read env-var-first via `core/secrets.lua`, redacted in the config API, and sent in the POST body (never the URL) so it stays out of `http_client`'s failure logs. The verdict cache is hard-capped so an unlimited-quota hub cannot be OOM'd by a distinct-IP flood. Three-agent two-pass review (§1a.6) hardened the key-in-log leak, the cache cap, a duplicate store-push on cache hit, and quota accounting. Provider free-tier terms differ sharply and are surfaced to operators (VPNAPI free = non-commercial only; IPQS free = 1000/month, evaluation-only; proxycheck commercial-use unconfirmed) - see `docs/BLOCKLIST.md`. A supporting `core/http_client.lua` precursor (F0) adds an optional `log_url` request field so a provider key carried in the request URL - query string or path - is kept out of the client's failure/crash logs (`out.error`/`out.put` log the request URL on a timeout, which connect-path lookups routinely hit); needed for F2's VPNAPI / IPQS adapters, which (unlike proxycheck) put the key in the URL and cannot move it to the POST body. **Phase F2** adds the **VPNAPI.io** and **IPQualityScore** provider adapters (operator picks one via `etc_proxydetect_provider`; both require a key, and because they carry it in the request URL - vpnapi query string, ipqs path - the plugin passes a key-free `log_url` via the F0 precursor so the key never reaches the failure log), plus a provider-failure op-chat alert: a run of `etc_proxydetect_fail_alert_threshold` failures within 60 s fires one alert (once per outage, until a lookup recovers) + a `proxydetect.provider.down` audit, so a clearly-broken provider (bad key / outage) is surfaced under fail-open rather than silently degrading detection (best-effort - a partial or low-traffic outage may not reach the threshold; tune it down for a quiet hub). This closes [#352](https://github.com/luadch-ng/luadch-ng/issues/352). 3.2.x only.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) - new cfg `http_bind_addr` makes the HTTP API listener's bind address configurable, primarily so container deployments can expose the API to sibling containers (WebUI, reverse proxy) over a private Docker network without publishing the port to the host. Default `"127.0.0.1"` keeps the loopback-only contract that existing installs rely on; behaviour is byte-identical when the cfg key is unset. Set to `"0.0.0.0"` / `"::"` ONLY in container setups - never on bare-metal hosts with a public interface (the API has no transport-layer TLS by design; token auth still applies at the API layer, but loopback IS the security premise). Docs (`docs/HTTP_API.md` §2 + `examples/cfg/cfg.tbl` + `core/cfg_defaults.lua` + `core/hub.lua` block comment) updated accordingly. 3.2.x only, not backported.
- [#214](https://github.com/luadch-ng/luadch-ng/issues/214) HBRI (PR-B, [#284](https://github.com/luadch-ng/luadch-ng/pull/284)) - **Hub-Bridged Reverse Initiation: verify a dual-stack client's secondary IP family over a second-family side-channel, then restore it to the broadcast INF.** Opt-in (`hbri_enabled`, default off; effective only with a listener on BOTH families AND `hbri_advertise_v4` / `hbri_advertise_v6` set - otherwise the secondary just stays stripped per Gap 1). Flow: after login a supporting client (advertising `ADHBRI`) is parked in a new `hbri` state; the hub mints a CSPRNG token (`adclib.createsalt` -> OpenSSL `RAND_bytes`), sends an `ITCP` pointer to its other-family listener, and only commits + broadcasts the secondary once the client connects back on that family. The committed address is always the side-channel's **authenticated TCP source**, never a client-supplied value; a connection from the claimed address is itself the reachability proof. On validation failure or a `hbri_timeout`-second timeout (swept on the ~1s hub timer) the client enters the hub with the secondary left stripped. New `core/hbri.lua` (token table + mint / validate / sweep / fail, single-use tokens, family + source-IP checks); `core/hub.lua` `login()` HBRI fork + sweep timer + disconnect cancel; `core/hub_dispatch.lua` pre-strip claim capture + `_protocol.HTCP` validator + `ADHBRI` SUP advertise; `TCP` command registered in `core/adc.lua` (**H-context only** so only inbound `HTCP` parses); cfg knobs in `core/cfg_defaults.lua` (advertise addresses reject whitespace / control bytes to prevent ITCP frame injection). No `core/server.lua` change - the validation socket rides the normal accept path, identified purely by its `HTCP`+token first frame. Two-pass review (§1a.6) caught + fixed a misplaced disconnect-cancel hook (had landed in dead commented code) and the advertise-validator injection gap. Smoke: success / timeout / unknown-token (`ISTA 220`) / disconnect-mid-validation cleanup. `docs/SECURITY.md` "dual-stack secondary-address verification" section + `examples/cfg/cfg.tbl` block updated. 3.2.x only.
- [#291](https://github.com/luadch-ng/luadch-ng/issues/291) HBRI login-time discovery ([#292](https://github.com/luadch-ng/luadch-ng/pull/292)) - HBRI now fires for the COMMON real-client case. Auto-detect clients (AirDC++ behind NAT) advertise the secondary family as the spec placeholder (`I6::` / `I40.0.0.0`, "discover my address"), not a concrete value; the #288 placeholder-skip treated that as "no secondary", so HBRI never solicited for them. The claim-capture gate in `core/hub_dispatch.lua` now triggers on a present, non-empty secondary field (placeholder included) and still skips a genuinely absent field (the #288 v4-only guard). `core/hbri.lua` `validate()` treats a placeholder/absent claimed value on the side-channel as a discovery request and commits the authenticated getpeername; only a CONCRETE stated address is cross-checked against the socket source. The dead `claimed_ip` token-table field was dropped so "never commit a client-stated address" is structural. Verified against the adchpp HBRI reference (trigger = non-empty secondary INF field, commit = getpeername). Falsifiable smoke `test_hbri_discovery_placeholder` (fails pre-fix: no ITCP) + `test_hbri_concrete_mismatch_rejected`. 3.2.x only.
- [#286](https://github.com/luadch-ng/luadch-ng/issues/286) HBRI post-login validation ([#293](https://github.com/luadch-ng/luadch-ng/pull/293)) - a client already in NORMAL state that LATER advertises a secondary via a post-login INF update now gets it validated over HBRI, instead of the secondary being silently stripped forever (#97 / #222). `core/hub_dispatch.lua` `_normal.BINF` calls `hbri.postlogin_inf()` BEFORE the `onInf` strip so it can read the secondary; the unverified value is still stripped from that broadcast (the #97 / #222 invariant holds), and only a side-channel-validated secondary is broadcast - the user stays in NORMAL the whole time (matching the adchpp flag-only post-login flow). Three loop guards stop a re-solicit storm from a client that re-emits its connectivity on every INF: in-flight token, already-validated idempotence, and a per-user 60s cooldown bounding the failed case. `initiate()` gained an explicit-claim + `postlogin` parameter (parks only at login); `commit_and_complete` broadcasts via `sendtoall` for the post-login case. Falsifiable smoke: post-login success / failure-stays-stripped / cooldown (all fail pre-fix: no ITCP). 3.2.x only.
- [#279](https://github.com/luadch-ng/luadch-ng/issues/279) follow-up - regserver announcer usability + reach. (1) **Multiple regservers:** `etc_regserver_announce_url` now accepts a string OR an array of strings (`{ "https://reg1/register", "https://reg2:1337/register" }`); the plugin tracks per-target state (confirmed / attempts / in-flight / give-up) and persists the confirmed HH per URL, so each hublist is registered + retried independently. (2) **IPv6 literals** in `core/http_client.lua`: `parse_url` now accepts bracketed IPv6 (`https://[2001:db8::1]:1337/register`) - the earlier reject is lifted; `drive` uses `socket.tcp6()` and the `Host` header re-brackets it. The host regex inside the brackets is hex+colon only and the whole-URL control-byte check runs first, so it stays injection-safe. Documented the DNS caveat: a *hostname* URL makes `connect()` do a synchronous getaddrinfo (brief block; luasocket has no async resolver); IP literals (v4/v6) skip DNS and are fully non-blocking. (3) **cfg discoverability:** `examples/cfg/cfg.tbl` now ships a proper `etc_regserver_announce.lua settings` block (all six keys + comments) instead of just the scripts-list line, and that line now points at it. Focused two-pass review on the delta (IPv6 parse injection-safety + the per-target closure capture in the multi-regserver loop) - clean, no findings. `etc_regserver_announce` v0.02; `http_client` unit test now 47 checks (IPv6 cases); smoke e2e uses the array url form. 3.2.x only.

- [#279](https://github.com/luadch-ng/luadch-ng/issues/279) - **Non-blocking outbound HTTP(S) for plugins** + the first consumer, an opt-in hublist regserver announcer. New `core/http_client.lua` lets a plugin make an outbound request WITHOUT freezing the single-threaded hub: it drives a non-blocking socket through a coroutine registered on the public `server.addtimer` (~1s timer) - connect / TLS-handshake / send / receive all `settimeout(0)` and `coroutine.yield()` between steps, so the select() loop keeps serving every connected user while the request is in flight. It touches NO `core/server.lua` internals. API: `http_client.request{ url, method, body, headers, timeout, verify, cafile, on_complete, on_error }` returns immediately. Hard bounds: per-request deadline (clamped 1..120s), response-size cap (default 64 KiB, 1 MiB ceiling), and a global in-flight cap (16) so a buggy caller cannot spawn unbounded timer coroutines. Security hardening from the two-pass review: CRLF/control-byte rejection on method / url / headers (anti request-smuggling - `has_ctrl` + `parse_url` reject CRLF, embedded credentials, bracketed IPv6, out-of-range ports); the receive loop yields after every read so a fast/large response can never pin the thread; TLS floored at 1.2 (`options = no_sslv3/no_tlsv1/no_tlsv1_1`); SNI set; `verify` defaults to `"none"` (documented as unauthenticated - acceptable for POSTing PUBLIC data, opt into `"peer"`+`cafile` for authenticated channels); no URL allowlist by design (caller's responsibility - documented; the bundled caller uses operator-cfg URLs only). The module is in the plugin sandbox whitelist; `const` is also added to the whitelist (capability-free static strings: PROGRAM_NAME / VERSION / etc) so plugins can report the app name + version. New `scripts/etc_regserver_announce.lua` (v0.01) is the first consumer: **OFF by default** (`etc_regserver_announce_activate = false` = private hub; this is the NMDC-style privacy gate). When enabled it POSTs the hub's PUBLIC ADC IINF (name / app / version / address / description / website / network / owner / online count - a fixed allowlist, ADC-escaped via `hub.escapeto`, never any secret) to the configured regserver URL. Event-driven per the design agreed with the regserver author: announce on start, retry via `onTimer` on a coarse interval (default 5 min, capped at 12 attempts) until a 2xx confirms, then go quiet; re-announce only if the advertised address (HH = `adcs://<hub_hostaddress>:<ssl_port>`) changes - the last confirmed HH is persisted to `scripts/data/` so a `+reload` with an unchanged address does not re-announce. An `in_flight` guard prevents overlapping requests. Tests: `tests/unit/http_client_test.lua` (39 checks: url parse, request build incl. Host/Connection spoof-guard + CRLF rejection, response parse) wired into Linux + Windows CI; smoke `test_regserver_announce` is an end-to-end proof - a loopback fake regserver (Python thread) receives the plugin's IINF POST and the hub answers a normal ADC handshake immediately afterward (the request did not block the loop). The standalone regserver itself is a separate side-project repo. 3.2.x only.

- [#263](https://github.com/luadch-ng/luadch-ng/issues/263) PR-B - Long-poll `?wait=<seconds>` on `GET /v1/events` (clamps to [0, 60]) + the 3 plugin-side event emits the PR-A spec deferred. When the immediate poll returns no matching events AND wait > 0, the handler emits a deferred response (status sentinel `"deferred"` + `defer` function). `core/http_router.lua`'s dispatch forwards it verbatim through `core/http.lua`'s `http_incoming`, which calls the defer function with the connection handler instead of writing+closing. The defer function registers the connection in `core/http_events.lua`'s waiter list with a render closure that owns the pm-scope filter. Resolution paths: (1) **event-driven** - `emit()` walks the waiter list after appending each event and resolves every matching one (sub-tick latency); (2) **deadline-driven** - a `server.addtimer` entry calls `tick()` ~once per second, resolving any waiter whose deadline has elapsed with an empty events array. Both paths write the response + close the connection wrapped in pcall so a client that disconnected mid-poll cannot crash the resolver. Plugin-side event emits also land in this PR: `cmd_ban` v0.40 emits `ban_added` after the upsert in `addban` and `ban_removed` after the DELETE handler's removal; `cmd_topic` v0.05 emits `topic_changed` from both `do_set_topic` and `do_reset_topic` (reset = change to default). Both plugins guard the emit behind `if http_events and http_events.emit then` so the migration is hub-version-agnostic. **No SSE.** Long-poll uses a deferred-response handshake at the dispatch layer, not a coroutine yield in the iostream pipeline - fits the existing request/response model without an SSE-grade rewrite of `core/iostream.lua` (~400-600 LoC saved). Smoke regression `test_http_events_pr_b_longpoll` covers both resolution paths via a Python-thread harness: (a) deadline-driven by polling for `types=topic_changed&wait=2` with no topic-change event fired, asserts response arrives in [1.5s, 5s] range with empty events; (b) event-driven by opening a long-poll in a background thread with `types=login&wait=10`, triggering a fresh `dummy` ADC login from the main thread, asserts the response arrives in < 4s with the login event in `events`. Bug caught in two-pass review: `core/http.lua`'s strict-mode env trap rejected the new `type(body) == "function"` check because `type` wasn't `use`'d - added `local type = use "type"`. 3.2.x only, not backported.

- [#263](https://github.com/luadch-ng/luadch-ng/issues/263) PR-A - HTTP API polled event stream: `GET /v1/events?since=<id>&types=<csv>` (read scope) returns an array of recent lifecycle events from an in-memory ringbuffer (size configurable via new cfg key `http_events_buffer_size`, default 1000 ~ 200 KB). New `core/http_events.lua` module loaded in `_core` after `scripts` so its `init()` can register a core-side tap into `scripts.firelistener` - the tap captures EVERY event the hub fires through the existing listener-chain machinery (no plugin changes needed) and maps them into 8 event types: `login` / `logout` / `broadcast` / `pm` / `failed_auth` / `reg_added` / `reg_removed` / `script_error`. Common envelope per event: `id` (monotonic per-process counter), `type`, `timestamp` (ISO 8601 UTC), plus per-type payload fields - all string payloads are control-byte sanitised at emit time. Response carries `cursor` (next `?since=` value) and `cursor_lost: true` when the supplied cursor is below the buffer's minimum id (= events evicted) so clients can catch up via per-resource endpoints. The `pm` event type is admin-only - filtered out for read-scope tokens, surfaced for admin-scope tokens; the handler propagates `req.token_scope` through to the poll function. Special `?since=latest` returns an empty events array plus the current cursor for no-replay subscription. Implementation in core - `core/scripts.lua` gets a new `register_tap(fn)` export that runs core-side taps BEFORE the plugin listener iteration so the event stream reflects what the hub OBSERVED (not just what was ultimately dispatched after PROCESSED short-circuits). The 3 plugin-side event types from the #263 spec (`ban_added`, `ban_removed`, `topic_changed`) need explicit `http_events.emit` calls from `cmd_ban` / `cmd_topic` - deferred to PR-B's plugin-integration pass. **PR-A is immediate-return polling only.** PR-B will add the `?wait=<seconds>` long-poll path via a coroutine-yield in the HTTP dispatcher (genuinely new infrastructure - kept scoped out of PR-A so the ringbuffer + tap machinery + event-type catalogue can land + bake without dragging the coroutine-dispatch change along). Polling at 1-2 second intervals stays well within the read budget (`http_api_rate_read = 120/min` default). Smoke regression `test_http_events_pr_a` triggers a `dummy` ADC login, verifies the `login` event lands in the buffer with the right payload + cursor advance + `?types=login` filter + `?types=unknown` empty + `?since=latest` no-replay; cfg.tbl bare-key format restored on exit (matches the #261/#262 test pattern). Body / response shape + event-type catalogue documented in [`docs/HTTP_API.md` §10.1 footnote](docs/HTTP_API.md). 3.2.x only, not backported.

- [#262](https://github.com/luadch-ng/luadch-ng/issues/262) - HTTP API config-management endpoints: `GET /v1/config` (read scope) returns a full cfg snapshot - every key registered in `core/cfg_defaults.lua` plus its current value - with sensitive entries masked as the literal string `"<redacted>"`. Initial denylist covers `http_api_tokens` (bearer credentials) and `master_key_path` (Phase 7c at-rest encryption key path); both stay one-line appends in `core/http_router.lua`'s `_config_denylist`. `PUT /v1/config/{key}` (admin scope) takes `{"value": <any JSON type>}` and mutates the cfg key via `cfg.set` (atomic write to `cfg.tbl`); denylisted keys return 403 `E_FORBIDDEN` (rotate via direct cfg.tbl edit + hub restart), unknown keys return 404 `E_NOT_FOUND`, validator-rejected values return 400 `E_BAD_INPUT` with the validator's err_msg surfaced. The response carries `apply_status` classifying the change as `live` (default - effect on `cfg.set`), `reload_required` (currently `scripts` / `language` plus the path-pointer keys `user_path` / `script_path` / `scripts_cfg_path` / `scripts_lang_path` / `core_lang_path`), or `restart_required` (currently the port arrays + `http_port` + `hub_listen` + `master_key_path` + `log_path` + `ssl_params` + `use_ssl`); the classification lives in a small lookup table in `core/http_router.lua`, NOT in `core/cfg.lua` - keeps cfg unburdened of API-specific metadata. PUT does NOT auto-trigger reload (operator chains `POST /v1/reload` for reload-required, full restart for restart-required - same explicit-action discipline as #261 plugin toggles). Core change: `cfg.set` return shape normalised to `(true)` on success, `(false, err_msg)` on any failure path (validator reject / unknown key / savetable error) - the existing single-return-value callers continue to work because `if not ok` correctly captures both `nil`-equivalent and `false`. Two new exports on `core/cfg.lua`: `is_known(key)` lets callers distinguish unknown-key from validator-reject without parsing error strings; `list_keys()` enumerates every registered key for the GET snapshot. Endpoints live in `core/http_router.lua`'s core surface (`plugin = "core"`) so the management endpoint cannot be disabled via the plugin API. Smoke regression `test_http_config_api` runs 8 assertions: GET shape + denylist masking, PUT live key (max_users -> apply_status=live), PUT reload key (language -> reload_required), PUT restart key (http_port -> restart_required), PUT denylist 403, PUT unknown 404, PUT empty body 400, PUT validator-rejected 400 with hint; restores cfg.tbl bare-key format on exit so downstream regex-based cfg flips (BLOM / ZLIF / hub_listen) keep working (same pattern as #261's plugins-api test). Body / response shapes documented in [`docs/HTTP_API.md` §10.1 + footnotes](docs/HTTP_API.md). 3.2.x only, not backported.

- [#264](https://github.com/luadch-ng/luadch-ng/issues/264) PR-B - Wire the remaining 5 HTTP API list endpoints into the `http_filter` helper landed by PR-A. Endpoints: `/v1/bans` (`cmd_ban` v0.39 - searchable nick/cid/ip/by_nick/reason + ban_seconds range + expires_at after/before; sortable id/nick/by_nick/ban_seconds/ban_start; default sort = id preserves the pre-#264 insertion order); `/v1/blacklist` (`etc_blacklist` v0.10 - searchable nick/by/reason + blacklisted_at after/before; sortable nick/by/blacklisted_at; default sort = nick); `/v1/msgmanager` (`etc_msgmanager` v0.9 - searchable nick/mode; sortable nick; `settings` sibling unfiltered); `/v1/trafficmanager/blocks` (`etc_trafficmanager` v2.6 - searchable nick/by/reason + blocked_at after/before; sortable nick/by/blocked_at); `/v1/usercleaner/expired` + `/v1/usercleaner/ghosts` (`cmd_usercleaner` v0.7 - searchable nick + level (+ range) + days_offline / days_since_reg (+ range) + nick_protected / level_protected booleans; sortable days field default DESC + nick + level - matches the pre-#264 reverse-sort). All five responses now carry a `pagination` sibling per §6.4 (was only on /v1/users + /v1/registered before). The `/v1/bans/history` endpoint is intentionally NOT in scope - its response shape is a dict-keyed-by-nick rather than a flat array, so the helper's filter/sort/paginate flow does not map cleanly; the pre-existing `?nick=` param remains intact. Wire-up smoke regression `test_http_filter_sort_pr_b` sends an unknown-filter-field query against each new endpoint and asserts 400 `E_BAD_INPUT` with the `allowed filters` hint - tight pre-fix-fails / post-fix-passes signal (the pre-fix handlers silently passed query params through and returned 200 with the full list). The `target_type` filter from the #264 spec is intentionally NOT wired on /v1/bans - it is not a stored ban-entry field; inferring from non-empty cid/ip/nick would be a separate enhancement, deferred. Body / response shape per-endpoint allowlists documented in [`docs/HTTP_API.md` §6.4 + footnotes](docs/HTTP_API.md). 3.2.x only, not backported.

- [#264](https://github.com/luadch-ng/luadch-ng/issues/264) PR-A - HTTP API filter + sort query params for list endpoints. New `core/http_filter.lua` helper (~330 LoC) loaded into `_core` and exposed in the plugin sandbox as `http_filter`; offers `apply(query, spec, rows)` that takes a per-endpoint field-spec (string / integer / boolean / date typed allowlists + sortable-fields map + default-sort declaration), runs filter → sort → paginate uniformly, returns rows + `{total, limit, offset, next_offset}` pagination metadata, or a 400 `E_BAD_INPUT` with allowed-fields hint on unknown filter/sort keys. String filter is case-sensitive substring (`string.find` plain=true); integer is exact + `_min` / `_max` range; boolean is `true` / `false` exact; date is `_after` / `_before` paired params with per-endpoint `parse_query` so epoch-int fields (e.g. `lastseen`) and string-date fields (e.g. `regged_at` in `YYYY-MM-DD / HH:MM:SS`) can coexist. Filter applies BEFORE pagination; `pagination.total` reflects the filtered count. Sort uses `?sort=field` ascending, `?sort=-field` descending; single key only; nil sorts to the end regardless of direction. PR-A wires `/v1/users` (in `core/http_router.lua`) and `/v1/registered` (in `scripts/cmd_reg.lua` v0.34) - the two highest-cardinality list endpoints, exercising all four field types. PR-B will wire the remaining 6 list endpoints (bans / bans/history / blacklist / msgmanager / trafficmanager/blocks / usercleaner/expired+ghosts). Existing endpoint behaviour unchanged when no filter / sort params are supplied. Smoke regression `test_http_filter_sort_pr_a` seeds three reg-users (filter_alice level 30, filter_bob 40, filter_carol 30), exercises nick substring + level exact + level_min + level_max + default-sort + sort=-nick + sort=level + pagination.total = filtered-count, plus negative paths (unknown filter field 400, unknown sort field 400, non-numeric integer 400). /v1/users portion uses the live `dummy` ADC session for substring + sort + 400 coverage. Body / response shape + per-endpoint allowlists documented in [`docs/HTTP_API.md` §6.4 + footnotes](docs/HTTP_API.md). 3.2.x only, not backported.

- [#261](https://github.com/luadch-ng/luadch-ng/issues/261) - HTTP API plugin-management endpoints: `GET /v1/plugins` (read scope) lists every entry in `cfg.scripts` with its runtime state (loaded / version / listeners / http_routes), and `PUT /v1/plugins/{name}/enabled` (admin scope) toggles a manageable plugin's enabled flag. `cfg.scripts` entries now accept TWO forms - a plain string `"name.lua"` (operator-managed, API-protected) or a table `{ "name.lua", enabled = bool }` (API-toggleable). Backward compatible: existing all-string cfg.tbl files load unchanged. Default `examples/cfg/cfg.tbl` ships 10 plugins in table-form (etc_prometheus / etc_motd / etc_banner / etc_chatlog / etc_records / etc_userlogininfo / etc_dhtblocker / bot_regchat / bot_session_chat / hub_runtime); the rest stay as strings ("infrastructure / core ADC functionality"). PUT mutates `cfg.tbl` via `cfg.set()` (atomic write) but does NOT trigger a reload; response carries `reload_required: true` so clients chain `POST /v1/reload` after a batch of toggles. Per-plugin reload is out of scope - tracked under #48. Endpoints live in `core/http_router.lua`'s core surface (NOT a plugin) so the management endpoint cannot be disabled by mutating its own backing plugin. Plugin version extracted via source-grep of `local scriptversion = "..."` at module-load time (zero plugin touches); plugins can opt-in to override via `return { _version = "..." }`. Drive-by fix in `core/util.lua`: `sortserialize`'s `table.sort(temp)` now uses a type-aware comparator (numbers before strings) so mixed-key tables like the new `{name, enabled=bool}` form survive serialisation - Lua 5.4 errors on default `<` comparison across types and would have crashed every `cfg.set` that touches scripts. Body / response shapes documented in [`docs/HTTP_API.md` §10.1](docs/HTTP_API.md). Smoke regression `test_http_plugins_api` runs a full toggle cycle (PUT disable → reload → GET shows loaded=false → PUT enable → reload → GET shows loaded=true) plus the three negative paths (403 on string-form, 404 on missing, 400 on bad body). 3.2.x only, not backported.

- [#83](https://github.com/luadch-ng/luadch-ng/issues/83) - Prometheus `/metrics` endpoint shipped as opt-in plugin `etc_prometheus.lua` v0.1. 7 gauges (online users / bots / share / files / uptime / lua-memory / active-bans) + 7 counters (logins / logouts / failed-auths / chat-msgs / pm-msgs / searches / script-errors). Read scope; Prometheus must be configured with the bearer token. Opt-in via cfg `etc_prometheus_activate` (default `false`); when off the route is not registered. Counters reset on hub restart AND on `+reload` per the Prometheus monotonic-since-scrape-target-restart convention. No level-breakdown labels (low cardinality). Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.

- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-1 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `GET /v1/registered` (read, paginated per §6.4), `POST /v1/registered` (admin), and `PATCH /v1/registered/{nick}` (admin). `cmd_reg` plugin v0.33 lets the existing ADC `+reg nick` / `+reg desc` chat-cmds coexist with the new HTTP endpoints. GET lists humans-only entries (bots excluded), sorted by nick. POST returns the (auto-generated or caller-supplied) password exactly once at creation so the operator can communicate it to the new user. PATCH currently supports only the `comment` field (mirrors ADC `+reg desc`); structured fields have their own PUT subresources. ADC-side `cmd_reg_permission` level-ladder bypassed on the HTTP path - admin token IS the auth gate. Body / response shapes documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-2 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `GET /v1/registered/{nick}` (read scope). `cmd_accinfo` plugin v0.33 lets the existing ADC `+accinfo` / `+accinfoop` chat-cmds coexist with the new HTTP endpoint. Returns the expanded view (= ADC `+accinfoop`): nick, level, level_name, by, regged_at, lastseen, is_online, comment, traffic_blocked, msg_blocked, ban. Bots return 404 (humans-only surface matches PR-1 GET list + PATCH). Password is **not** in the response - the HPAS challenge-response is protocol-mandated cleartext-equivalent (#95 / F-AUTH-1), but the API surface never echoes it; rotation goes through PR-3 `PUT /v1/registered/{nick}/password`. ADC-side `level < 10` gate bypassed on the HTTP path - read scope IS the auth gate. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-3 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `PUT /v1/registered/{nick}/password` (admin scope). `cmd_setpass` plugin v0.22 lets the existing ADC `+setpass nick` chat-cmd coexist with the new HTTP endpoint. Body `{password: string}`; whitespace rejected, length validated against `cfg.min_password_length` / `cfg.max_password_length`. Returns `{action:"password-set", nick, online_notified}`; online users get the same `msg_ok2` PM as on the ADC path. PUT is idempotent (same-password retry returns 200, unlike the ADC `msg_nochange`). Bots return 404 (humans-only surface matches PR-1 / PR-2). ADC-side `cmd_setpass_permission` ladder bypassed on the HTTP path - admin token IS the auth gate. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-4 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `PUT /v1/registered/{nick}/nick` (admin scope). `cmd_nickchange` plugin v2.0 lets the existing ADC `+nickchange` chat-cmd coexist with the new HTTP endpoint. Body `{new_nick: string}`; whitespace rejected, length validated against `cfg.min_nickname_length` / `cfg.max_nickname_length`. Returns `{action:"nick-changed", nick, previous_nick, online_kicked}`; online users get the `ISTA 230 ... TL-1` disconnect so the client re-connects under the new nick. `cmd_reg_descriptions.tbl` entry migrates from old to new nick. PUT is idempotent (rename-to-same returns 200, no kick). Bots return 404 (humans-only surface matches PR-1 / PR-2 / PR-3). ADC-side `cfg.nick_change` gate + `cmd_nickchange_minlevel` / `_oplevel` ladders bypassed on the HTTP path - admin token IS the auth gate; `cfg.nick_change` is the chat-side self-service feature flag and intentionally does not apply to operator actions via API. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-5 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `PUT /v1/registered/{nick}/level` (admin scope). `cmd_upgrade` plugin v0.22 lets the existing ADC `+upgrade` chat-cmd coexist with the new HTTP endpoint. Body `{level: integer}`; validated against `cfg.levels`. Returns `{action:"level-changed", nick, level, level_name, previous_level, online_kicked}`; online users get the `ISTA 230 ... TL300` kick with the formatted level-change message (matches ADC `+upgrade` kill semantics; the 5-minute TL gives a graceful reconnect window). PUT is idempotent (same-level retry returns 200, no kick). Bots return 404 (humans-only surface matches PR-1 / PR-2 / PR-3 / PR-4). ADC-side `cmd_upgrade_permission` ladder (admin can only promote up to their own ceiling) bypassed on the HTTP path - admin token IS the auth gate, the API can set any level present in `cfg.levels`. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) registered-users family PR-6 ([#236](https://github.com/luadch-ng/luadch-ng/issues/236)) - HTTP API `DELETE /v1/registered/{nick}` (admin scope, `X-Confirm: yes` required per §4.6). `cmd_delreg` plugin v0.31 lets the existing ADC `+delreg` chat-cmd coexist with the new HTTP endpoint. Body `{reason?: string}` - absent / empty performs a plain delreg; non-empty also adds the nick to `cmd_delreg_blacklist.tbl` (so a subsequent `POST /v1/registered` returns 409). Returns `{action:"delreg", nick, blacklisted, online_kicked}`; online users get the `ISTA 230 ... TL-1` immediate-kick with reason banner. Cascade cleanups on success: `cmd_reg_descriptions.tbl`, `cmd_ban.del`, `etc_trafficmanager.del`, `cfg.checkusers()` (matches the ADC `+delreg` v0.29 path). HTTP path is delreg-only (regged user removal); the secondary blacklist-cleanup branch of ADC `+delreg` belongs to a future `DELETE /v1/blacklist/{nick}` endpoint owned by `etc_blacklist`. Bots return 404 (humans-only surface matches PR-1..PR-5). ADC-side `cmd_delreg_permission` ladder bypassed on the HTTP path - admin token IS the auth gate. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). Closes #236. 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) - HTTP API `POST /v1/announce` (admin scope). `cmd_mass` plugin v0.19 unifies the three ADC chat-cmds (`+mass` / `+masshub` / `+masslvl`) behind a single endpoint via the `scope` body field (`"all"` / `"hub"` / `"level"`). Shared `do_announce_all()` / `_hub()` / `_level()` helpers cover ADC + HTTP. Closes the deferred Phase-2-spec entry. 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) - HTTP API `POST /v1/topic` (admin scope). `cmd_topic` plugin v0.04 coexists ADC `+topic` and the new HTTP endpoint via shared `do_set_topic()` / `do_reset_topic()` helpers. Body `{topic?: string}`: missing / empty resets to `cfg.hub_description`, non-empty sets. HTTP callers can literally set topic to the word `"default"` (the ADC `+topic default` magic-keyword does NOT apply because the body expresses reset via absence). Closes the deferred Phase-2-spec entry. 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) - HTTP API `POST /v1/reload` (X-Confirm required). `cmd_reload` plugin v0.04 coexists ADC `+reload` and the new HTTP endpoint. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). Idempotency cache replays a successful reload-retry without double-reloading. Closes the deferred Phase-2-spec entry. 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 3 PR-1 (#225) - HTTP API `POST /v1/restart` (X-Confirm required). `cmd_restart` plugin v0.12 coexists ADC `+restart` and the new HTTP endpoint via a shared `do_restart()` helper. ADC-side `cmd_restart_permission` level table is bypassed on the HTTP path - the bearer token's `admin` scope IS the authorisation gate. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 3 PR-2 (#225) - HTTP API `POST /v1/shutdown` (X-Confirm required). `cmd_shutdown` plugin v0.11 coexists ADC `+shutdown` and the new HTTP endpoint via a shared `do_shutdown()` helper. ADC-side `cmd_shutdown_permission` level table is bypassed on the HTTP path - the bearer token's `admin` scope IS the authorisation gate. Mirror of Phase 3 PR-1 (`POST /v1/restart`). Drive-by ADC fix: `+shutdown` with no comment no longer broadcasts an empty banner (matches `+restart`). Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 3 PR-3 (#225) - HTTP API `GET /v1/log/error?lines=N` (admin scope). `cmd_errors` plugin v0.13 coexists ADC `+errors` and the new HTTP endpoint via a shared `read_log_tail()` helper. Pattern-setter for log-read endpoints (Phase 3 PR-4 `/v1/log/cmd` will mirror). Drive-by ADC fix: `+errors` tail off-by-one corrected (now returns exactly maxlines=200 lines instead of an approximation). Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 3 PR-4 (#225) - HTTP API `GET /v1/log/cmd?lines=N` (admin scope). `etc_cmdlog` plugin v1.3 coexists ADC `+cmdlog show` and the new HTTP endpoint. ADC path unchanged (whole-file dump to chat banner); HTTP path is line-tail per §6.4, same shape as Phase 3 PR-3 (`/v1/log/error`). Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-1 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/chatlog?lines=N` (read scope). `etc_chatlog` plugin v1.6 coexists ADC `+history show` and the new HTTP endpoint via a shared `get_log_tail()` helper. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 3 PR-5 (#225) - HTTP API `DELETE /v1/log/{name}` (admin scope). `etc_log_cleaner` plugin v0.9 coexists ADC `+cleanlog <name>` and the new HTTP endpoint. `{name}` is one of `error` / `cmd` (matching the plugin's supported set; spec was originally aspirational about `event` / `script`, now corrected). Bypasses the ADC-side `activate_X` cfg gates consistent with the rest of Phase 3 - admin token IS the auth gate. Body / response shape documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-2 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/records` (read) + `DELETE /v1/records` (admin). `etc_records` plugin v0.8 coexists ADC `+records show` / `+records reset` and the new HTTP endpoints. Raw byte counts in the response (no unit formatting; consistent with `/v1/stats`). DELETE is not X-Confirm gated - records are recomputed continuously from live hub state, so the lost data is bounded. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-3 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/blacklist` (read) + `DELETE /v1/blacklist/{nick}` (admin). `etc_blacklist` plugin v0.9 coexists ADC `+blacklist show` / `+blacklist del` and the new HTTP endpoints. File loaded on-demand per request (same pattern as cmd_delreg; no cross-plugin staleness window). DELETE returns the deleted entry as `removed` for audit / undo; not X-Confirm gated (single-nick removal is reversible via ADC `+delreg <nick> <reason>`). Drive-by spec fix: blacklist rows moved INTO the cmd_ban table BEFORE the cmd_ban footnote defs - the pre-fix layout had them rendering as plain text after the footnote defs closed the table. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-4 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/runtime` (read) + `PUT /v1/runtime` (admin). `hub_runtime` plugin v0.9 coexists ADC `+runtime show` / `+runtime reset` and the new HTTP endpoints. GET returns raw integer seconds (`session_seconds` + `total_seconds`), no `util.formatseconds` formatting; clients format. PUT generalises ADC's reset-only verb to "set runtime to N" via body `{hubruntime: integer >= 0}` (`PUT {hubruntime: 0}` is the reset shape). PUT also rewrites `hubruntime_last_check` so the next 60s onTimer tick computes diff from now rather than racing the accumulator forward. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-5 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/msgmanager` (read) + `POST /v1/msgmanager/{nick}` (admin) + `DELETE /v1/msgmanager/{nick}` (admin). `etc_msgmanager` plugin v0.8 coexists ADC `+msgmanager` (showusers / showsettings / blockmain / blockpm / blockboth / unblock) and the new HTTP endpoints. GET returns combined view (blocks + cfg blocklevel settings) in a single response. POST body `{mode: "main"|"pm"|"both"}` enum-validated by the router; offline-tolerant (a divergence from ADC's online-only block). DELETE is offline-tolerant too (fixes the pre-existing ADC UX limitation where an offline target could not be unblocked). 409 on already-blocked, 404 on already-unblocked / never-blocked. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-6 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/usercleaner/{expired,ghosts}` (read) + `DELETE /v1/usercleaner/{expired,ghosts}` (admin, both require `X-Confirm: yes`). `cmd_usercleaner` plugin v0.6 coexists ADC `+usercleaner show/del{expired,ghosts}` and the new HTTP endpoints. GET returns the list with per-entry `nick_protected` / `level_protected` flags so operators can preview which rows a subsequent DELETE would skip. DELETE returns three categorised arrays (`deleted`, `skipped_exception`, `skipped_protected_level`) folded into a single structured response (vs ADC's one-banner-per-nick chat stream). Both DELETEs are router-enforced X-Confirm gated - bulk delreg is destructive at scale even though each individual cascade (description + ban + trafficmanager block cleanup) matches the cmd_delreg precedent. Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#82](https://github.com/luadch-ng/luadch-ng/issues/82) Phase 4 PR-7 ([#249](https://github.com/luadch-ng/luadch-ng/issues/249)) - HTTP API `GET /v1/trafficmanager/{settings,blocks}` (read) + `POST/DELETE /v1/trafficmanager/blocks/{nick}` (admin). `etc_trafficmanager` plugin v2.4 coexists ADC `+trafficmanager show/block/unblock` and the new HTTP endpoints. Block + unblock are offline-tolerant. POST body `{reason?}` (control-byte sanitised); 409 on already-blocked, 409 on autoblocked (level in blocklevel_tbl or shares below threshold). DELETE returns the pre-removal entry as `removed` for audit / undo; 404 on not-blocked. Closes the Phase 4 tracker (last PR of 7; #249 to be closed by maintainer after merge). Body / response shape and authorisation semantics documented in [`docs/HTTP_API.md` §10.2](docs/HTTP_API.md). 3.2.x only, not backported.
- [#159](https://github.com/luadch-ng/luadch-ng/issues/159) (Sopor) - pre-compiled `linux-aarch64` release artifact. The `release.yml` workflow gains a `build-linux-aarch64` job on GitHub's native `ubuntu-24.04-arm` runner (Cobalt 100, public-repo-free since 2025). Produces `luadch-vX.Y.Z-linux-aarch64.tar.gz` alongside the existing x86_64 / Windows artifacts on every tag push. Covers Raspberry Pi 3+ / 4 / 5 / Zero 2W with a 64-bit OS (>95% of the installed Pi base in 2026). Backported to `release/3.1.x` as v3.1.9 - the workflow lives in `.github/`, which is mirrored from master for every backport release, so the cherry-pick is purely the workflow file.
- (Boro, [forum thread](https://forum.dchublist.org/viewtopic.php?f=25&t=1195)) - the hub now identifies itself as **Luadch-NG** rather than Luadch on the wire and in user-visible defaults. The ADC `AP` (application) field in `IINF` changes from `APLUADCH` to `APLUADCH-NG` for normal-client, reg-only, and pinger SUP paths; the `IINF` `NI` placeholder in the reg-only pre-auth handshake template changes from `Luadch` to `Luadch-NG`; the `core/const.lua` `PROGRAM_NAME` constant becomes `Luadch-NG` (used in the boot banner and `cmd_hubinfo`); the `FORK` constant shortens to `fork by Aybo` so the banner reads `Luadch-NG vX.Y.Z by blastbeat and pulsar (2007-YYYY), fork by Aybo`. Default `cfg_defaults.hub_name` and `etc_usercommands_toplevelmenu` follow suit ("Luadch-NG Hub" / "Luadch-NG Commands"); existing installations with persisted cfg are unaffected. Rationale: the fork has diverged enough from upstream (Lua 5.4, security-hardening, full build-system rewrite) that hublist scrapers like dchublist.org should track it as distinct software, not as legacy Luadch. 3.2.x only, not backported.
- [#147](https://github.com/luadch-ng/luadch-ng/issues/147) T3.1 (HBRI) - the hub now accepts a BINF carrying BOTH `I4` and `I6` in one frame, so dual-stack peers can advertise both addresses on login. The hub validates the field matching the connecting TCP source's family against `userip` under `kill_wrong_ips`; the OTHER family is forwarded to other clients as-is (unverified). That trade-off is unavoidable - a peer connecting on v4 has no v6 socket through which we could authenticate their v6 address. Documented in [`docs/SECURITY.md`](docs/SECURITY.md). Post-login INF updates still cannot mutate either I4 or I6 (the #97 closeout in `scripts/hub_inf_manager.lua` stays in force; the comment expanded to mention HBRI explicitly so a future contributor does not relax the asymmetry). Spec verification on 2026-05-14: CTM and RCM carry no IP (ADC §6.3.8 / §6.3.9 - confirmed via [adc.sourceforge.io](https://adc.sourceforge.io/ADC.html) and the luadch parser at `core/adc.lua:388-414`), so the original plan-doc claim of an "IP-spoof check at DCTM/ECTM/DRCM/ERCM" was retracted - those handlers stay as pure relays. New positive smoke test `test_binf_with_both_i4_and_i6_accepted`. Second item under Phase 8b (see [`docs/phases/PHASE_8B_DUAL_STACK.md`](docs/phases/PHASE_8B_DUAL_STACK.md)). **Superseded by [#214](https://github.com/luadch-ng/luadch-ng/issues/214):** the "forwarded as-is / unavoidable trade-off" wording no longer holds - the unverified secondary is now stripped before broadcast (Gap 1, [#283](https://github.com/luadch-ng/luadch-ng/pull/283)) and only restored after HBRI side-channel validation ([#284](https://github.com/luadch-ng/luadch-ng/pull/284)). 3.2.x only, not backported.
- [#107](https://github.com/luadch-ng/luadch-ng/issues/107) - dual-stack listening on the same port (HTTP/80-style). `core/server.lua` `_server` registry switches from `port`-keyed to `(port, family)`-keyed; the second `addserver()` call with the same port number on the other family no longer hits the existence check. Operators can now use `tcp_ports = { 5000 }, tcp_ports_ipv6 = { 5000 }` and let one URL serve both stacks instead of publishing two URLs or running an external proxy. The historical `5000` / `5002` split still works unchanged for operators who prefer it. The bundled luasocket forces `IPV6_V6ONLY = 1` on AF_INET6 sockets at creation (`luasocket/src/inet.c` `inet_trycreate`), so the v6 listener does not accidentally also accept v4-mapped traffic - a comment in `core/server.lua` documents this implicit dependency. `docs/CONFIGURATION.md` / `docs/DOCKER.md` updated to reflect the new layout. New smoke test asserts both listeners bind on the same port. First item under Phase 8b (see [`docs/phases/PHASE_8B_DUAL_STACK.md`](docs/phases/PHASE_8B_DUAL_STACK.md)). 3.2.x only, not backported.
- [#137](https://github.com/luadch-ng/luadch-ng/issues/137) (Sopor) - `etc_hubcommands.lua` now catches the literal-bracket mistake. Users who type `[+!#]command` (with the square brackets, reading the doc notation as if it were the actual syntax) get a hint and the broadcast is swallowed - same swallow-and-hint mechanism as the bare-word case (#223). Critical: the hint never echoes the input args, because the args can carry a password when the user typed e.g. `[+!#]reg <user> <pw>`. Matches the literal-bracket form with one or more of `+`, `!`, `#` inside the brackets. Plugin bumped to v0.05. 3.2.x only, not backported.

### Notes

- ⚠️ **Default change** - `ssl_ports_ipv6` default flipped from `{ 5003 }` to `{ 5001 }` to align with `ssl_ports` now that same-port dual-stack is supported since v3.2.x (post-#107). Operators upgrading **without an explicit `ssl_ports_ipv6` in their `cfg/cfg.tbl`** will see the v6 TLS listener move from port 5003 to port 5001 (same as v4). Operators who previously published `adcs://hub.example.com:5003` to v6 clients must EITHER add `ssl_ports_ipv6 = { 5003 }` to their `cfg/cfg.tbl` to keep the historical port OR update their published URLs. The historical 5000/5001/5002/5003 split still works for operators who set it explicitly. Deferred cosmetic flip from Phase 8b (#107). 3.2.x only, not backported.

## [v3.1.8] - 2026-05-12

Modernisation-complete patch release. Concludes the
[#80](https://github.com/luadch-ng/luadch-ng/issues/80) ratelimit v2 work
(four buckets + per-userlevel tiers) and the
[#147](https://github.com/luadch-ng/luadch-ng/issues/147) ADC protocol
coverage T1 line (eight items). After this release, `master` opens
the 3.2.x line; security fixes for 3.1.x go to the
`release/3.1.x` branch per [`CLAUDE.md`](CLAUDE.md#8-release-lines-and-support-policy)
§8.

Smoke 37/37 PASS on Linux + Windows.

### Features

- **Per-userlevel rate-limit tiers** ([#80](https://github.com/luadch-ng/luadch-ng/issues/80), closed) -
  the PM, BINF, and CTM/RCM dispatch paths each get their own
  independent rate-limit bucket on top of the existing chat / search
  buckets ([#138](https://github.com/luadch-ng/luadch-ng/pull/138)
  / [#139](https://github.com/luadch-ng/luadch-ng/pull/139)
  / [#140](https://github.com/luadch-ng/luadch-ng/pull/140)). All five
  buckets become optionally tier-mappable per user level via the new
  `ratelimit_tiers` + `ratelimit_tier_for_level` cfg keys
  ([#141](https://github.com/luadch-ng/luadch-ng/pull/141)) - operators
  can give unreg / guest strict tiers and bots headroom without
  touching the global scalars. Strict-positive validators reject the
  silent-mute failure modes
  ([#142](https://github.com/luadch-ng/luadch-ng/pull/142)
  / [#143](https://github.com/luadch-ng/luadch-ng/pull/143)).
- **ADC protocol coverage** ([#147](https://github.com/luadch-ng/luadch-ng/issues/147), T1 closed)
  - eight protocol-completeness items:
  - **NATT relay** ([#148](https://github.com/luadch-ng/luadch-ng/pull/148)) -
    DNAT / DRNT handlers, hub-relay-only NAT-traversal per ADC-EXT 3.9.
  - **RDEX rich redirect** ([#149](https://github.com/luadch-ng/luadch-ng/pull/149)) -
    `IINF.RP` advertisement + `IQUI.RX` (alternative URLs) / `IQUI.PT`
    (permanent flag) NPs, cfg-driven via `hub_redirect_protocols` /
    `hub_redirect_alternatives` / `hub_redirect_permanent`.
  - **PING completeness** ([#150](https://github.com/luadch-ng/luadch-ng/pull/150)
    / [#146](https://github.com/luadch-ng/luadch-ng/pull/146)) - `SS` /
    `SF` (total share / files), `HE` (email), `MU` / `MR` / `MO`
    (min hubs) added to the `ADPING` reply. `MU/MR/MO` cfg-tunable
    via new `min_user_hubs` / `min_reg_hubs` / `min_op_hubs` keys.
  - **STA emission codes** ([#151](https://github.com/luadch-ng/luadch-ng/pull/151)) -
    `cmd_shutdown` / `cmd_restart` emit `ISTA 212` ("Hub disabled")
    before close; `cmd_ban` switches from 230 / 231 to spec-correct
    `ISTA 232` for finite-TL temporary bans.
  - **FRES routing** ([#152](https://github.com/luadch-ng/luadch-ng/pull/152)) -
    feature-filtered search-result delivery (F-class RES) now
    dispatches alongside DRES.
  - **HQUI from client honored** ([#153](https://github.com/luadch-ng/luadch-ng/pull/153)
    + [#156](https://github.com/luadch-ng/luadch-ng/pull/156)) - clean
    close on client-initiated quit in any state, instead of `ISTA 125`
    unknown-command.
  - **ECTM / ERCM dispatch** ([#146](https://github.com/luadch-ng/luadch-ng/pull/146)) -
    modern E-class CTM / RCM variants now routed.
- **Operator-facing docs** -
  new [`docs/SCRIPTS.md`](docs/SCRIPTS.md)
  ([#144](https://github.com/luadch-ng/luadch-ng/pull/144)) lists every
  bundled plugin with its commands and cfg keys plus the full
  rate-limit configuration guide. README cleaned up
  ([#145](https://github.com/luadch-ng/luadch-ng/pull/145)). Passthrough
  ADC-EXT extensions ([#154](https://github.com/luadch-ng/luadch-ng/pull/154)),
  release-line / support policy ([#155](https://github.com/luadch-ng/luadch-ng/pull/155))
  documented.

### Bugfixes

- **`user.sendsta` typo** ([#151](https://github.com/luadch-ng/luadch-ng/pull/151)) -
  the long-standing `type( flags == "table" )` typo made
  `pairs(nil)` crash whenever a caller omitted the optional flags
  arg. Now `type( flags ) == "table"` matches the docstring contract.
- **`cmd_ban` STA code spec compliance** ([#151](https://github.com/luadch-ng/luadch-ng/pull/151)) -
  three call sites now use 232 (temporary ban with TL) instead of
  231 (permanent ban, no TL) for finite-duration bans.
- **`user.redirect` quitmsg escape**
  ([#156](https://github.com/luadch-ng/luadch-ng/pull/156)) -
  `MS<quitmsg>` was emitted raw; multi-word reasons produced
  malformed ADC. Now `adclib_escape`'d.
- **Ratelimit cfg validator strict-positive**
  ([#142](https://github.com/luadch-ng/luadch-ng/pull/142)) - rate /
  burst / period values must be > 0; pre-fix an operator typo like
  `msg_burst = -1` would silent-mute every non-op user.
- **Smoke battery exercises rate-limit gates correctly**
  ([#156](https://github.com/luadch-ng/luadch-ng/pull/156)) - the PM /
  CTM / RCM / NATT burst tests were short-circuiting at the
  target-lookup before reaching the dispatcher; now self-target so
  the rate-limit code path is genuinely covered.

### Notes

- **No breaking changes at defaults.** All new cfg keys are
  additive with conservative defaults that preserve v3.1.7
  behaviour.
- **ERES (Echo-class search result) is no longer parsed.** ADC 5.3.6
  defines only `D` and `F` classes for RES; previously the parser
  accepted ERES via the broader `[DE]` context and the hub forwarded
  it as E-class. The parser context tightened to `[FD]` as part of
  FRES routing ([#152](https://github.com/luadch-ng/luadch-ng/pull/152)).
  No known client emits ERES; if an exotic NMDC bridge does, it is
  now rejected at parse instead of routed.
- `scripts/lang/cmd_shutdown.lang.{en,de}` and
  `scripts/lang/cmd_restart.lang.{en,de}` add a new `msg_hub_disabled`
  key for the STA 212 message. Operators with custom translations
  for these files need a one-time additive merge; missing keys fall
  back to the hardcoded English string.
- **Release-line model takes effect.** Starting after this release,
  `master` is the 3.2.x active-development line; security-only
  patches for 3.1.x land on `release/3.1.x`. See
  [`CLAUDE.md` §8](CLAUDE.md#8-release-lines-and-support-policy) for
  the full policy including the cherry-pick workflow and the
  "when to backport" decision table.

[v3.1.8]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.8


## [v3.1.7] - 2026-05-11

Plugin data-integrity patch release. `util.savearray` / `util.savetable` are atomic-by-default, defensive `or {}` swept across bundled plugins that load tables, cross-month uptime accounting bug fixed, and `cmd_gag` gains shadowmute mode + duration syntax. Smoke 31/31 PASS on Linux + Windows.

### Features

- [#85](https://github.com/luadch-ng/luadch-ng/issues/85) - `cmd_gag` adds **shadowmute** as a 4th mode plus optional duration on all modes ([#132](https://github.com/luadch-ng/luadch-ng/pull/132)). Shadowmute = target sees their own messages echo but nobody else does. Duration accepts `30s` / `10m` / `2h` / `1d` / `1w` (combinable as `1h30m`); empty = permanent. Auto-expire walks the gag table every 60s. Offline ungag now works via `hub.getregusers()` lookup. Right-click menu gets a "Shadowmute User" entry.
- [#128](https://github.com/luadch-ng/luadch-ng/issues/128) - `encrypt_usertbl` opt-out toggle in `cfg/cfg.tbl` ([#129](https://github.com/luadch-ng/luadch-ng/pull/129)). Default `true` preserves the Phase-7f AES-256-GCM at-rest encryption. `false` writes plaintext `user.tbl` for single-user / home hubs where direct operator read access matters more than disk confidentiality. Auto-detected on read via LDC1 magic prefix - migration is transparent in both directions, master.key auto-generated only when encryption is on.

### Bugfixes

- [#127](https://github.com/luadch-ng/luadch-ng/issues/127) - `usr_uptime` cross-month accounting fix ([#131](https://github.com/luadch-ng/luadch-ng/pull/131)). Sessions spanning month boundaries no longer accumulate as "years" of uptime. New per-tick credit pattern attributes each 60s tick to the calendar month that contains it.
- [#133](https://github.com/luadch-ng/luadch-ng/issues/133) F-PLG-1 / F-PLG-2 - bundled plugin data-integrity sweep:
  - `util.savearray` / `util.savetable` are **atomic-by-default** ([#134](https://github.com/luadch-ng/luadch-ng/pull/134)) via the new public `util.atomic_write(path, content)` helper (tmp + rename, Windows fallback). 21 plugin save sites in the bundled tree get crash-safe writes with zero call-site changes. `cfg_users.lua` delegates to the shared helper for a single source of truth.
  - Defensive `or {}` on 22 `util.loadtable` consumer sites across 12 bundled plugins ([#135](https://github.com/luadch-ng/luadch-ng/pull/135)). Missing / unreadable / parse-fail `.tbl` files no longer crash listeners on first `pairs()` / `ipairs()` / field access. Sites with the existing init-pattern (`check_hci` style) intentionally left as-is - they handle nil correctly and auto-create with defaults.
  - F-PLG-3 (silent no-op on incomplete `+cmd`) audited across 24 bundled scripts - no actionable bugs found, all command handlers already have explicit `msg_usage` fallbacks.

### Notes

- `scripts/lang/cmd_gag.lang.{en,de}` modified: 5 new keys (`msg_invalid_duration`, `msg_add_user_with_duration`, `msg_expired`, `ucmd_duration`, `ucmd_menu_ct1b`); rewritten: `msg_usage`, `help_usage`, `help_desc`, `msg_show_users`. Operators with custom `cmd_gag.lang.*` translations need a one-time merge.
- `examples/cfg/cfg.tbl` gains `encrypt_usertbl = true`. Existing `cfg/cfg.tbl` files without the key default to encrypted (preserves v3.1.6 behaviour). To opt out, add `encrypt_usertbl = false`.
- New public `util.atomic_write` + `util.tabletostring` helpers in `core/util.lua`. Plugins that roll their own save path can route through them for crash-safe writes; companion `luadch-ng/scripts` PRs already use them in `ptx_poll_bot`, `ptx_freshstuff`, `etc_requests`, and `etc_mainecho` (min hub version: v3.1.7).
- Smoke harness: 31/31 PASS unchanged - no new tests this release (audit-discussion outcome was "don't grow the testbench for refactors that don't change the protocol surface").

[v3.1.7]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.7


## [v3.1.6] - 2026-05-09

Security-themed patch release. Hub now defaults to TLS-only with auto-generated self-signed certs on first boot, the password leakage in admin reply paths is closed for `+setpass` / `+accinfo` / `+usersearch`, and Docker deployments pick up new bundled language files automatically. Smoke 14/14 PASS on Linux + Windows.

### Breaking

- Default `cfg/cfg.tbl` ships TLS-only on **both stacks** ([#77](https://github.com/luadch-ng/luadch-ng/issues/77) / [#113](https://github.com/luadch-ng/luadch-ng/pull/113)): IPv4 (`tcp_ports = { }`, `ssl_ports = { 5001 }`) and IPv6 (`tcp_ports_ipv6 = { }`, `ssl_ports_ipv6 = { 5003 }`), with `use_ssl = true`. Existing `cfg/cfg.tbl` files are not migrated - operators upgrading from v3.1.5 keep their plain-port settings on both stacks unless they choose to flip. Fresh installs and Docker first-boot are TLS-only.

### Features

- Auto-generated self-signed P-256 ECDSA cert on first boot when no `servercert.pem` / `serverkey.pem` exists ([#113](https://github.com/luadch-ng/luadch-ng/pull/113)). Pure-Lua + adclib OpenSSL bindings, no `make_cert.{sh,bat}` needed. Keyprint logged to stdout so `docker compose logs` shows it for the operator to share as `adcs://host:port/?kp=SHA256/<base32>`.
- Bundled `slaxml` XML parser at `lib/slaxml/` ([#112](https://github.com/luadch-ng/luadch-ng/pull/112)). Plugins that need to parse XML (e.g. RSS feeds) can now `use "slaxml"` without a separate dep install.
- Docker entrypoint now also adds new bundled `scripts/lang/*.lang.*` files on container start ([#118](https://github.com/luadch-ng/luadch-ng/pull/118)). Strictly add-only - existing translations are never overwritten. Same `LUADCH_AUTOSYNC_SCRIPTS=0` toggle covers both the `*.lua` overwrite-on-diff sync and the new lang add-only sync.

### Bugfixes

- [#95](https://github.com/luadch-ng/luadch-ng/issues/95) - password no longer echoed in admin reply paths ([#119](https://github.com/luadch-ng/luadch-ng/pull/119)). `+setpass` drops the password from the caller's reply (target still receives it via PM, needed for first login); `+accinfo` and `+usersearch` show `<REDACTED>` in the password column. The `+reg` auto-generated password delivery is intentionally unchanged - target needs the value to log in. Three of four sub-tasks of #95 closed.
- [#48](https://github.com/luadch-ng/luadch-ng/issues/48) - `usr_nick_length` now routes its `onFailedAuth` reason and the `ISTA 221` kill message through `scripts/lang/usr_nick_length.lang.{en,de}` instead of hardcoded English ([#117](https://github.com/luadch-ng/luadch-ng/pull/117)). Operator-facing string lands localised in `cmd.log` and any blacklist plugin listening on `onFailedAuth`.
- [#114](https://github.com/luadch-ng/luadch-ng/issues/114) - grammar fix `'an user'` -> `'a user'` in nine plugin headers across `scripts/` and `examples/etc/other_available_scripts/` ([#116](https://github.com/luadch-ng/luadch-ng/pull/116)). Comment-only.

### Notes

- New bundled lang files: `scripts/lang/usr_nick_length.lang.{en,de}`. New lang keys: `msg_redacted` in `cmd_accinfo.lang.{en,de}` and `cmd_usersearch.lang.{en,de}`. Modified key: `msg_ok` in `cmd_setpass.lang.{en,de}` (reads as a complete sentence after the password drop). Operators with custom lang files for these scripts: see release-body for the per-script delta.
- Bug-report and feature-request issue templates added under `.github/ISSUE_TEMPLATE/`.

[v3.1.6]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.6


## [v3.1.5] - 2026-05-07

Patch release on top of v3.1.4. Drop-in upgrade. Closes upstream `luadch/luadch#189` (registered users disappearing) and adds image-side auto-sync of bundled plugin code. Smoke 13/13 PASS on Linux + Windows.

### Bugfixes

- [#108](https://github.com/luadch-ng/luadch-ng/issues/108) / [upstream luadch#189](https://github.com/luadch/luadch/issues/189) - registered users no longer disappear from `user.tbl`. Two root causes: stale file-scope `hub.getregusers()` cache in `cmd_nickchange.lua` (saved snapshots over later `+reg` writes), and non-atomic `cfg_users.saveusers` (truncate-during-write left `user.tbl` partial; `checkusers()` then fell back to a weeks-old `.bak`). Now: `cmd_nickchange.lua` fetches `hub.getregusers()` per call; `saveusers()` writes via `.tmp` + `rename(2)`; `user.tbl.bak` refreshes on every successful save and is byte-identical to `user.tbl`.

### Features

- Docker entrypoint auto-syncs bundled `scripts/*.lua` from the image to the mounted `scripts/` directory on every container start. Plugin bug-fixes now reach existing deployments on `docker compose pull` without manual file copies. Operator-owned state (`scripts/lang/`, `scripts/data/`, `scripts/cfg/`, custom `*.lua`, `cfg/`, `certs/`, `secrets/`) is never touched. Opt-out via `LUADCH_AUTOSYNC_SCRIPTS=0`. See [`docs/DOCKER.md`](docs/DOCKER.md).

### Notes

- `user.tbl.bak` is now refreshed on every successful save (was: only at `+reload`). Operators relying on `.bak` as a stale-rollback should adjust workflows.
- New smoke test: `test_usertbl_bak_atomic_refresh` verifies `.bak` byte-equality with `user.tbl` after writes (12 -> 13 tests).

[v3.1.5]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.5


## [v3.1.4] - 2026-05-07

Patch release on top of v3.1.3. Drop-in upgrade; no cfg / on-disk-format changes. Smoke 12/12 PASS on Linux + Windows. First release with an official container image.

### Features

- [#87](https://github.com/luadch-ng/luadch/issues/87) - Pure-rootless container image on `ghcr.io/luadch-ng/luadch` (linux/amd64 + linux/arm64). Compose-file at repo root, operator guide at [`docs/DOCKER.md`](docs/DOCKER.md).

### Bugfixes

- [#97](https://github.com/luadch-ng/luadch-ng/issues/97) - `kill_wrong_ips` defaults to `true`; `I4` / `I6` added to `hub_inf_manager` forbidden-on-INF flags (closes IP-spoof on post-login INF). Operator opt-out for NAT-weird deployments documented in `docs/SECURITY.md` § 5.
- [#103](https://github.com/luadch-ng/luadch-ng/issues/103) - `etc_motd`: multi-placeholder MOTDs no longer crash on login; new `{nick}` template form, `%s` kept for backwards compat.

### Notes

- `docker.yml` workflow build/push status badge added to README.
- Audit meta tracker [#98](https://github.com/luadch-ng/luadch-ng/issues/98) closed; remaining deferral [#95](https://github.com/luadch-ng/luadch-ng/issues/95) (admin reply paths) stays Phase-8.

[v3.1.4]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.4


## [v3.1.3] - 2026-05-07

Security patch on top of v3.1.2. Drop-in upgrade; no cfg / on-disk-format changes. Smoke 12/12 PASS on Linux + Windows.

### Bugfixes

- [#91](https://github.com/luadch-ng/luadch-ng/issues/91) - Pre-auth DoS via reg_only nick collision (HPAS-gated takeover)
- [#92](https://github.com/luadch-ng/luadch-ng/issues/92) - Encrypted `user.tbl` bypass in `cmd_setpass` / `cmd_nickchange` / `cmd_upgrade`
- [#93](https://github.com/luadch-ng/luadch-ng/issues/93) - `master_key_path` POSIX shell-quoting in `cfg_secret`
- [#94](https://github.com/luadch-ng/luadch-ng/issues/94) - Failed-auth state pollution in HPAS handler
- [#96](https://github.com/luadch-ng/luadch-ng/issues/96) - `etc_cmdlog` redacts password-bearing arguments via new `etc_cmdlog_redact_args` cfg key

### Notes

- Smoke regression test added for #92 (`test_setpass_preserves_encryption`)
- Deferred to Phase-8 hardening: [#95](https://github.com/luadch-ng/luadch-ng/issues/95), [#97](https://github.com/luadch-ng/luadch-ng/issues/97)
- Audit meta tracker: [#98](https://github.com/luadch-ng/luadch-ng/issues/98)

[v3.1.3]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.3


## [v3.1.2] - 2026-05-06

Patch release. Drop-in upgrade from v3.1.1; no cfg / on-disk-format
changes, no Lua API changes. Single fix: bundled LuaSocket / LuaSec
now install in the canonical layout so plugins doing
`require "socket.http"` / `require "ssl.https"` load drop-in.

### Fixed

- **Canonical LuaSocket / LuaSec install layout** (closes
  [#88](https://github.com/luadch-ng/luadch-ng/issues/88)). The Lua-side
  helpers were installed flat under `lib/luasocket/lua/` and
  `lib/luasec/lua/`, which broke `require "socket.http"` and
  `require "ssl.https"` for plugins. Even calling `require "http"`
  directly didn't help because `http.lua`'s own internal
  `require "socket.url"` and `require "socket.headers"` hit the same
  wall. Split the CMake `install(FILES ...)` rules so entrypoints
  (`socket.lua`, `mime.lua`, `ltn12.lua`, `mbox.lua`, `ssl.lua`) stay
  top-level and `socket.X` / `ssl.X` submodules go into nested
  subdirectories. Source files unchanged; hub-internal usage
  (`use "socket"`, `use "ssl"` only) unaffected. Unblocks the
  [`luadch-ng/scripts ptx_RSSFeedWatch`](https://github.com/luadch-ng/scripts)
  plugin and any future HTTP-using plugin.

### Added

- **Smoke regression test** for the canonical layout
  (`tests/smoke/run.py:test_canonical_socket_layout`). Static
  path-exists check on the LuaSocket / LuaSec submodule paths; if a
  future CMake change drifts back to the flat bundling, the smoke
  gate fires. Smoke harness now runs 11 protocol-level + state-of-disk
  tests (was 10).

[v3.1.2]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.2


## [v3.1.1] - 2026-05-05

Patch release. Drop-in upgrade from v3.1.0; no cfg / on-disk-format changes,
no Lua API changes, smoke harness still 10 / 10. Two upstream-issue triage
rounds plus a small post-modernisation cleanup landed since v3.1.0.

### Fixed

- **DSCH search fanout** (closes upstream
  [luadch#200](https://github.com/luadch/luadch/issues/200), security).
  `etc_trafficmanager.lua` `onSearch` was fanning out direct (D / E type)
  searches to every user; receivers logged a `SECURITY WARNING: received
  a DSCH message that should have been sent to a different user.` for
  every cross-addressed message. The hub's default direct-routing path
  already delivers correctly to the single intended SID; the fan-out was
  redundant and protocol-violating. Same root cause as upstream
  [luadch#226](https://github.com/luadch/luadch/issues/226) (AirDC++ 4.21
  "search spam detected (severe)" disconnects).
- **Negative DS in INF blocked login** (closes upstream
  [luadch#241](https://github.com/luadch/luadch/issues/241)).
  `_regex.integer` now accepts signed decimals (`^%-?%d+$`) so a single
  buggy field doesn't cause the parser to drop the entire BINF and
  reject the login.
- **`etc_trafficmanager.lua` no message / no `[BLOCKED]` flag with
  custom prefix scripts** (closes upstream
  [luadch#240](https://github.com/luadch/luadch/issues/240)). Resolve
  blocked target via firstnick iteration instead of computing
  `prefix + firstnick` and looking up `hub.isnickonline()`. Robust
  against any nick-prefix scheme.
- **`+delreg <blacklisted-nick>` failed** (closes upstream
  [luadch#228](https://github.com/luadch/luadch/issues/228)). Extend
  the command to remove a blacklist entry when the target is not (or
  no longer) registered. New `blacklist_del()` helper +
  `msg_deblacklist` lang string.
- **Forgot-prefix commands leaked into main chat** (closes upstream
  [luadch#223](https://github.com/luadch/luadch/issues/223)). New
  `onBroadcast` fallback in `etc_hubcommands.lua`: if a message starts
  with a known command name as a whole word and is shaped like a
  forgotten command, swallow the broadcast and reply with a prefix
  hint.
- **`+help cmd_mass` showed misleading min-level** (closes upstream
  [luadch#217](https://github.com/luadch/luadch/issues/217)).
  `util.getlowestlevel()` returns just the lowest TRUE-keyed level,
  hiding gaps in the permission table. Append the actual permitted-level
  list to `help_desc` at script-load time, formatted with level names
  where available.
- **Cryptic "wrong sslctx parameters" on first Windows run** (closes
  upstream [luadch#177](https://github.com/luadch/luadch/issues/177)).
  `core/server.lua` `wrapserver` now `io.open`-pre-checks
  `sslctx.{key,certificate,cafile}` and surfaces a human-readable hint
  pointing at `certs/make_cert.{sh,bat}` or `use_ssl = false`.
- **Multi-byte nicks rejected at lower codepoint counts than ASCII**
  (refs [#48](https://github.com/luadch-ng/luadch-ng/issues/48)).
  `usr_nick_length.lua` switched from `#nick` (byte length) to
  `utf.len(nick)` (codepoint length). `min/max_nickname_length` is
  documented in codepoints; Cyrillic / multi-byte nicks were tripping
  the threshold sooner than intended.
- **`hub_inf_manager.lua` failure reason hardcoded English**
  (refs [#48](https://github.com/luadch-ng/luadch-ng/issues/48)). Routed
  through the existing per-script lang file as `msg_failedauth_reason`.
- **Smoke harness Windows-only hang** (refs
  [#48](https://github.com/luadch-ng/luadch-ng/issues/48)).
  `subprocess.run(..., capture_output=True)` lets `cmd.exe` inherit
  the parent console; `make_cert.bat` ends with `pause` and blocked
  forever waiting for a keypress when the harness was invoked from an
  interactive shell. Pass `stdin=subprocess.DEVNULL` so `pause` sees
  EOF and exits cleanly. CI was unaffected (Linux uses
  `make_cert.sh`, no pause).

### Audited as already addressed by an earlier fix

Documented in [`docs/phases/INTERLUDE_UPSTREAM_TRIAGE_2.md`](docs/phases/INTERLUDE_UPSTREAM_TRIAGE_2.md)
so the next triage round doesn't re-discover them.

- Upstream [luadch#214](https://github.com/luadch/luadch/issues/214)
  (failed-auth hammering): handled by Phase 7c F-AUTH-3 (per-IP
  failed-auth lockout).
- Upstream [luadch#221](https://github.com/luadch/luadch/issues/221)
  (search protection): handled by Phase 7c F-RL-2 (per-user search
  rate cap).
- Upstream [luadch#226](https://github.com/luadch/luadch/issues/226)
  (AirDC++ 4.21 search-spam disconnect): same root cause as #200,
  fixed there.
- Upstream [luadch#230](https://github.com/luadch/luadch/issues/230)
  (sporadic invalid-password): symptom matches the unresolved
  data-loss [luadch#189](https://github.com/luadch/luadch/issues/189);
  same workaround (`+reload`).
- Upstream [luadch#236](https://github.com/luadch/luadch/issues/236),
  [luadch#237](https://github.com/luadch/luadch/issues/237),
  [luadch#238](https://github.com/luadch/luadch/issues/238),
  [luadch#242](https://github.com/luadch/luadch/issues/242):
  resolved or made unreachable by Phase 6 / Phase 7 work.

### Other

- Repo transferred from `Aybook/luadch` to `luadch-ng/luadch`
  (auto-redirects keep historic links working). Project page:
  <https://luadch-ng.github.io/>. Doc references and release notes
  updated to point at the new path; existing operator install trees
  need no action.

[v3.1.1]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.1


## [v3.1.0] - 2026-05-04

Phase 6 (refactor + smoke harness) and Phase 7 (security audit + hardening)
of the modernisation programme. No breaking changes for operators on
default cfg; existing `cfg/user.tbl` is transparently migrated to the
new encrypted-at-rest format on first save after upgrade. Phase journals
in [`docs/phases/PHASE_6.md`](docs/phases/PHASE_6.md) and
[`docs/phases/PHASE_7.md`](docs/phases/PHASE_7.md).

### Added

#### Phase 7 - security features

- **AES-256-GCM at-rest encryption of `cfg/user.tbl`** (Phase 7f, F-AUTH-1).
  New module `core/cfg_secret.lua`; auto-managed master key at
  `cfg/master.key` with chmod 600 enforcement (refuse-to-start on POSIX
  if mode != 0600). New cfg key `master_key_path` (Phase 7h) lets
  operators move the key outside the install directory for backup
  separation - strongly recommended for production. See
  [`docs/SECURITY.md`](docs/SECURITY.md) §3.
- **DoS hardening** (Phase 7c, F-NET-1 / F-NET-2 / F-AUTH-3 / F-RL-1 /
  F-RL-2). New module `core/ratelimit.lua` with token-bucket counters:
  per-IP parallel-socket cap (default 16, NAT-friendly), per-IP accept
  rate, TLS handshake wallclock deadline (default 10 s, slowloris cut),
  per-IP failed-auth lockout (independent of per-account
  bad_pass_timeout), per-user chat / search rate limits with op-level
  bypass. All cfg-tunable.
- **Sandboxed config / state loaders** (Phase 7e, F-FIO-1, **critical**).
  `util.loadtable()` runs chunks with empty `_ENV` so a tampered `.tbl`
  file cannot reach `os` / `io` / `debug` / `package` / `require`.
  Eliminates the universal RCE-on-tampered-file class for `cfg.tbl`,
  `user.tbl`, language files, and the 20+ plugin state files. Format
  on disk unchanged - zero migration.
- **POSIX file-permission enforcement on secret files** (Phase 7b, F-SEC-1).
  Hub `chmod 600`s `user.tbl` and `user.tbl.bak` after every write;
  `make_cert.sh` 0600s the generated TLS private keys. Windows: `icacls`
  recipe in [`docs/BUILDING.md`](docs/BUILDING.md) and
  [`docs/SECURITY.md`](docs/SECURITY.md) §4.
- **Secure salt generation** (Phase 7b, F-AUTH-2). HPAS challenge salts and
  SIDs now drawn from OpenSSL `RAND_bytes` via the new
  `adclib.random_bytes` C function instead of `math.random` reseeded
  with `os.time()`. New `adclib.aes_gcm_seal` / `aes_gcm_open` C
  functions wrap OpenSSL EVP_CIPHER for the at-rest encryption.
- **ADC parser hardening** (Phase 7d, F-PRS-1..5).
  - `_regex.default` and `_regex.nowhitespace` now reject raw control
    bytes (`%c`); the previous validators were a no-op and a literal
    `\\n`/`\\s` substring search respectively.
  - `parse()` is reentrant: `_buffer` / `_clone` / `_eol` are
    parse-locals instead of module-globals.
  - `MAX_COMMAND_SIZE = 64 KiB` cap at parser entry; UTF-8 check
    re-enabled at parse entry.
  - Unknown two-letter named parameters now go through the same
    control-byte-rejecting default validator as known fields.
- **adclib hardening** (Phase 7b, F-C-1 + F-C-3).
  - `hash_pas` / `hash_pas_oldschool` no longer use VLAs sized by
    Lua-side input; salt length is bounded against
    `MAX_SALT_BYTES = 64`.
  - `luaL_checklstring` everywhere binary-safe Lua strings are
    expected, so an embedded NUL in a password no longer silently
    truncates the input.
- **Tiger hash hygiene** (Phase 7g, F-C-2 + F-C-5). Explicit parens on
  `pos & ( BLOCK_SIZE - 1 )`; `tigerCompress` receives memcpy'd
  aligned `unsigned long long[]` instead of an aliased `tmp` cast;
  bit-length finalize uses memcpy at offset 56.

#### Phase 7 - documentation

- **`docs/SECURITY.md`** (new top-level doc): threat model, plugin trust
  contract, F-AUTH-1 protocol-mandated cleartext disclosure, file-
  permission baseline (Linux + Windows `icacls`), network-defense map,
  TLS configuration, CVE-tracking process, security-issue reporting
  channel, audit history.
- **`docs/phases/PHASE_7_FINDINGS.md`**: full security-audit findings
  document (24 findings with severity + evidence + fix direction); each
  finding linked to its closing PR.
- **`docs/phases/PHASE_7.md`**: phase journal with the 12-PR list,
  recurring techniques, module-state shapes, review-gate findings.

#### Phase 6 - refactor + tests

- **CI smoke harness**: 10 protocol-level tests (plain + TLS handshake,
  full ADC login, +cmd routing, CSPRNG-salt-uniqueness, per-IP
  connection cap, encrypted-at-rest user.tbl, no-script-errors-in-log)
  on every push and PR via `.github/workflows/smoke.yml`. Both Linux
  (ubuntu-latest) and Windows (windows-latest with msys2 UCRT64).
- Vendored Tiger-192 implementation as `tests/smoke/tiger.py` so the
  smoke client and the hub agree on the hash by construction.
- **`docs/phases/PHASE_6.md`** journal.

### Changed

#### Phase 7

- Bundled Lua **5.4.7 → 5.4.8** (Phase 7b, F-DEP-1). Drop-in upstream
  patch-version sync; ABI-stable.
- `find_package(OpenSSL 3.0 REQUIRED)` in CMakeLists.txt - refuse to
  link against EOL'd OpenSSL 1.1.1 (Phase 7g, F-DEP-4).
- `basexx/basexx.lua` carries a provenance header (upstream tag,
  retrieval date, license, MIT) for future auditability (Phase 7g,
  F-DEP-3).
- `hub/hub.c` `restart()` checks the `atexit()` return value and exits
  loudly when the registration limit is hit, instead of silently
  exiting cleanly without re-entering Lua (Phase 7g, F-C-4).
- `core/util.lua` gains `chmod_secret`, `arraytostring`, and
  `loadtable_string` helpers used by the encryption path.

#### Phase 6

- **`core/cfg.lua` 3688 → 668 lines** (Phase 6c). Decomposed into:
  - `core/cfg_defaults.lua` (~3000-line data table, ceiling-exempt)
  - `core/cfg_users.lua` (user.tbl I/O)
  - `core/cfg_lang.lua` (language file loader)
- **`core/hub.lua` 2245 → 1497 lines** (Phase 6d). Decomposed into:
  - `core/hub_user_object.lua` (`createuser` factory)
  - `core/hub_bot_object.lua` (`createbot` factory)
  - `core/hub_dispatch.lua` (ADC state-machine handler tables)
- All code modules now under the 1500-line ceiling, with
  `cfg_defaults.lua` exempt as a flat data table.

### Fixed

- **Path anchoring** (Phase 6b, closes #12): hub `chdir`s to its binary
  directory at startup so `log/exception.txt` and other relative paths
  resolve correctly regardless of how the hub is launched.
- TODO/FIXME audit (Phase 6e) and dead-file removal (`core/test.lua`,
  `slnunicode/.travis.yml`).

### Security

Full audit findings in
[`docs/phases/PHASE_7_FINDINGS.md`](docs/phases/PHASE_7_FINDINGS.md).
Severity rollup: 24 findings filed; 22 fixed; 1 deferred-by-design
(F-AUTH-1 transparent KDF migration is ADC-protocol-immanent and
mitigated via at-rest encryption + chmod 600 + configurable
`master_key_path`); 1 stays `upstream-blocked` (F-DEP-2 LuaSec
OpenSSL-3 deprecated APIs, tracked as #3).

### Migration

- **Operators on v3.0.0**: upgrade is in-place. First boot generates
  `cfg/master.key`; first post-login save migrates `user.tbl` from
  plaintext to AES-256-GCM-encrypted. **Strongly recommended**: set
  `master_key_path = "/etc/luadch/master.key"` (or your preferred
  external path) in `cfg/cfg.tbl` and move the key file before
  putting real users into `user.tbl`. See
  [`docs/SECURITY.md`](docs/SECURITY.md) §3 "Backup separation".
- **Existing world-readable secrets**: run once on POSIX after upgrade:

  ```sh
  chmod 600 cfg/user.tbl cfg/user.tbl.bak certs/serverkey.pem certs/cakey.pem
  ```

  On Windows, see the `icacls` recipe in
  [`docs/SECURITY.md`](docs/SECURITY.md) §4.

[v3.1.0]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.1.0


## [v3.0.0] - 2026-05-03

First release of the modernised `Aybook/luadch` fork. Forked from upstream
`luadch/luadch` source `v2.24 [RC4]`; upstream's last public release was
`v2.23` (2022-04-02).

This release lifts the project off Lua 5.1 (EOL since 2012), replaces the
ad-hoc Linux/Windows shell+batch build pipeline with a single CMake build,
and ships several pre-existing-bug fixes on top.

### Breaking changes

- **Lua runtime upgraded from 5.1.5 to 5.4.7.** Plugins relying on
  `setfenv`, `getfenv`, `loadstring`, `unpack`, `module(...)`, `math.atan2`,
  `math.pow`, `math.log10`, `LUA_MAXCAPTURES`, etc. will not load - update
  to 5.4 idioms (`load`, `table.unpack`, explicit `_ENV`, `math.atan` two-arg
  form).
- **Build system replaced with CMake.** The old `compile` shell script and
  `compile_with_mingw.bat` are gone (including the `*.c.not` source-rename
  hack). The new build is `cmake -B build && cmake --build build && cmake --install build`
  on every supported platform.
- **`slnunicode` C module replaced by a 40-line pure-Lua shim** built on
  Lua 5.4's builtin `utf8` library. Same surface API as the old `unicode`
  table; pattern-matching call sites confirmed ASCII-only by audit. Plugins
  using non-trivial Unicode-class patterns (`%l` against German umlauts,
  etc.) would need a dedicated function added to the shim.

### Added

- CMake build system covering Linux x86_64, Windows x86_64 (MinGW UCRT),
  and ARM aarch64 (cross-compile from Linux). Same three-step pipeline
  on every platform.
- GitHub Actions release workflow that builds Linux + Windows binaries
  and attaches them to the GitHub release on tag push.
- `.gitattributes` enforcing platform-correct line endings (CRLF for
  `.bat` / `.cmd`; LF everywhere else).
- `docs/BUILDING.md` rewritten with platform-specific Linux / Windows /
  ARM sections, OpenSSL cross-compile recipe, ARM cross-toolchain setup.
- `docs/INSTALLING.md`: deployment guide (Linux service-user pattern,
  systemd unit, Windows NSSM service, backups, update procedure).
- `docs/CONFIGURATION.md`: post-install configuration reference (first-run
  checklist, cfg.tbl tour, plugin categories, troubleshooting).
- `docs/phases/PHASE_{1..5}.md` and `INTERLUDE_UPSTREAM_TRIAGE.md`:
  per-phase modernisation journals.
- New `msg_del_reason` template in `cmd_delreg.lua` so deleted users see
  the reason on disconnect (en + de language files updated).

### Changed

- Hub launcher `hub/hub.c`: `lua_open()` -> `luaL_newstate()` (5.4 API).
- `adclib/adclib.cpp`: `luaL_reg` -> `luaL_Reg`,
  `luaL_register` -> `luaL_newlib + return 1`.
- Windows build: `wmic` calls in `cmd_hubinfo.lua` replaced with
  PowerShell `Get-CimInstance` (Windows 11 24H2+ removed `wmic`).
- CMake's Windows OpenSSL DLL bundling now searches both flat
  (`$ROOT/`) and bin-subdir (`$ROOT/bin/`) layouts so msys2 UCRT64 and
  WinLibs / ShiningLight Win64 both work without manual flattening.

### Fixed

- `os.difftime` 1-arg call pattern (silently tolerated in 5.1, errors in
  5.4): 12 scripts updated to `os.time() - x`.
- `cmd_hubinfo.lua` crash on missing certificate file (`get_certinfos`
  missing return).
- `+!#` server commands sent as PMs to the hubbot now route through the
  command pipeline again
  ([PR #13](https://github.com/Aybook/luadch/pull/13)).
- `make_cert.sh`: `UID` variable collision with bash's read-only builtin -
  renamed to `RAND_ID`.
- `make_cert.bat`: silently produced certs with an empty CN on
  OpenSSL 3.5+ because `openssl rand -hex 16 -out X` requires the
  positional `<num>` last; reordered. Also dropped OpenSSL-1.0.x-era
  `RANDFILE` legacy.
- `register` keyword warnings in `adclib/tiger.cpp` (C++17 deprecation).
- `cmd_delreg.lua` help-text fallback corrected: "delregs an existing
  user".
- `cmd_usercleaner.lua`: `+usercleaner showghosts` crashed when any
  registered user lacked a `date` attribute - guard `reg_date` before
  comparison.
- `usr_uptime.lua`: crashed on first user login after the database file
  was missing or unparseable - removed the `else` branch that returned
  early with an empty table before the entry-setup ran.
- `+delreg <nick> <reason>` now relays the reason to the deleted user
  before kicking, instead of the silent generic "You were delregged."
- `+shutdown` and `+restart` countdowns now block main-chat broadcasts so
  users cannot type during the countdown.

### Removed

- Legacy build scripts: `compile`, `compile_with_mingw.bat`, `cleanall`.
- The `*.c.not` rename trick the Windows build used to skip Unix-only
  LuaSocket sources.
- Unmaintained `slnunicode` C module (1366 lines of C, replaced by the
  40-line Lua shim).

### Security

- TLS 1.3 + AES-256-GCM verified end-to-end after the modernisation.
- `cfg/cfg.tbl` and `cfg/user.tbl` documented as 0600 by deployment
  guide (they hold the default-account password and the registered-user
  hashes respectively).

### Phase journals

For the full per-phase narrative (activities, design decisions, build
output specs, review-gate checklists), see [`docs/phases/`](docs/phases/).

[v3.0.0]: https://github.com/luadch-ng/luadch-ng/releases/tag/v3.0.0
